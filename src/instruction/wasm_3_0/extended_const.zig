//! Wasm 3.0 `extended_const` proposal — reserved per ADR-0023 §3 P-H.
//!
//! Implementation lands when the Phase 8+ proposal phasing
//! opens this slot per ROADMAP §11. `extended_const` adds no
//! new opcodes (const-expression extension only); the file
//! exists so the Wasm 3.0 surface is discoverable at a glance.
//!
//! Zone 1 (`src/instruction/`).
