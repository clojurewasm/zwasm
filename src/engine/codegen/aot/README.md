# engine/codegen/aot

Reserved subsystem slot per ADR-0023 §3 reference table + §3 P-H
(future-state accommodation).

Implementation phase: ROADMAP §11 / Phase 8+ skeleton; finalised
in Phase 12. The `format.zig` (.cwasm header + serialization) +
`linker.zig` (AOT relocation) shape is set per ADR-0023 §3
reference table.

Until Phase 8 opens, this directory is intentionally empty
except for this README.
