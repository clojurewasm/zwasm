//! `zwasm wat` subcommand — placeholder per ADR-0023 §3
//! reference table + §3 P-H (future-state accommodation).
//!
//! Implementation phase: ROADMAP §10 lists this subcommand as a
//! later-phase addition (Phase 11 / 12 depending on the slot).
//! Until the phase opens, src/main.zig dispatches only `run`;
//! the placeholder exists so the subcommand directory shape is
//! visible from day 1.
//!
//! Zone 3 (`src/cli/`).
