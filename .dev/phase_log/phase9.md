# Phase 9 execution log

> **Doc-state**: ARCHIVED-IN-PLACE

> Sub-chunk records absorbed from `.dev/ROADMAP.md` §9.9 task list
> (Phase 9 task table) on 2026-05-11. ROADMAP itself is a now-snapshot
> per §18; per-sub-chunk prose lives here, not there. Authoritative
> history is `git log` — this file is a readable grouping by row.


## Row 9.5 — ARM64 emit (NEON): load/store + lane access + int arith

**Scope**: ARM64 emit (NEON): SIMD load/store + lane access + integer arithmetic..

**Status**: [x]

### Sub-chunks (commit-time order)

- **9.5-a** — encoder foundation `[x]`
- 9.5-b-i/ii/iii (shape-tag pipeline + per-op handlers + dispatch `[x]`)
- 9.5-c-i/ii (Q-form v128 spill helpers + op_simd refactor `[x]`)
- **9.5-c-iii** — i32x4 extract/replace_lane via UMOV/INS `[x]`
- **9.5-c-iv** — i8x16/i16x8/i64x2 add+sub via shared `emitV128Binop` helper `[x]`
- **9.5-c-v** — i16x8/i32x4 NEON MUL via encMul8H/encMul4S; encMul16B preserved for completeness though Wasm has no i8x16.mul `[x]`
- **9.5-c-vi** — int lane access for B/H/D element forms — i8x16 / i16x8 / i64x2 extract_lane{,_s,_u} + replace_lane via UMOV / SMOV / INS B/H/D encoders + shared `emitV128ExtractLane` / `emitV128ReplaceLane` helpers `[x]`
- **9.5-c-vii** — f32x4 / f64x2 extract_lane / replace_lane via DUP-scalar S/D + INS-element S/D `[x]`
- 9.5-c-vii-mul (i64x2.mul synthesis via per-lane UMOV X.D / scalar X-form MUL / INS X.D, X16 / X17 IP0 / IP1 scratch reused from existing convention `[x]`).

## Row 9.6 — ARM64 emit (NEON): cmp + shuffle + FP arith + convert

**Scope**: ARM64 emit (NEON): SIMD comparison + shuffle + float arithmetic + conversion..

**Status**: [x]

### Sub-chunks (commit-time order)

- **9.6-a** — f32x4 / f64x2 binary FP arith — FADD/FSUB/FMUL/FDIV `[x]`
- **9.6-b** — FP unary — sqrt/abs/neg/ceil/floor/trunc/nearest via FABS/FNEG/FSQRT/FRINT* + shared `emitV128Unop` helper `[x]`
- **9.6-c-i** — FP min/max via FMIN/FMAX, NaN-propagating `[x]`
- **9.6-c-ii** — FP pmin/pmax synthesis via FCMGT + BSL using V31 as SIMD scratch; pseudo-min/max with zero-on-equal-magnitude `[x]`
- **9.6-d** — int compare — 36 ops: eq/ne/lt/gt/le/ge × signed+unsigned for i8x16/i16x8/i32x4 + signed-only for i64x2 — via CMEQ/CMGT/CMHI/CMGE/CMHS + NOT V16B for ne synthesis + emitV128BinopSwapped helper for lt/le `[x]`
- **9.6-e** — FP compare — 12 ops: eq/ne/lt/gt/le/ge × f32x4/f64x2 via FCMEQ/FCMGT/FCMGE; reuses 9.6-d helpers `[x]`
- **9.6-f-i** — i8x16.swizzle via NEON TBL 1-register form `[x]`
- **9.6-f-ii** — i8x16.shuffle + v128.const codegen via per-function const-pool with PC-relative LDR-Q-literal + post-emit fixup pass per ADR-0042; copy-to-V30/V31 preamble for TBL 2-register form `[x]`
- **9.6-g** — conversion family — multi-chunk): 9.6-g-i (extend low/high, 12 ops, SXTL/UXTL `[x]`
- **9.6-g-ii** — narrow saturating, 4 ops via SQXTN/SQXTN2 + SQXTUN/SQXTUN2 — Wasm narrow_*_u uses SQXTUN per cranelift cross-check `[x]`
- **9.6-g-iii** — FP convert i→f, 4 ops via SCVTF/UCVTF; f64x2.convert_low uses SXTL/UXTL+SCVTF/UCVTF synthesis `[x]`
- **9.6-g-iv** — FP promote/demote — FCVTL .2D-.2S + FCVTN .2S-.2D, 2 ops `[x]`
- 9.6-g-v (trunc_sat 4 ops via FCVTZS/U .4S + FCVTZS/U .2D → SQXTN/UQXTN .2S synthesis; NEON NaN→0 + sat semantics match Wasm spec without explicit NaN-mask `[x]`).

## Row 9.7 — x86_64 emit (SSE4.1+SSE4.2 baseline)

**Scope**: x86_64 emit (SSE4.1+SSE4.2 baseline per ADR-0041 §5 amend at 9.7-m): SIMD load/store + lane access + integer arithmetic.

**Status**: [x]

### Sub-chunks (commit-time order)

- **9.7-a** — foundation: PADDD encoder + i32x4.add handler via op_simd.zig + emit dispatch wiring; spilled v128 surfaces UnsupportedOp until later MOVDQU helpers; mirror of 9.5-b-iii single-op foundation per ADR-0041 §"Decision" / 2 `[x]`
- **9.7-b** — packed int add/sub bundle: 7 new encoders encPaddB/W/Q + encPsubB/W/D/Q via shared `encSsePackedIntBinop(opcode, ...)` factor; shared `emitV128IntBinop(encoder)` handler helper extracted from 9.7-a + 8 1-line per-op wrappers per ARM64 mirror P7; 8 dispatch arms total `[x]`
- **9.7-c** — native packed int multiply: extends helper to `encSsePackedIntBinopExt(escape2, opcode, ...)` for SSE4.1 second-escape form; encPmullW (SSE2) + encPmullD (SSE4.1 — first SSE4.1-exclusive op per ADR-0041 §5); reuses emitV128IntBinop without ABI change; 10 SIMD ops handled total `[x]`
- **9.7-d** — i64x2.mul synthesis: 11-instruction PMULUDQ + shift/add idiom — no native SSE4.1 form, AVX-512 VPMULLQ gated by baseline; new encoders encPmuludq + encPsrlqImm + encPsllqImm via `encSsePackedShiftImmGroup` /X-group factor; SIMD scratch reuses fp_spill_stage_xmms (XMM14/15) without ABI change per p9-9.7-d-survey scratch-strategy-B; 11 SIMD ops handled total `[x]`
- **9.7-e** — lane access foundation: encPshufd (SSE2 70 /r ib) + encPextrD (SSE4.1 3A 16 /r ib — ModR/M.reg carries source XMM, .r/m the GPR); emitI32x4Splat (MOVD + PSHUFD broadcast) + emitI32x4ExtractLane (single PEXTRD); enables end-to-end pipeline for the 13 SIMD ops handled total but edge fixtures defer to §9.9 since the existing runner is Mac-aarch64-only `[x]`
- **9.7-f** — replace_lane wide-int pair: encPinsrD (RVMI 3A 22 /r ib) + encPinsrQ (REX.W mandatory) — XMM dst in ModR/M.reg, GPR src in r/m (opposite of PEXTR's RMI shape); parametric helper emitV128IntReplaceLane32Or64(is_64) + 1-line per-op wrappers with MOVAPS-elision when dst aliases vec; 15 SIMD ops handled total `[x]`
- **9.7-g** — narrow-int extract+replace: encPextrB (SSE4.1 RMI 3A 14) + encPextrW (SSE2 0F C5, opposite REX role with gpr in reg/xmm in r/m) + encPinsrB (SSE4.1 RVMI 3A 20) + encPinsrW (SSE2 0F C4 RVMI); parametric extract helper covering 4 variants (signed via MOVSX r32, r8/r16; unsigned natively zero-extended) + parametric replace helper covering 2 variants; 21 SIMD ops handled total `[x]`
- **9.7-h** — integer splat trio i8x16/i16x8/i64x2: new encoders encPxor (SSE2 0F EF) + encPshufb (SSSE3 0F 38 00) + encPshuflw (F2 0F 70 /r ib) + encPunpcklqdq (66 0F 6C); i8x16 uses XMM14 scratch for the all-zero PSHUFB ctrl mask; i16x8 chains PSHUFLW + PSHUFD; i64x2 self-unpack low qwords; 24 SIMD ops handled total `[x]`
- **9.7-i** — f32x4 lane access trio: new encoder encInsertps (SSE4.1 3A 21 /r ib) — imm8 encodes count_s / count_d / ZMASK; splat + extract_lane reuse encPshufd via PSHUFD-broadcast on FP-domain data (bit-identical to integer shuffle); replace_lane via MOVAPS + INSERTPS with `imm8 = lane << 4`; 27 SIMD ops handled total `[x]`
- **9.7-j** — f64x2 lane access trio: new encoders encMovsdXmmXmm (F2 0F 10 /r mod=11 reg-reg form preserves upper 64 per Intel SDM) + encMovlhps (0F 16 /r); splat + extract reuse encPshufd with imm 0x44 / 0xEE; replace_lane MOVAPS + (MOVSD lane=0 / MOVLHPS lane=1); 30 SIMD ops handled total — full lane-access surface for all 6 Wasm shapes `[x]`
- **9.7-k** — int compare eq/ne family 8 ops: encPcmpeqB/W/D (SSE2 0F 74/75/76) + encPcmpeqQ (SSE4.1 0F 38 29); eq handlers reuse emitV128IntBinop unchanged; ne handler `emitV128IntNe(encoder_eq)` applies NOT-via-PXOR with all-ones mask generated by PCMPEQB scratch, scratch on XMM14; 38 SIMD ops handled total `[x]`
- **9.7-l** — signed compare lt_s/gt_s/le_s/ge_s for 8/16/32-bit shapes — 12 ops: new encoders encPcmpgtB/W/D (SSE2 0F 64/65/66) + parametric helper `emitV128IntCmpSigned(encoder_gt, kind)` covering all 4 variants via operand swap (lt/ge) + PXOR-with-all-ones NOT (le/ge); i64x2 signed compares deferred to 9.7-m (PCMPGTQ is SSE4.2, beyond ADR-0041 baseline — needs ADR or synthesis decision); same commit splits op_simd.zig (2064 LOC, §A2 hard-cap break) into op_simd.zig (1156 source) + op_simd_test.zig (923 tests, new file) per D-030 emit_test mirror; 50 SIMD ops handled total `[x]`
- **9.7-m** — i64x2 signed compares lt_s/gt_s/le_s/ge_s — 4 ops: new encoder encPcmpgtQ (SSE4.2 66 0F 38 37 /r); reuses 9.7-l's `emitV128IntCmpSigned(encoder_gt, kind)` helper unchanged with the new encoder; ADR-0041 §5 amended to raise x86_64 baseline SSE4.1 → SSE4.2 (Steam April 2026 98.18% adoption; cranelift 9-instruction synthesis from `inst.isle:3179-3191` rejected per Alternative E) + CPUID detection bumps from bit 19 to bit 20 (SSE4.2 implies SSE4.1 on every x86 vendor); 54 SIMD ops handled total `[x]`
- **9.7-n** — unsigned compares lt_u/gt_u/le_u/ge_u for 8/16/32-bit shapes — 12 ops: new encoders encPmaxub/encPminub (SSE2 0F DE/DA) + encPmaxuw/encPminuw/encPmaxud/encPminud (SSE4.1 0F 38 3E/3A/3F/3B); new helper `emitV128IntCmpUnsigned(encoder_minmax, encoder_pcmpeq, kind)` mirrors emitV128IntCmpSigned shape with the cranelift PMINU/PMAXU + PCMPEQ recipe (`lower.isle:2016-2080`): gt/lt = NOT eq(min/max, rhs); ge/le = eq(lhs, max/min). i64x2 unsigned not in Wasm SIMD spec; 66 SIMD ops handled total `[x]`
- **9.7-o** — FP compare eq/ne/lt/gt/le/ge for f32x4 + f64x2 — 12 ops: new encoders encCmpps (SSE 0F C2 /r ib, no 66 prefix) + encCmppd (SSE2 66 0F C2 /r ib), both taking imm8 predicate per Intel SDM Vol 2A "CMPPS" Table 3-7; new helper `emitV128FpCmp(encoder, imm8, swap_operands)` mirrors signed-compare shape with cranelift's predicate selection from `lower.isle:2149-2176` — eq=0/ne=4/lt=1/le=2 direct, gt/ge swap operands + lt/le predicate (no native ordered-gt in legacy 0..7 imm8); NaN semantics match spec via NEQ_UQ for ne and ordered LT_OS/LE_OS/EQ_OQ for the rest; 78 SIMD ops handled total `[x]`
- **9.7-p** — FP arithmetic add/sub/mul/div + sqrt for f32x4 + f64x2 — 10 ops: 10 new encoders ADDPS/SUBPS/MULPS/DIVPS/SQRTPS (SSE 0F 58/5C/59/5E/51, no 66 prefix) + ADDPD/SUBPD/MULPD/DIVPD/SQRTPD (SSE2 66 prefix); new factor `encSseFpPsBinop(opcode)` for the no-66 PS shape, PD reuses `encSsePackedIntBinop`; 8 binary ops dispatch through 9.7-b's `emitV128IntBinop` unchanged (encoder signature identical, NaN propagates canonically per IEEE-754); 2 unary sqrt ops use new `emitV128FpUnop(encoder)` helper (single-instruction emit, no MOVAPS preamble); f32x4/f64x2 .min and .max deferred to 9.7-q because cranelift wraps MINPS/MAXPS with 7-instruction NaN/zero-correction synthesis (`lower.isle` F32X4/F64X2 fmin/fmax) — structurally different per LOOP "Split when ANY hold"; 88 SIMD ops handled total `[x]`
- **9.7-q** — f32x4 + f64x2 min/max NaN-correction synthesis — 4 ops: 11 new encoders MINPS/MAXPS/MINPD/MAXPD (SSE/SSE2 0F 5D/5F) + ORPS/ORPD/XORPS/XORPD/ANDNPS/ANDNPD (SSE/SSE2 0F 56/57/55) + encPsrldImm (SSE2 66 0F 72 /2 ib via opcode-parametric `encSsePackedShiftImmGroup`); new helpers `emitV128FpMin` (10 instr) + `emitV128FpMax` (13 instr) per cranelift recipe (`lower.isle:2783-2939`) — fmin OR-merges min1/min2 + CMP-UNORD + ANDN-mask; fmax XOR-detects divergence + OR-blend-NaN + SUB-restore-+0 + CMP-UNORD self + ANDN. Shift count 10 (F32X4: 1 sign + 8 exp + 1 QNaN preserved) / 13 (F64X2: 1 + 11 + 1). Two scratch xmms (XMM14/XMM15) used per recipe. Produces canonical IEEE-754-2019 minimum/maximum where naive MIN/MAX returns src2 on unordered (off-spec); 92 SIMD ops handled total `[x]`
- **9.7-r** — v128 bitwise + any_true — 7 ops: 4 new encoders PAND (SSE2 0F DB) + POR (0F EB) + PANDN (0F DF) + PTEST (SSE4.1 0F 38 17). 7 handlers: v128.not (3-instr synth via PCMPEQB ones,ones + PXOR), v128.and/or/xor (dispatch via emitV128IntBinop), v128.andnot (custom 2-instr — MOVAPS dst,rhs + PANDN dst,lhs computes lhs & ~rhs since SSE PANDN's first op is the negated side), v128.bitselect (custom 5-instr PAND/PANDN/POR chain per cranelift), v128.any_true (PTEST + SETNE + MOVZX scalar reduction). regalloc liveness extended for the 5 v128-result ops + 1 scalar-result op; 99 SIMD ops handled total `[x]`
- **9.7-s** — per-shape all_true + bitmask reductions — 8 ops: 4 new encoders MOVMSKPS (SSE 0F 50) + MOVMSKPD (SSE2 66 0F 50) + PMOVMSKB (SSE2 66 0F D7) + PACKSSWB (SSE2 66 0F 63); new factor `encSseXmmToGprRM(prefix_66, opcode)` for the RM-form xmm→gpr shape (opposite of PEXTR* which puts xmm in reg). 4 all_true via 5-instr cranelift recipe (PXOR XMM14 + PCMPEQ_<lane> + PTEST + SETZ + MOVZX) parametric on `encoder_pcmpeq`; 4 bitmask shapes — i8x16 PMOVMSKB direct (1 instr), i32x4 MOVMSKPS direct, i64x2 MOVMSKPD direct, i16x8 PACKSSWB(scratch,src) + PMOVMSKB + SHR 8 (4 instr). regalloc liveness extended for 8 scalar-result ops; 107 SIMD ops handled total `[x]`
- **9.7-t** — i*x* packed shifts shl/shr_s/shr_u for i16x8 + i32x4 + i64x2 — 8 ops: 8 new shift-reg encoders PSLLW/D/Q + PSRLW/D/Q + PSRAW/D (66 0F F1/F2/F3 + D1/D2/D3 + E1/E2 /r). New helper `emitV128IntShift(encoder_shift, mask_imm)` per shape: AND count_r, mask_imm (15/31/63) + MOVD scratch_xmm, count_r + MOVAPS dst,vec (skip-elide) + <shift> dst,scratch. The AND-mask resolves the SSE-vs-Wasm divergence: SSE saturates lanes to all-zero (PSLL/PSRL) or sign-extends (PSRA) when count >= lane_width while Wasm requires `c mod lane_width`. i8x16 + i64x2.shr_s deferred to 9.7-u (no native byte shift / SSE lacks PSRAQ — synthesis only); 115 SIMD ops handled total `[x]`
- **9.7-u** — i64x2.shr_s synthesis — 1 op: 1 new encoder encPsubq (SSE2 66 0F FB) + 9-instruction recipe per cranelift `lower.isle:943-951`. Sign-bit mask synthesised inline via PCMPEQB+PSLLQ-imm (avoids const-pool dep on still-pending ADR-0042 plumbing at the cost of 2 extra instr per call). i8x16 shifts (3 ops) defer to 9.7-v pending const-pool decision; 116 SIMD ops handled total `[x]`
- **9.7-v** — i8x16.shl + i8x16.shr_u inline-mask synthesis — 2 ops: 1 new encoder encPsrlwImm + 9-/10-instruction recipes using PCMPEQB-derived all-ones + PSLLW/PSRLW + PSHUFB byte-0 broadcast (PXOR-zero-ctrl). Avoids ADR-0042 const-pool dep at cost of ~5 extra instr per call vs cranelift's mask-table path. i8x16.shr_s defers to 9.7-w (PUNPCKLBW/PUNPCKHBW byte→word sign-extension + PSRAW + PACKSSWB — structurally different); 118 SIMD ops handled total `[x]`
- **9.7-w** — i8x16.shr_s sign-extension synthesis — 1 op: 2 new encoders encPunpcklbw + encPunpckhbw + 11-instruction recipe per cranelift `lower.isle:846+`. Sign-mask via PCMPGTB(0, src) → byte→word sign-extend via PUNPCKL/HBW → PSRAW per half → PACKSSWB pack. Uses both XMM14/XMM15 scratches with careful re-use sequencing. Closes the i*x*.shift family (12 shift ops total across 9.7-t/u/v/w, all synthesised inline without const-pool dep); 119 SIMD ops handled total `[x]`
- **9.7-x** — i*x*.extend_low/high family — 12 ops: 6 new SSE4.1 encoders PMOVSXBW/WD/DQ + PMOVZXBW/WD/DQ (66 0F 38 20/23/25 + 30/33/35 /r). 2 helpers: emitV128ExtendLow (1-instr direct) + emitV128ExtendHigh (PSHUFD imm=0xEE high→low + PMOVSX/ZX). 12 wrappers covering i16x8/i32x4/i64x2 × extend_{low,high} × {s,u}. Aliasing-safe (PSHUFD reads-then-writes); 131 SIMD ops handled total `[x]`
- **9.7-y** — i*x*.narrow_*_{s,u} saturating — 4 ops: 3 new encoders PACKSSDW + PACKUSWB (SSE2) + PACKUSDW (SSE4.1); PACKSSWB pre-existed from 9.7-s. All single-instr via emitV128IntBinop. SSE PACK* saturation matches Wasm spec (signed→signed-clamped half, unsigned→unsigned-clamped half); 135 SIMD ops handled total `[x]`
- **9.7-z** — i*x*.abs — 4 ops: 3 new SSSE3 encoders PABSB/W/D (66 0F 38 1C/1D/1E /r) + i64x2.abs synthesis via 5-instr sign-mask + PXOR/PSUBQ recipe (no PABSQ in SSE; PCMPGTQ from SSE4.2 baseline used). Single-instr unaries for 8/16/32-bit lanes via emitV128FpUnop. i*x*.neg + i8x16.swizzle defer to 9.7-aa+; 139 SIMD ops handled total `[x]`
- **9.7-aa** — i*x*.neg — 4 ops: no new encoders (PSUBB/W/D/Q all existing). emitV128IntNeg helper 3-instr recipe: PXOR XMM14,XMM14 + PSUB_<shape> XMM14, src + MOVAPS dst, XMM14. Aliasing-safe; PSUB wraps modulo lane width matching Wasm spec; 143 SIMD ops handled total `[x]`
- **9.7-ab** — FP convert signed + promote/demote — 4 ops: 4 new SSE2 encoders CVTDQ2PS / CVTPS2PD (no 66 prefix) + CVTPD2PS (66 prefix) + CVTDQ2PD (mandatory F3 prefix). Single-instr unaries via emitV128FpUnop. CVTPD2PS naturally produces "_zero" semantics (high 64 of dst zeroed). u-variants (2 ops) + trunc-sat (4 ops) defer pending ADR-0042 const-pool plumbing for the float-magic-number recipes per cranelift `lower.isle:3761+`; 147 SIMD ops handled total `[x]`
- **9.7-ac** — i8x16.swizzle inline-synth — 1 op: 10-instruction recipe avoiding cranelift's PADDUSB+const-pool 0x70-broadcast pattern. Synthesises 0x0F-broadcast inline (PCMPEQB + PSRLW imm 12 + PSHUFB-broadcast byte 0), uses PCMPGTB to detect idx>15 (signed compare; 128..255 already handled by PSHUFB high-bit semantics), POR-merges mask into ctrl, then PSHUFB. No new encoders; 148 SIMD ops handled total `[x]`
- **9.7-ad** — FP unop family — 12 ops: 3 new encoders encPslldImm (SSE2 66 0F 72 /6 ib via existing packed shift-imm group helper) + encRoundps + encRoundpd (SSE4.1 66 0F 3A 08/09 /r ib). 4 abs/neg via inline sign-mask synthesis: PCMPEQB ones + PSLL{D,Q}-imm {31,63} → per-lane sign-mask in XMM14; abs uses PANDN+MOVAPS (5 instr) for x AND ~mask; neg uses MOVAPS+PXOR (4 instr) for x XOR mask. Avoids ADR-0042 const-pool dep at cost of 3 extra instr per call vs upstream mask-table path. 8 ceil/floor/trunc/nearest single-instr via SSE4.1 ROUND{PS,PD} imm — bit3 set for precision-exception suppression, bits[1:0] = mode (00 nearest-even / 01 floor / 10 ceil / 11 trunc) per Intel SDM Vol 2A; 160 SIMD ops handled total `[x]`
- **9.7-ae** — inline-synth FP convert + trunc-sat — 2 ops: 4 new encoders encCvttps2dq (SSE2 F3 0F 5B /r) + encPsradImm (SSE2 66 0F 72 /4 ib via shift-imm group) + encAndps + encAndpd (SSE/SSE2 0F 54). f32x4.convert_i32x4_u 11-instr split-and-recombine recipe per cranelift `lower.isle:3811-3831`: PSLLD/PSRLD-imm 16 mask low halves, PSUBD-recover high, signed CVTDQ2PS each, ADDPS-double the high to undo /2-shift, sum. i32x4.trunc_sat_f32x4_s 9-instr NaN-mask + XOR-fix recipe per `lower.isle:3848-3869`: CMPPS-self-EQ_OQ NaN-detect, ANDPS-mask NaN→+0.0, CVTTPS2DQ saturating-trunc, then XOR-flip positive-OOR's 0x80000000 → 0x7FFFFFFF via PSRAD-imm-31-derived sign-extend mask. Both use only XMM14/XMM15 of the 2-scratch budget. Remaining 4 variants (f64x2.convert_low_i32x4_u + i32x4.trunc_sat_{f32x4_u, f64x2_s_zero, f64x2_u_zero}) defer to 9.7-ag pending ADR-0042 const-pool plumbing — trunc_sat_f32x4_u additionally needs 3 scratch xmms; 162 SIMD ops handled total `[x]`
- **9.7-af** — q15mulr_sat_s + dot_i16x8_s — 2 ops: 2 new encoders encPmulhrsw (SSSE3 66 0F 38 0B /r via existing escape-2 helper) + encPmaddwd (SSE2 66 0F F5 /r). PMULHRSW's hardware "M" suffix = round-to-nearest matches Wasm Q15 multiply-round-saturate exactly. PMADDWD's pairwise i16×i16→i32 wrapping accumulation matches Wasm spec at the boundary INT16_MIN^2 + INT16_MIN^2 = 0x80000000+0x80000000 → 0 mod 2^32 (cranelift `lower.isle:4073-4078`). Both single-instr ops dispatch through existing emitV128IntBinop helper (no new infrastructure). 12 extmul + 1 popcnt + 4 extadd_pairwise variants defer to 9.7-ag/ah/ai (different recipe shapes / const-pool deps); 164 SIMD ops handled total `[x]`
- **9.7-ag** — i16x8.extmul × 4: i16x8.extmul_{low,high}_i8x16_{s,u} via cranelift `lower.isle:1197-1285` recipe — extend each i8x16 operand to i16x8 via PMOVSXBW (signed) or PMOVZXBW (unsigned), then PMULLW. 2 parametric helpers: emitV128IntExtmulLow (3-instr: PMOVSX/ZX BW lhs→XMM14, PMOVSX/ZX BW rhs→dst, PMULLW dst, XMM14) + emitV128IntExtmulHigh (5-instr: PSHUFD imm=0xEE prefix on each operand to swap upper-64→lower-64 into XMM14/XMM15 before extending). 4 wrappers parametric on extend encoder (signed vs unsigned). No new encoders — pure composition of 9.7-c (PMULLW) + 9.7-x (PMOVSX/ZX) + 9.7-e (PSHUFD) primitives; 168 SIMD ops handled total `[x]`
- **9.7-ah** — i32x4.extmul × 4: i32x4.extmul_{low,high}_i16x8_{s,u} via the same cranelift recipe shape as 9.7-ag — PMOVSXWD / PMOVZXWD extend (i16→i32) + PMULLD multiply (SSE4.1, from 9.7-c). 4 wrappers reuse emitV128IntExtmulLow/High helpers from 9.7-ag unchanged, parameterised over extension width and multiply encoder. No new encoders. Continuation chunk under textbook_survey.md's narrow definition (zero new encoders / helpers / design choices); 172 SIMD ops handled total `[x]`
- **9.7-ai** — i64x2.extmul × 4: i64x2.extmul_{low,high}_i32x4_{s,u} with distinct recipe shape — PMULDQ (SSE4.1) / PMULUDQ already widen i32→i64 from even-numbered lanes so no separate PMOVSX/ZX prefix is needed. 3-instr inline recipe per cranelift `lower.isle`: PSHUFD imm=0x50 (low: lanes 0/1 → slots 0/2) or 0xFA (high: lanes 2/3 → slots 0/2) on each operand into dst + XMM14, then PMULDQ/PMULUDQ. New encoder encPmuldq (SSE4.1 66 0F 38 28 /r); PMULUDQ already from 9.7-d. New helper emitV128I64x2Extmul (parametric on PSHUFD imm + multiply encoder). Closes the extmul family across i16x8/i32x4/i64x2 (12 ops total across 9.7-ag/ah/ai); 176 SIMD ops handled total `[x]`
- **9.7-aj** — i16x8.extadd_pairwise_i8x16 × 2: PCMPEQB ones + PABSB → 0x01-per-byte mask synthesised inline (no const-pool dep), then PMADDUBSW (SSSE3 — first/second operand role flipped between _u and _s variants since PMADDUBSW reads dst as unsigned and src as signed; with +1 in either slot the saturating-multiply-and-add reduces to plain pairwise add fitting in i16). New encoder encPmaddubsw (SSSE3 66 0F 38 04 /r). 2 new ZirOp entries — extadd_pairwise_i8x16_{s,u} were missing from the enum. 4-instr (_u: needs MOVAPS dst,src) / 3-instr (_s: dst becomes the mask directly); 178 SIMD ops handled total `[x]`
- **9.7-ak** — i32x4.extadd_pairwise_i16x8_s 1 op: PCMPEQB ones + PSRLW imm 15 → 0x0001-per-word mask (= +1 per i16 lane), then PMADDWD reduces to pairwise add. 4-instr inline. PMADDWD existing from 9.7-af; PSRLW-imm existing from 9.7-v. No new encoders. 2 new ZirOp entries (i32x4.extadd_pairwise_i16x8_{s,u}); only `_s` lands now — `_u` variant defers because PMADDWD reads operands as signed i16 so high u16 lanes need const-pool sign-flip pre-correction (deferred to ADR-0042 chunk); 179 SIMD ops handled total `[x]`
- **9.7-al** — ADR-0042 const-pool port to x86_64 + v128.const 1 op: mirrors §9.6/9.6-f-ii ARM64 work (commit `c12760cb`) for x86_64. New SimdConstFixup struct (types.zig: disp32_byte_offset + post_insn_byte + const_idx) + simd_const_fixups ArrayList threaded through emit.zig + post-emit pool append/patch loop after the trap stub (16-byte aligned). New encoders encMovupsXmmRipRelPlaceholder (SSE PC-relative MOVUPS xmm, [RIP+disp32] — 6 bytes for XMM0..XMM7, 7 bytes with REX.R for XMM8..XMM15) + patchRipRelDisp32. emitV128Const handler. ZirFunc.simd_consts already populated by lower.zig since 9.6-f-ii; only x86_64 emit-side was missing. i8x16.shuffle defers to 9.7-am (needs PSHUFB recipe + per-lane index validation); 180 SIMD ops handled total `[x]`
- **9.7-am** — i32x4.trunc_sat_f64x2_s_zero 1 op: first multi-instr const-pool consumer beyond v128.const, validates the foundation. New per-emit-pass extra_consts ArrayList collects emit-time-derived shared consts (distinct from func.simd_consts per-instance literals); post-emit pool concatenates both lists, fixup const_idx maps uniformly. Recipe per cranelift `lower.isle:4194-4214`: MOVUPS const-load (INT32_MAX_f64-broadcast) + MOVAPS + CMPPD-self-EQ NaN-mask + MINPD upper-clamp + ANDPD NaN-zero + CVTTPD2DQ. CVTTPD2DQ writes 2 i32 to low half, zeros high half (matches "_zero" suffix). Negative OOR auto-saturates to 0x80000000 = INT32_MIN per CVTTPD2DQ semantics. New encoder encCvttpd2dq (SSE2 66 0F E6 /r). i8x16.shuffle still deferred — needs derived a-mask/b-mask plumbing which is structurally different; 181 SIMD ops handled total `[x]`
- **9.7-an** — i8x16.popcnt 1 op: SSSE3 PSHUFB-LUT recipe per cranelift `lower.isle:2491-2517`. Two const-pool entries via extra_consts: 16-byte POPCNT_LUT (byte i = popcount(i) for i in 0..15) + NIBBLE_MASK_BROADCAST (0x0F per byte). 11-instr inline recipe (incl. 2 MOVUPS-RIP-rel const loads): split each input byte into low/high nibbles, look each up in LUT via PSHUFB, PADDB the two halves. PSHUFB clobbers dst so LUT loads twice — the per-instance double-load is cheaper than 3rd-scratch juggling and the const is shared via extra_consts dedup. Also factors helpers `lookupOrAppendExtraConst` + `emitConstLoad` for future const-pool consumers; 182 SIMD ops handled total `[x]`
- **9.7-ao** — f64x2.convert_low_i32x4_u 1 op: IEEE-754 mantissa-overlay trick per cranelift `lower.isle:3775-3779`. 2 const-pool entries via extra_consts: UINT_MASK_LOW (0x43300000-per-dword, single-precision pattern of 0x1.0p+52) + UINT_MASK_HIGH (0x4330000000000000-per-qword, double-precision 2^52). 5-instr recipe: MOVAPS dst,src + MOVUPS const_low + UNPCKLPS-interleave-low (each qword becomes 0x4330_0000_<u32>, which as f64 = 2^52 + u32) + MOVUPS const_high + SUBPD subtract 2^52 → u32 as f64 exactly via IEEE-754 unit-ULP recovery. New encoder encUnpcklps (SSE 0F 14 /r). Discharges one of the 4 deferred 9.7-ae u-variants; 184 SIMD ops handled total `[x]`
- **9.7-ap** — i32x4.trunc_sat_f64x2_u_zero 1 op: ROUNDPD + ADDPD-magic + SHUFPS-extract recipe per cranelift `lower.isle:5061-5093`. 7-instr inline recipe + 2 const-pool entries: MOVAPS dst,src + PXOR t1,t1 (zeros) + MAXPD-zero-clamp (NaN→0 via 2nd-operand-on-unordered + negative→0) + MINPD-UMAX_f64-clamp + ROUNDPD imm 0x0B (round-to-zero | suppress-precision) + ADDPD-2^52-magic + SHUFPS imm 0x88 to gather low-32 of each qword into i32x4 lanes 0/1 with lanes 2/3 zero ("_zero" suffix). Reuses 9.7-ao's UINT_MASK_HIGH (= 2^52 magic) via extra_consts dedup; new const UINT32_MAX_F64_BROADCAST (4294967295.0). New encoder encShufps (SSE 0F C6 /r ib). Discharges another of the 4 deferred 9.7-ae u-variants; 185 SIMD ops handled total `[x]`
- **9.7-aq** — i32x4.extadd_pairwise_i16x8_u 1 op: closes the extadd_pairwise family. PMADDWD reads operands as signed i16, so for u16 lanes we sign-flip pre-multiply and bias-correct post-multiply — XOR src with 0x8000-per-word converts u16 → i16 in [-0x8000, 0x7FFF]; PMADDWD with +1 produces (i16+i16) i32 sums = (u16+u16) - 0x10000; PADDD adds 0x10000 per i32 to recover. 11-instr inline recipe (no const-pool dep — all 3 mask constants synthesised from PCMPEQB seed via shifts: PSLLW-imm 15 → 0x8000-per-word, PSRLW-imm 15 → 0x0001-per-word, PSRLD-imm 31 + PSLLD-imm 16 → 0x00010000-per-dword). New encoder encPsllwImm (SSE2 66 0F 71 /6 ib via existing shift-imm-group helper). Closes the extadd_pairwise family across all 4 variants (9.7-aj's i8x16-input pair + 9.7-ak's i16x8 _s + this chunk's i16x8 _u); 186 SIMD ops handled total `[x]`
- **9.7-ar** — i8x16.shuffle 1 op: closes the structural blocker flagged in §9.7-al/am — needs derived per-instance a-mask + b-mask. Resolution: emit-time derivation from `func.simd_consts[ins.payload]`; handler appends both derived masks to extra_consts. Recipe per cranelift `lower.isle:4710+`: PSHUFB(src1, a_mask) | PSHUFB(src2, b_mask) where a_mask[i] = mask[i] if mask[i] < 16 else 0x80, b_mask[i] = mask[i]-16 if mask[i] in 16..31 else 0x80. PSHUFB writes 0 when ctrl bit 7 set, so each side contributes only its valid lanes; POR merges. 7-instr inline (incl. 2 const loads). No new encoders. Side-finding: spike confirmed D-054 (OrbStack as-loop-broke) is hoist-pass codegen bug, not Rosetta — see updated D-054 entry; 187 SIMD ops handled total `[x]`
- **9.7-as** — D-054 close: SysV x86_64 frame_unaligned was missing r15_save_bytes, causing local 0 at [RBP-16] to sit BELOW RSP after `SUB RSP, frame_bytes` when outgoing_max_bytes=0 (no Win64 shadow space). The next `call $dummy` pushed its return address to [RSP-8] = [RBP-16] clobbering local 0 (the observed 0xFD1BD386 garbage = stack residue). Win x86_64 hid the bug because outgoing_max_bytes=32 (Win64 shadow) inflated the frame to 40 bytes. 1-line fix in emit.zig:278 — `frame_unaligned = outgoing_max_bytes + locals_bytes + spill_bytes + r15_save_bytes`. OrbStack now 212/0/20 strict (was 211/1/20 D-054 carry). Investigation: cross-compiled the same as-loop-broke fixture's emit bytes for x86_64-linux + x86_64-windows (via Mac), diffed → only 4 byte positions differ (entry_arg0 reg + frame size); decoded that frame size for SysV is too small to contain the local. 187 SIMD ops total (D-054 close orthogonal to SIMD count) `[x]`
- **9.7-at** — i32x4.trunc_sat_f32x4_u 1 op: closes the last of the 4 deferred 9.7-ae u-variants. 14-instr inline two-path recipe per cranelift `lower.isle:3919-3962`: clamp(src, 0) via XORPS+MAXPS → CVTTPS2DQ for [0, INT_MAX] lanes; subtract magic (0x4f000000 = INT_MAX+1 as f32, synthesised via PCMPEQD+PSRLD+CVTDQ2PS) → CVTTPS2DQ for [INT_MAX+1, UINT_MAX] lanes; CMPPS-LE mask + PXOR + PMAXSD-with-zero clamp + PADDD merge. New encoder encPmaxsd (SSE4.1 0F 38 3D /r) via existing encSsePackedIntBinopExt helper. Reframing: prior session's "3 scratch xmm" reading was wrong — dst (regalloc'd from XMM8..XMM13) + XMM14 + XMM15 already gives 3 distinct physical registers within fp_spill_stage_xmms reservation; same dual-scratch pattern as 9.7-q/w/ac. No ABI change. 188 SIMD ops handled total `[x]`
- **9.7-au** — int min/max + sat arith + avgr_u — 22 ops: i8x16/i16x8/i32x4 × {min_s, min_u, max_s, max_u} (12 ops) + i8x16/i16x8 × {add_sat_s, add_sat_u, sub_sat_s, sub_sat_u} (8 ops) + i8x16/i16x8.avgr_u (2 ops). 15 new encoders — encPminsb/Pmaxsb/Pminsd (SSE4.1 via encSsePackedIntBinopExt), encPminsw/Pmaxsw + encPaddsb/w + encPsubsb/w + encPaddusb/w + encPsubusb/w + encPavgb/w (SSE2 via encSsePackedIntBinop). Unsigned min/max encoders already landed in 9.7-n (PMINU/PMAXU recipe consumers). All 22 wrappers dispatch through 9.7-b's `emitV128IntBinop` unchanged. Liveness extended in shared/regalloc.zig (22 entries appended, same 2-in 1-out v128 shape as add/sub cluster). Cranelift cross-check `inst.isle:2470-2486` confirms direct PMIN*/PMAX* mapping. 210 SIMD ops handled total `[x]`
- **9.7-av** — FP pseudo-min/max — 4 ops: f32x4/f64x2 × {pmin, pmax}. Single-instruction MINPS/MAXPS/MINPD/MAXPD dispatch with **operand swap**: dst=c2, src=c1. The Wasm pmin(c1,c2) = `if c2 < c1: c2 else c1` semantics align with x86 MINPS's "return SRC on equal/NaN/both-zero" (Intel SDM Vol 2A) when dst holds c2. cranelift `lower.isle:1542-1545` makes the same call via CLIF bitselect-of-fcmp-LT pattern matching MINPS. New helper emitV128FpPseudoBinop (operand-swapped variant of 9.7-b's emitV128IntBinop). No new encoders — reuses 9.7-q's encMinps/Maxps/Minpd/Maxpd. Distinct from 9.7-q's fmin/fmax which need 10-13 instr NaN-correction synthesis (those produce IEEE-754 canonical min/max; pmin/pmax are spec-asymmetric). 214 SIMD ops handled total `[x]`
- **9.7-aw** — i64x2.extract_lane — 1 op via PEXTRQ (SSE4.1 REX.W=1 variant of PEXTRD; same opcode 0x16, REX.W promotes operand to 64-bit). New encoder encPextrQ mirrors encPextrD with W bit always set. Handler emitI64x2ExtractLane mirrors 9.7-e's emitI32x4ExtractLane with u1 lane (i64x2 has 2 lanes); uniform 8-byte spill stride via existing gprStoreSpilled. Liveness already in shared/regalloc.zig from §9.4 IR opening. 215 SIMD ops handled total `[x]`
- **9.7-ax** — v128.load + v128.store — 2 ops, foundation memory chunk. Mirrors scalar `op_memory.emitMemOp` shape with access_size=16 + MOVUPS final encoding. New encoder encMovupsMemBaseIdx (load 0F 10 /r, store 0F 11 /r — no prefix per Intel SDM Vol 2A; ModR/M+SIB scale=1 base+index addressing mirrors encMovssMovsdMemBaseIdx). emitV128Load + emitV128Store + shared v128MemPrologue helper in op_simd.zig (RAX/RCX/RDX scratches reused; bounds_fixups + ADR-0028 trace.writeBounds wired uniformly with scalar). uses_runtime_ptr prescan in emit.zig extended (v128.load / v128.store join the existing scalar-mem-op list — both touch [R15+vm_base_off] / [R15+mem_limit_off]). Subsequent 9.7-ay/az/ba/bb sub-chunks (load_splat / load_zero / load_lane / load*x*_*) reuse the v128MemPrologue helper. 217 SIMD ops handled total `[x]`
- **9.7-ay** — v128.load{8,16,32,64}_splat — 4 ops. All reuse 9.7-ax's v128MemPrologue with access_size 1/2/4/8 + a per-lane-width broadcast tail. No new encoders. load8_splat: MOVZX RCX, byte [mem]; MOVD dst, ECX; PXOR XMM14, XMM14; PSHUFB dst, XMM14 (zero-mask broadcast — cranelift `lower.isle:4840-4843` uses PINSRB-mem; we lack mem-form so 1-extra-instr GPR roundtrip). load16_splat: MOVZX + MOVD + PSHUFLW imm 0 + PSHUFD imm 0. load32_splat: MOVSS [mem] (zero-extend upper 96) + PSHUFD imm 0 (cranelift `lower.isle:4893-4895`). load64_splat: MOVSD [mem] (zero-extend upper 64) + PSHUFD imm 0x44 (broadcast low qword to upper); MOVDDUP would be 1 instr but adding encoder for one consumer is over-investment. uses_runtime_ptr prescan extended for 4 ops. 221 SIMD ops handled total `[x]`
- **9.7-az** — v128.load{32,64}_zero — 2 ops. Single-instruction MOVSS/MOVSD memory load — the scalar mem-form already zero-extends the upper bits per Intel SDM Vol 2A (MOVSS load zeros upper 96; MOVSD load zeros upper 64), exactly matching Wasm load*_zero semantics. Reuses 9.7-ax's v128MemPrologue with access_size 4/8 and the existing encMovssMovsdMemBaseIdx encoder. 223 SIMD ops handled total `[x]`
- **9.7-ba** — v128.load_lane / store_lane × 4 sizes — 8 ops. memarg + 1-byte lane immediate (sub-opcodes 84..91 of SIMD prefix 0xFD). Validator path: new opSimdLoadLane (pop i32+v128, push v128) + opSimdStoreLane (pop i32+v128) replace prior `84..91 => opSimdBinop()` stack-effect mismatch. Lower path: new emitMemargLane helper packs offset into payload + lane byte into extra (align dropped — unused in emit). Emit path: 2 parametric helpers v128LoadLane (load + PINSR{B/W/D/Q} reg-form merge) + v128StoreLane (PEXTR{B/W/D/Q} + store with PUSH/POP RCX preservation around the prologue's RCX-clobbering LEA). 8 wrappers parametric on access_size {1,2,4,8}. No new encoders. Cranelift mem-form PINSR/PEXTR optimisation deferred to §9.10. uses_runtime_ptr prescan extended for 8 ops. 231 SIMD ops handled total `[x]`
- 9.7-bb (v128.load{8x8,16x4,32x2}_{s,u} — 6 ops. Extending memory loads — closes the §9.7 v128 op surface. 8 bytes loaded into low qword via MOVSD (zero-extends upper 64), then PMOVSX/ZX{BW,WD,DQ} extends each lane to the next-larger size (8→16, 16→32, 32→64). Cranelift recipe at `lower.isle:4977-5010` is identical. Shared helper v128LoadExtend (parametric on extend encoder); 6 wrappers. Reuses 9.7-ax's v128MemPrologue with access_size=8 and the existing PMOVSX/ZX encoders from 9.7-x. No new encoders. uses_runtime_ptr prescan extended for 6 ops. 237 SIMD ops handled total — all v128 ZirOps in zir.zig:184-288 now have x86_64 emit handlers. `[x]`).

## Row 9.9 — `simd.wast` spec test wiring

**Scope**: `simd.wast` spec test wired in; fail=skip=0 across both backends (3-host gate).

**Status**: [ ]

### Sub-chunks (commit-time order)

- **9.9-III-close** — **Cat III closure** per ADR-0065. γ-4 permanent
  relax landed; both `hasUnbindableImports` registered-exporter
  branches and full bridge-thunk pinned-cohort save active.
  Mac aarch64 spec_assert non-simd 25307/0/705 → **25308/0/705**
  (+1 PASS = imports.1.wasm print64); ubuntunote x86_64
  25195/113/2008 (γ-4 probe pre-cycle-4) → **24034/0/2015**
  (= +113 PASS net via the cycle-3/4 thunk + dual-view fix
  chain). 144-directive drain target satisfied: 136 cross-module
  / 4 host imports (no-op trap stub per (c)-1b) / 2 start-trap
  (SIGSEGV recovery) / 2 link-typecheck — all 0 FAIL. Cycle 4
  closure commit (`b137a44b`) extends ADR-0066 §A2 (thunk
  56 → 96 bytes for X19 + X24..X28 reserved-invariant cohort)
  and absorbs D-126 + D-144. `[x]`
- **9.9-a** — foundation per ADR-0045 — parallel `simd_assert_runner` + v128-aware text manifest format + `scripts/regen_spec_simd_assert.sh` skeleton + build.zig `test-spec-simd` step. Manifest list empty; runner reports "0 passed, 0 failed, 0 skipped (over 0 manifests; foundation)". NOT aggregated into test-all yet (`d8ffe36b`) `[x]`
- **9.9-b** — v128 return marshal per ADR-0046 — both backends gain v128 single-result support: x86_64 MOVAPS XMM0, src_x; ARM64 MOV V0.16B, Vn.16B (alias of ORR V0.16B, Vn.16B, Vn.16B). resolveXmm/resolveFp (no spill staging) used to surface UnsupportedOp on spilled v128 explicitly. v128 PARAM marshal split off to follow-up chunk per ADR-0046 §"Decision" / 2. Updated emit_test_float.zig:1488 v128-result test from expectError to expect compile-success. v128-param rejection test stays valid (`aced46e5`) `[x]`
- **9.9-c** — manifest population + JIT execution wiring per ADR-0045 — `scripts/regen_spec_simd_assert.sh` populates lightweight starter set NAMES={simd_address, simd_align, simd_const, simd_select} + wast2json + Python distillation. `simd_assert_runner.zig` gains manifest parsing + JIT execution for `() → {i32, i64, f32, f64, v128, ()}` and `(i32) → {i32, v128}` shapes. New entry helpers `callV128NoArgs` + `callV128_i32` in `src/engine/codegen/shared/entry.zig` returning `[16]u8` via `@Vector(16, u8)` (lowers to V0 / XMM0). Bad-module flag suppresses cascade FAIL on assert_returns under a module that failed compile. v128 hex tokens lower-byte-first matching in-memory little-endian Wasm v128 layout. Mac aarch64 baseline: 74 passed, 301 failed (158 UnsupportedOp + 143 BadValType — v128 valtype acceptance + missing v128.load*_lane / v128.load*x*_{s,u} codegen entries in op_simd dispatch), 478 skipped (v128-param-pending → 9.9-e; assert_invalid SKIP-VALIDATOR-GAP; cascaded-bad-module asserts). NOT aggregated into test-all (deferred to 9.9-g) (`540cef72`) `[x]`
- **9.9-d-1** — discharge BadValType + IR-liveness UnsupportedOp clusters: `parse/sections.zig:readValType` accepts 0x7B → `.v128`; `ir/analysis/liveness.zig:stackEffect` gains the full v128 op catalogue ~135 LOC mirroring `regalloc.zig:382-628` shape-tag table + `zir.zig:184-288` ZirOp enum; per-manifest after fix on Mac aarch64: simd_address 2/3/44, simd_select 0/1/6, simd_const 60/158/232 — simd_align SEGV mid-run on .90/.91 v128.load due to ARM64 emitV128Load missing bounds-checked vm_base translation, tracked as D-060 + 9.9-d-2 (`14ead9d9`) `[x]`
- **9.9-d-2** — closes D-060: ARM64 `emitV128Load` / `emitV128Store` rewritten to mirror `op_memory.emitMemOp` prologue — ORR W16+offset-fold+ADD X17 #16+CMP X27+B.HI fixup ending in `LDR/STR Q<vt>, [X28, X16]`. Private `v128MemPrologue(ctx, addr_vreg, offset, size)` helper extracted in op_simd.zig for reuse by upcoming load_extend / load_splat / load_zero / load_lane / store_lane chunks. New encoders `encLdrQReg` / `encStrQReg` in `inst_neon.zig` — base 0x3CE06800 / 0x3CA06800 verified against clang-as. Mac aarch64 simd_assert_runner totals: 72 PASS / 234 FAIL / 286 SKIP — runner completes (no SEGV); residual 14 compile:UnsupportedOp + 150 value-mismatch fails are subsequent 9.9-d-N chunks (`d71d7958`) `[x]`
- **9.9-d-3** — 12 ARM64 v128 mem ops bundled per chunk-granularity rule sharing `v128MemPrologue` helper: 2 load_zero via `LDR S/D` reusing scalar zero-extending semantics; 4 load_splat via `ADD X16,X28,X16` + LD1R (4 new encoders `encLd1r{16B,8H,4S,2D}` bases 0x4D40C000/0x4D40C400/0x4D40C800/0x4D40CC00 verified against clang-as); 6 load_extend via `LDR D` + existing 9.6-g-i SXTL/UXTL. Shared scaffolding helper `emitV128LoadFamily(ctx, ins, access_size, emit_tail)` parametric on per-shape tail closure. Mac aarch64 totals: 62 PASS / 200 FAIL / 296 SKIP — compile-stage UnsupportedOp 14→3; residual: 3 compile (select_v128 + load_lane / store_lane), 26 simd_address runtime mismatches (likely runner-side data-segment gap OR JIT prologue routing issue under (i32)→v128 shape — investigate via spike in 9.9-d-4), and ~158 FP-correctness in simd_const (9.9-d-N FP cluster) (`6fb3341e`) `[x]`
- **9.9-d-4** — fix ARM64 function-level `end` handler's v128 return marshal — missed in 9.9-b. The `.return` op handler had the correct `.v128 → MOV V0.16B, Vn.16B` arm but the function-level `.end` handler classified v128 as `is_fp = false` and routed through `MOV X0, Xn` GPR path, leaving V0 unwritten so the caller read pre-call V0 leakage. Spike via debug prints + JIT body disassembly identified the bug. Replace `is_fp` boolean switch with exhaustive switch matching `.return` shape; lesson [`fn-end-vs-return-parallel-handlers`](../lessons/2026-05-10-fn-end-vs-return-parallel-handlers.md) records the bug_fix_survey miss. Mac aarch64 totals: 62→226 PASS / 200→36 FAIL / 296 SKIP. The simd_const 60-PASS pre-fix bias was stale-V0 coincidence; post-fix the bias resolves correctly. (`1a1c2da5`) `[x]`
- **9.9-i-1** — **§9.9 functional close — D-084 full discharge** (Win64 v128 marshal via hidden-pointer per ADR-0055 / cranelift `ABIArg::ImplicitPtrArg` recipe). 3-host bit-identical SIMD subset achieved on first iteration: windowsmini `simd_assert_runner: 13301 passed, 0 failed, 440 skipped (= 50 skip-impl + 390 skip-adr) (over 26 manifests)` matches Mac aarch64 + OrbstStack x86_64 exactly (41 FAIL → 0 FAIL). Implementation per Microsoft x64 ABI §"Parameter passing": `__m128` types passed by reference (16-byte aligned caller-allocated memory, pointer in next int-arg-reg slot). **Callee-side** (`src/engine/codegen/x86_64/emit.zig` param-marshal switch): Win64 v128 in register slot (RDX/R8/R9) → `MOVUPS xmm_tmp, [int_reg]` + `MOVUPS [RBP+local_disp], xmm_tmp`; Win64 v128 in stack-overflow slot (slot ≥ 4) → load pointer via `MOV RAX, [RBP+stack_disp]` then deref. **Caller-side** (`src/engine/codegen/x86_64/op_call.zig:marshalCallArgs`): write v128 source → 16-byte aligned scratch slot in caller's outgoing-args region via `encStoreXmmV128MemRSPDisp32`; `LEA ptr_reg, [RSP+scratch_disp]` into int-arg slot, or store pointer onto stack overflow. `computeOutgoingMaxBytes` extended to reserve `n_v128 * 16` scratch above shadow+overflow, 16-byte aligned. **Co-discharges**: D-062 partial — SysV `fp_arg_idx >= 8` v128 stack-overflow path also landed (NSAA SSE-class 2-eightbyte alignment per AMD64 ABI §3.2.3); arm64 9th+ v128 arg still pending. D-052 partial — extracted 10 rbp/rsp form-selectors (~90 LOC) to new `src/engine/codegen/x86_64/rbp_disp.zig` so emit.zig stays under 2000 hard cap (1998 → 1987 LOC post-extract + Win64 v128 marshal additions). **New encoders**: `encLeaR64BaseDisp32` + `encLeaR64BaseRspDisp32` (LEA disp32 forms; RSP variant separate because RSP base mandates SIB byte 0x24); `encStoreXmmV128MemRSPDisp32` (MOVUPS [RSP+disp32], xmm; pre-existing `encMovupsXmmMemBaseDisp32` asserts non-RSP base). 3-host gate: Mac aarch64 `zig build test` + `test-spec-simd` (13301/0/440) green; OrbStack x86_64 `zig build test` + `test-spec-simd` (13301/0/440) green; windowsmini `zig build test` + `test-spec-simd` (**13301/0/440 — bit-identical**) + `test-spec` (9/0) + `test-all` (all runners green incl. realworld 45/55+10 skip-WASI + wasi 2/0) green. ADR-0055 Status flipped Draft → Accepted. D-084 deleted from debt.md Active; recorded in Recently discharged. **§9.9 row exit criterion now legitimately satisfiable** (`fail = skip = 0` across both backends, 3-host gate). (`7a7e387c`) `[x]`
- **9.9-h-31** — §9.9 close residual chunk 7 — **D-083 full close** (x86_64 emitV128Select). Mirror of arm64 9.9-h-30. **simd_assert_runner Mac+OrbStack bit-identical: 13301/0/440** (50 skip-impl + 390 skip-adr) = +6 PASS, 0 FAIL vs h-30 baseline. **Manifest-line skip-impl now 0**. New `emitV128Select` in `x86_64/op_simd.zig` using mask-based recipe: XOR/MOV-imm → TEST cond → CMOVNE r_mask, r_neg1 → MOVQ XMM7 → PSHUFD broadcast → XOR-trick `dst = val2 ^ (mask & (val1 ^ val2))`. **Flag-ordering bug surfaced + fixed mid-chunk**: first attempt ordered TEST before XOR/MOV; XOR clobbers EFLAGS (ZF=1 from result=0) masking TEST's ZF before CMOVNE — pre-fix OrbStack 3 FAIL (cond ≠ 0 → got val2). Reordered to XOR/MOV-imm-first → TEST → CMOVNE so TEST is the latest flag-setter. Post-fix 0 FAIL. SPILL-EXEMPT (mirror of arm64): val2 via resolveXmm (rejects spill), val1 via xmmLoadSpilledV128 stage 1 (XMM15), result via xmmDefSpilledV128 stage 0; XMM14 reused as scratch tmp (safe — result is SPILL-EXEMPT so XMM14 never the dst home). XMM7 = SIMD scratch per abi.zig:200. Aliasing safety: XOR-trick handles dst==val1 / dst==val2 cases via XMM14 holding the intermediate across the final MOVAPS dst, val2. Dispatch arm added in `x86_64/emit.zig` `.select / .select_typed`: `if (alloc.shapeTag(val1_v) == .v128)` routes to op_simd.emitV128Select (mirrors arm64/emit.zig:1106). SUPPORTED entry restored in regen script (deferred since 9.9-h-27). Manifest delta: simd_select 6 skip-impl → 6 assert_return. emit.zig 1991→1998 (under 2000 hard cap; comment trimmed to stay within file_size_check gate). D-083 row deleted from debt.md. 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` + `test-spec-simd` green; windowsmini deferred per ADR-0049. Lint + fmt + zone + file_size + check_skip_adrs clean. **§9.9 close status: manifest-line skip-impl = 0**. Runner-classified ~50 SKIP-VALIDATOR-GAP / SKIP-PARSER-GAP on assert_invalid/assert_malformed compile-clean cases remain (distinct from manifest-line; Phase-10+ concern per manifest-line-only §9.9 exit interpretation). Next: §9.11 (audit + SHA backfill) + §9.12 (Track D wiring for Phase 10 hard gate). (`8af28286`) `[x]`
- **9.9-h-30** — §9.9 close residual chunk 6: **D-083 part 1 close** (arm64 emitV128Select alias-stash). Root-caused the v128 select FAIL as a regalloc-alias-on-mask-V-reg pattern in `arm64/op_simd.emitV128Select` — regalloc LIFO slot-reuse can place `result_vreg`'s home V reg equal to `val1_v`'s or `val2_v`'s home reg (when all three non-spilled in V16..V28); subsequent `DUP mask_v.2D, X17` clobbered the aliased operand before BSL read it. With cond=0 + val2-alias, BSL computed `(0 & val1) \| (~0 & 0) = 0` — matching the observed `got v128:00…00 expected <val2>` FAIL. Mirror class of D-066 / D-070. Fix: resolve mask_v / val1_v_phys / val2_v_phys BEFORE DUP; if either val aliases mask_v, stash through V31 (project-canonical SIMD scratch). Verification: with SUPPORTED entry temporarily restored, Mac aarch64 simd_assert 13295/0/446 → **13301/0/440** (+6 PASS, 0 FAIL). **However**, OrbStack x86_64 showed 3 FAIL on cond ≠ 0 cases (got val2 expected val1) — x86_64 `select` handler in emit.zig:1372-1411 has no v128 dispatch and routes v128 vregs through the GPR CMOV path. **D-083 part 2 filed**: x86_64 needs (a) v128 dispatch arm + (b) new `x86_64/op_simd.emitV128Select` using mask-based PAND/PANDN/POR. SUPPORTED entry **stays deferred** until part 2 lands. arm64 fix preserved as latent-bug guard (no regression; future v128 select alias patterns covered). 3-host gate: Mac aarch64 `zig build test` + `test-spec-simd` green (13295/0/446 with SUPPORTED still deferred); OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. Lint + fmt + zone + file_size + check_skip_adrs all clean. Files: `arm64/op_simd.zig:emitV128Select` (+18 net — alias detection + V31 stash + reorder); regen comment updated to D-083 part 2 status. (`33218eef`) `[x]`
- **9.9-h-29** — §9.9 close residual chunk 5: `assert_trap` directive support + quoted-name tokenisation discharge the last 2 manifest-level skip-impl categories (22 lines). **After this chunk, the only manifest-line skip-impl is the 6 D-083-deferred `select_v128_i32` lines.** Part A: `scripts/regen_spec_simd_assert.sh` replaces unconditional `skip-impl assert_trap-v128-pending` emit with real `assert_trap <fn> <args>` distillation gated by SUPPORTED (added `((i32,), ())` for `store_data_6`); `test/spec/simd_assert_runner.zig` adds `assert_trap` directive handler + `runAssertTrap` (~125 LOC) covering `()/v128` result × `()/(i32,)` arg shapes. Maps `error.Trap` → PASS, success → FAIL "did NOT trap", other error → FAIL "errored X (expected Trap)". Mirrors `spec_assert_runner.zig`. Part B: regen removes `export-name-has-spaces` skip filter, emits `assert_return 'v128.load align=16' i32:0 -> v128:…` with single-quoted fn name; runner adds `splitFnAndArgs` helper recognising `'…'` quoted form, routes `runAssertReturn` + `runAssertTrap` through it. simd_assert_runner Mac+OrbStack: 13273/0/468 (78 skip-impl + 390 skip-adr) → **13295/0/446 (= 56 skip-impl + 390 skip-adr)** = **+22 PASS, −22 skip-impl, 0 FAIL** bit-identical. Manifest-line breakdown: assert_trap-v128 18 → 0; export-name-has-spaces 4 → 0; v128-param-pending 6 (D-083). Runner-classified ~50 skip-impl from SKIP-VALIDATOR-GAP / SKIP-PARSER-GAP on assert_invalid/assert_malformed compile-clean cases (distinct from manifest-line; Phase-10+ concern). Files: regen_spec_simd_assert.sh (+26 net); simd_assert_runner.zig (+190 net); 3 manifest files (simd_address/simd_align/simd_load_extend). (`2f1e75f1`) `[x]`
- **9.9-h-28** — §9.9 close residual chunk 4: 11 new v128-mixed-arg entry helpers discharge residual 32 v128-param-pending lines. Helpers (`callI32_v128v128v128`, `callI32_v128i32`, `callI32_v128i64`, `callI64_v128i64`, `callF32_v128f32`, `callF64_v128f64`, `callV128_v128v128v128v128`, `callV128_v128i32v128`, `callV128_v128i32v128i32`, `callV128_v128i64v128i64`, `callVoid_i32v128`) cover: *_with_v128.bitselect (12); i*x*_replace_lane-{s,u} + as-i*x*_any_true-operand (10); as-i32x4_any_true-operand2 (1); i64x2_replace_lane (1; surfaced as `(v128,i64)→i64` not `(v128,i32)→i32` — subagent corrected from initial brief); f32x4/f64x2_replace_lane (2); swizzle/shuffle-as-i8x16_*-operands (2); as-v8x16_swizzle-operand (1); as-v8x16_shuffle-operands + as-i*x*_add-operands family (~5); v128.store align=16 (1, void return). simd_assert_runner Mac+OrbStack: 13241/0/500 (110 skip-impl + 390 skip-adr) → **13273/0/468 (= 78 skip-impl + 390 skip-adr)** = **+32 PASS, −32 skip-impl, 0 FAIL**. One reclassification: v128.store align=16 line moved from v128-param-pending → export-name-has-spaces (shape filter now passes; export-name quoted filter catches the trailing space). Export-name-has-spaces tally 3 → 4; no regression. **v128-param-pending residual = 6** (all D-083-deferred select_v128_i32). Files: entry.zig (+265); regen_spec_simd_assert.sh (+11 SUPPORTED entries); simd_assert_runner.zig (+11 arms); 3 manifest files (simd_align/simd_boolean/simd_lane). 0 FAIL on either host — new shapes are pure ABI plumbing; JIT op handlers already correct from earlier §9.7/§9.9 work. Remaining §9.9 close work: v128-param-pending 6 (D-083 deferred); assert_trap-v128-pending 18 (next); export-name-has-spaces 4. (`81cc1ecc`) `[x]`
- **9.9-h-27** — §9.9 close residual chunk 3: 3 new v128-mixed-arg entry helpers discharge 136 v128-param-pending; D-083 deferral. 3 active shapes: `(v128) → i64` (`callI64_v128`; i64x2.extract_lane); `(v128, i64) → v128` (`callV128_v128i64`; i64x2.replace_lane); `(v128, v128) → i32` (`callI32_v128v128`; composite `*_with_v128.{and,or,xor}` / `*_as_i32.*_operand` exports doing `(any_true\|all_true)(v128 op v128)`). 4th helper `callV128_v128v128i32` for `(v128, v128, i32) → v128` (`select_v128_i32`) landed in entry.zig + runner arm but SUPPORTED entry deferred — surfaced **2 FAILs** at gate-time (`got v128:00…00, expected v128:05…08` for `cond=0`). Static-analysis of `arm64/op_simd.emitV128Select` BSL semantics appears correct; bug filed as **D-083** with 3 root-cause hypotheses (i32-param-marshal vs W0/W1; populateShapeTags for select cond; entry-helper sig mismatch). 6 select_v128_i32 lines re-skipped post-deferral. simd_assert_runner Mac+OrbStack: 13105/0/636 → **13241/0/500 (= 110 skip-impl + 390 skip-adr)** = **+136 PASS, −136 skip-impl, 0 FAIL** after deferral. Residual v128-param-pending 39 (6 select_v128_i32 D-083-deferred + 12 *_with_v128.bitselect needing `(v128,v128,v128)→i32` + 8 *_replace_lane-{s,u} needing `(v128,i32)→i32` + 13 one-off composites). Files: entry.zig (+91); regen_spec_simd_assert.sh (+20); simd_assert_runner.zig (+31); 2 manifest files (simd_boolean / simd_lane). 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` + `test-spec-simd` green; windowsmini deferred per ADR-0049. Lint + fmt + zone + file_size + check_skip_adrs all clean. (`fe4a7c4d`) `[x]`
- **9.9-h-26** — §9.9 close residual chunk 2: 6 new v128-mixed-arg entry helpers discharge 499 v128-param-pending lines. New helpers in `src/engine/codegen/shared/entry.zig`: `callI32_v128` ((v128)→i32 for all_true/any_true/bitmask/i*x*.extract_lane.{s,u}); `callF32_v128` / `callF64_v128` (FP extract_lane); `callV128_v128i32` ((v128, i32)→v128 for i*x*.shl/shr_s/shr_u + i*x*.replace_lane); `callV128_v128f32` / `callV128_v128f64` (FP replace_lane). `callconv(.c)` handles AAPCS64/SysV FP-vs-GPR routing automatically; no manual coercion needed. SUPPORTED set in regen script + dispatch arms in simd_assert_runner.zig mirror the new shapes. 12 manifest files regenerated. simd_assert_runner Mac aarch64: 12606/0/1135 (745 skip-impl + 390 skip-adr) → **13105/0/636 (246 skip-impl + 390 skip-adr)** = **+499 PASS, −499 skip-impl, 0 new FAILs**. OrbStack x86_64 tally bit-identical (13105/0/246/390). Residual v128-param-pending 175 lines (out-of-h-26-scope shapes: (v128)→i64 i64x2.extract_lane 15; (v128, i64)→v128 i64x2.replace_lane 8; (v128, v128, i32)→v128 select_v128_i32 6; (v128, v128)→i32 + as_i32/as_select_cond/as_if_cond/as_br_if_cond composites ~140) — follow-up chunk 9.9-h-27. Files: entry.zig (+127); regen_spec_simd_assert.sh (+20); simd_assert_runner.zig (+48); 3 manifest files (simd_bit_shift / simd_boolean / simd_lane). 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. Lint + fmt + zone + file_size + check_skip_adrs all clean. (`b33008e8`) `[x]`
- **9.9-h-25** — §9.9 close residual chunk 1: NaN-pattern lane support in simd_assert manifest+runner; discharge 1222 skip-impl. New manifest token `v128_lanes:<shape>:<lane0>,...` where `<shape>` ∈ {f32x4, f64x2} and each lane is `c` (canonical NaN) / `a` (arithmetic NaN) / `V<hex>` (exact bits). Emitted by regen script only when ≥1 lane is `nan:*`; legacy `v128:<32-hex>` retained for all-concrete lanes. Per-lane semantics per Wasm spec §A.2: `c` (f32) ∈ {0x7fc00000, 0xffc00000}; `c` (f64) ∈ {0x7ff8000000000000, 0xfff8000000000000}; `a` (f32) = (bits & 0x7fc00000) == 0x7fc00000; `V<hex>` = bit-exact. New runner helpers: `parseV128LanesToken` + `matchLaneF32` + `matchLaneF64`; existing 5-shape arg-dispatch ladder refactored into shared `invokeV128` helper (legacy `v128:` byte-identical). 2 manifests regenerated (610 f32x4 + 612 f64x2 lines flipped from `skip-impl` to `assert_return v128_lanes:...`). NaN tokens only appear in `expected`, never in `args` (verified empirically). simd_assert_runner Mac aarch64: 11384/0/2357 (1967 skip-impl + 390 skip-adr) → **12606/0/1135 (745 skip-impl + 390 skip-adr)** = **+1222 passed, −1222 skip-impl, 0 new FAILs**. Validates JIT FP NaN-propagation correctness (P1) — both ARM64 NEON and SSE produce ±canonical NaN per IEEE-754 default-NaN on canonical inputs; `c` test accepts both ±canonical, `a` is strictly looser. Lint + fmt + zone + file_size + check_skip_adrs all clean. 3-host gate: Mac + OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. Files: `scripts/regen_spec_simd_assert.sh` (+50/-12); `test/spec/simd_assert_runner.zig` (+191/-34); 2 manifests. (`b93d191c`) `[x]`
- **9.9-h-24** — Track C chunk 4/4 — **Track C COMPLETE**. (1) ADR-0029 §"Revision history" gains 2026-05-12 Path B closure row describing the chunk 9.9-h-21..-24 chain that resolved the 2026-05-11 design-vs-implementation divergence; §"Decision" prefix-vocab now load-bearing (implementation matches design). (2) 3 skip-ADRs (skip_text_format_parser / skip_embenchen_emcc_env_imports / skip_externref_segment) gain §"Implementation" subsection naming their prefix consumer + manifest counts (410 / 4 / 1 lines respectively). (3) 2 affected skip-ADRs drop "NOT EFFECTIVE per ADR-0050 D-2" Status warning; replaced with "Operationally effective via `skip-adr-<id>` prefix per ADR-0029 Path B"; historical "Current effectiveness gap (2026-05-11)" sections kept as audit trail. (4) `scripts/check_skip_adrs.sh` extended with prefix-vocab coherence checks: every `skip-adr-<id>` line resolves to existing ADR file; every skip-ADR has ≥1 manifest consumer; gate output reports 4+1+410=415 manifest consumers across the 3 ADRs (0 violations). (5) `scripts/gate_commit.sh` invokes `check_skip_adrs --gate` between file_size_check and zig build test (output suppressed; value is the exit code). (6) **D-072 + D-073 deleted** from `.dev/debt.md` Active table (D-072 (c)-path superseded by D-082; D-073 closed by ADR-0029 Path B). 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` green; check_skip_adrs --gate green; gate_commit green; windowsmini deferred per ADR-0049. zig fmt + zone_check + file_size_check (still 0 hard-cap) all clean. **Track C complete; §9.9 close residual is next (skip-impl reduction)**. (`c49e856c`) `[x]`
- **9.9-h-23** — Track C chunk 3/4 (ADR-0029 Path B vocab migration): `wast_runtime_runner.zig` gains prefix-aware skip vocab + 5 wasmtime_misc manifest_runtime.txt files migrated + D-072 (a/b) discharged + **D-082 filed**. `handleLine` return type changed bool → `LineResult` enum (`passed`/`failed`/`skipped_impl`/`skipped_adr`); 17 existing sub-handlers kept returning bool, wrapped in `blk:` for the dispatch tail. Three prefix branches added at handleLine top: `skip-impl <reason>` → `.skipped_impl`; `skip-adr-<ADR-id> <reason>` → `.skipped_adr`; bare `skip <reason>` → stderr WARN + `.skipped_impl`. `walkCorpusOrCategory` + `runCorpus` signatures gain `skipped` + `skipped_adr` params; main tallies + emits `wast_runtime_runner: N passed, M failed, S skipped (= SI skip-impl + SA skip-adr)` matching other runners. 5 manifests hand-migrated: 4 embenchen `module *.1.wasm` → `skip-adr-skip_embenchen_emcc_env_imports *.1.wasm`; externref-segment `module externref-segment.0.wasm` → `skip-adr-skip_externref_segment externref-segment.0.wasm`. `zig build test-wasmtime-misc-runtime`: `266 passed, 0 failed, 5 skipped (= 0 skip-impl + 5 skip-adr)` (down from 266/5/0 pre-migration). **D-072 (a/b)-path discharged** (runner-side enforcement now operational); narrative updated to defer final row deletion to 9.9-h-24. **D-082 filed** carrying the (c)-path actual-fixture-fixes scope with two concrete `blocked-by:` sub-rows (Phase 11 embenchen suite for 4 embenchen fixtures; Phase 10 GC-touch OR Phase 11 for externref-segment). Skip-ADR `Status` line update (drop "NOT EFFECTIVE" warning) deferred to 9.9-h-24 alongside ADR-0029 §"Amendment log" + check_skip_adrs.sh extension. 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. Lint clean. zig fmt clean. (`3e5d31c2`) `[x]`
- **9.9-h-22** — Track C chunk 2/4 (ADR-0029 Path B vocab migration): regen scripts emit prefix vocab + manifest sweep. `scripts/regen_spec_1_0_assert.sh` rewrites 9 `skip` lines (8 → `skip-impl <reason>`, 1 → `skip-adr-skip_text_format_parser`). `scripts/regen_spec_simd_assert.sh` rewrites 8 (7 + 1) plus header docstring updated. Re-run both regen scripts: 26 simd-assert + 5 wasm-1.0-assert manifests regenerated; 2346 insertions / 2345 deletions (1-line-per-skip prefix rewrite + minor structural deltas). Bare-skip count in `test/spec/` after sweep: **0** (was 2357 simd + several 1.0-assert). New tally: skip-impl 1917 + skip-adr 410 = 2327 prefixed lines. simd_assert_runner now prints `11384 passed, 0 failed, 2357 skipped (= 1967 skip-impl + 390 skip-adr) (over 26 manifests)` — PASS/FAIL counts unchanged; 9.9-h-21 WARN noise gone. Remaining `test/wasmtime_misc/wast/{embenchen,reftypes}/manifest_runtime.txt` (hand-authored, not regen-driven) retain bare-skip until chunk 9.9-h-23 hand-migration + wast_runtime_runner prefix-aware update. 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test-all` green; windowsmini deferred per ADR-0049. Lint clean. zig fmt clean. (`dedb1413`) `[x]`
- **9.9-h-21** — Track C chunk 1/4 (ADR-0029 Path B vocab migration): both spec runners gain prefix-aware skip directive classification. `spec_assert_runner.zig` + `simd_assert_runner.zig` now parse `skip-impl <reason>` / `skip-adr-<ADR-id> <reason>` prefixes directly; bare `skip <reason>` retained as back-compat (stderr WARN + counts as skip-impl) until chunk 9.9-h-22's regen sweep migrates manifests. simd_assert_runner gains parallel `skipped_adr` tally; output format now twin-tally `N passed, M failed, S skipped (= SI skip-impl + SA skip-adr) (over K manifests)` matching spec_assert_runner. spec_assert_runner's docstring updated to list the three vocab forms. `spec_assert_runner.zig` bare-skip arm keeps the legacy hardcoded `directive-assert_malformed-text` → skip-adr classification (preserves correctness for `skip_text_format_parser.md` until 9.9-h-22 regen lands); to be removed in a follow-up once 0 bare-skip lines remain. `wast_runner.zig` checked — no skip handling (corpus does not use). `wast_runtime_runner.zig` prefix-aware migration deferred to chunk 9.9-h-23 alongside D-072 (a/b) discharge. 3-host gate: Mac aarch64 `zig build test` + `zig build test-spec-simd` green (runner reports 11384/0/2357 with full skip-impl tally); OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. Lint clean. zig fmt clean. WARN lines fire for every bare-skip line during the h-21→h-22 transition window — expected per prep doc §6.2 ("expected noise; gate stays green because skip-impl counts are preserved"). (`fc9bd370`) `[x]`
- **9.9-h-20** — Track B chunk 6/6 — **Track B COMPLETE**. (1) `scripts/gate_commit.sh` invokes `bash scripts/file_size_check.sh --gate` (was warn-only); removes "(warn-only, see D-057)" callout. (2) **ADR-0054 lands** (`.dev/decisions/0054_track_b_source_split.md`) — codifies 5 prep-mode design decisions (single ADR for both arches / 4-way op_simd / 3-way encoder / 4-way test mirror with strict `<source>_test.zig` suffix / tiered pub) + two pragmatic facade strategies + warn→gate transition sequencing. Alternatives A..E rejected with rationale. ADR-0054 supersedes the prep doc as load-bearing reference; prep doc remains as implementation walkthrough. (3) **D-057 + D-065 closed** (rows deleted from `.dev/debt.md` Active table). D-057 discharge via 9.9-h-15/-h-16/-h-17 (x86_64 op_simd + op_simd_test + inst_sse splits). D-065 discharge via 9.9-h-18/-h-19 (arm64 op_simd + inst_neon splits). (4) **D-081 filed** (legacy `emit_test_int.zig` / `emit_test_float.zig` rename, blocked-by `emit.zig` source split per D-052; current emit.zig 1991 LOC well past 1000 soft cap so barrier effectively dissolved, awaiting loop capacity). Gate verification: `scripts/file_size_check.sh --gate` exit 0 (16 soft-cap warnings, 0 hard-cap violations); `scripts/gate_commit.sh` exit 0 (all sub-gates pass). Track B complete; next §9.9 close residual per handover queue. (`00f93d24`) `[x]`
- **9.9-h-19** — Track B chunk 5/6: arm64 `inst_neon.zig` 3-way encoder split per `.dev/phase10_prep/track_b_source_split.md` §4.3 (arm64 encoder partition; mirror of x86_64 9.9-h-17). `inst_neon.zig` 2323 LOC / 176 fns → `inst_neon.zig` (494 LOC; foundation: Memory ops LDR/STR Q-form + LD1R + reg-move ORR/MOV/DUP + splat + bitwise AND/BIC/EOR/MVN/BSL/BIT/BIF/NOT + Vn/Xn type aliases) + `inst_neon_arith.zig` (1282 LOC; ADD/SUB/MUL/MIN/MAX/ABS/NEG/CNT/AVGR/sat-arith + shifts USHL/SSHL/SSHR-imm + reductions UMAXV/UMINV/ADDV + ZIP1/EXT/TBL + SXTL/UXTL + SQXTN/SQXTUN + SCVTF/UCVTF/FCVTL/FCVTN/FCVTZ + FP arith FADD/FSUB/FMUL/FDIV + FP unary FABS/FNEG/FSQRT/FRINT* + FMAX/FMIN) + `inst_neon_lane_cmp.zig` (590 LOC; UMOV/SMOV/INS lane access B/H/S/D + DUP-scalar/INS-element FP lane + CMEQ/CMGT/CMGE/CMHI/CMHS int cmp + FCMEQ/FCMGT/FCMGE FP cmp). All 3 files under 1500 LOC soft cap target. Approach: Option A (call-site updates) — arm64 has no inst.zig facade analog. Consumer rewrites: op_simd 7+9, op_simd_int_arith 45+3, op_simd_int_cmp_lane 33+54, op_simd_float 40+20 (arith/lane_cmp counts, +2 imports each); foundation-only consumers (emit.zig, gpr.zig, op_call.zig, op_control.zig, op_globals.zig) untouched. Cross-file test refs patched: inst_neon_arith uses inst_neon.encOrrV16B in one test; inst_neon_lane_cmp uses inst_neon_arith.encFMax4S in one test (+1 import in lane_cmp for the arith ref). Vn/Xn type aliases re-exported via `const Vn = inst_neon.Vn;` pattern. Helpers promoted to pub: none (Vn/Xn were already pub in foundation). zwasm.zig: 2 new test-discovery imports. Lint clean. zig fmt clean. Zone check clean. 3-host gate: Mac aarch64 `zig build test` 1586/12/0; OrbStack x86_64 1570/28/0; windowsmini deferred per ADR-0049. **file_size_check hard-cap list 1 → 0** — last hard-cap exceeder closed; warn→gate flip lands chunk 9.9-h-20 alongside D-057 + D-065 close. (`8c3a30f6`) `[x]`
- **9.9-h-18** — Track B chunk 4/6: arm64 `op_simd.zig` 4-way source split per `.dev/phase10_prep/track_b_source_split.md` §4.3 (arm64 partition; mirror of x86_64 9.9-h-15). `op_simd.zig` 2307 LOC / 249 fns / no inline tests → `op_simd.zig` (752 LOC; orchestrator: helpers + V128 mem family + V128 bitwise + Const + Bitselect + AnyTrue) + `op_simd_int_arith.zig` (325 LOC; Add/Sub/Mul/Abs/Neg/Popcnt + Min/Max/AvgrU + Shl/ShrS/ShrU + i64x2.mul) + `op_simd_int_cmp_lane.zig` (844 LOC; splat + extract/replace lane + cmps + extend low/high + narrow + bitmask + AllTrue + shuffle/swizzle + extmul + extadd-pairwise) + `op_simd_float.zig` (525 LOC; all emitF* 54 fns). All four well under 1500 LOC soft cap. Approach: Option A (call-site updates), mirroring x86_64's 9.9-h-15 — arm64 has no inst.zig facade analog. Tiered pub cross-class primitives in op_simd.zig per ADR-0054 §"Helper visibility": `simd_scratch_v` (V31), `emitV128Binop`, `emitV128Unop`, `emitV128BinopSwapped`, `emitV128Ne`. emit.zig: 3 new module imports + 180 call-site rewrites (46/76/58 across arith/cmp_lane/float); ~40 remaining `op_simd.*` calls untouched (mem/bitwise stay in orchestrator). zwasm.zig: 3 new test-discovery imports. Lint clean. zig fmt clean. Zone check clean (Zone 2 inter-arch boundary preserved — no x86_64 imports). 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. file_size_check hard-cap list shrinks 2 → 1 (arm64 op_simd exits; remaining = arm64 inst_neon.zig 2323 LOC, chunk 9.9-h-19). (`322ccd73`) `[x]`
- **9.9-h-17** — Track B chunk 3/6: x86_64 `inst_sse.zig` 3-way encoder split per `.dev/phase10_prep/track_b_source_split.md` §4.2 first table. `inst_sse.zig` 2464 LOC / 170 fns → `inst_sse.zig` (493 LOC; 15 tests; foundation = EncodedInsn + mem load/store XMM + MOV reg-shape + scalar cvt + MOVUPS-RIP-rel patch helper) + `inst_sse_packed.zig` (1086 LOC; 81 tests; all encP* packed integer + MOVMSK* + saturating arith + PAVG*) + `inst_sse_scalar.zig` (521 LOC; 20 tests; SSE scalar binary + FP packed shapes ADD/SUB/MUL/DIV/SQRT/MIN/MAX PS+PD + Cmpps/pd + Roundps/pd + packed cvt CVTDQ2PS/PS2PD/PD2PS/DQ2PD/TPS2DQ/TPD2DQ + Shufps/Unpcklps/Insertps). Test totals 15+81+20 = 116 preserved. `inst.zig` updated to route each `pub const enc*` re-export at the correct sub-module — all ~800+ consumer call sites stay byte-identical (`op_simd*.zig`/`op_alu_float.zig`/`op_convert.zig`/`emit.zig` all reference encoders through the `inst.encXxx` facade). Helper duplication trade-off: `encSsePackedIntBinop` (66-prefix) used by both packed + scalar; scalar file carries private copy `encSsePd66Binop` (~10 lines bit-identical) to preserve mutual file independence. `encMovmskps`/`encMovmskpd` placed in packed file as companion to encPmovmskb (share `encSseXmmToGprRM` helper). zwasm.zig: 2 new test-discovery imports. Lint clean. zig fmt clean. Zone check clean. 3-host gate: Mac aarch64 + OrbStack x86_64 `zig build test` green; windowsmini deferred per ADR-0049. file_size_check hard-cap list shrinks 3 → 2 (inst_sse leaves; remaining 2 are arm64 inst_neon.zig 2323 + arm64 op_simd.zig 2307 — chunks 9.9-h-18 / 9.9-h-19). (`cef126e9`) `[x]`
- **9.9-h-16** — Track B chunk 2/6: x86_64 `op_simd_test.zig` 4-way mirror per `.dev/phase10_prep/track_b_source_split.md` §4.2 (last table — tests partition). `op_simd_test.zig` 2700 LOC / 91 tests → split into `op_simd_test.zig` (134 LOC; 4 tests: V128 Not/And/Andnot/Bitselect), `op_simd_int_arith_test.zig` (491 LOC; 14 tests: Add/Sub/Mul/sat/shift/min-max/neg/abs/avgr/popcnt/Q15mulr/Dot), `op_simd_int_cmp_lane_test.zig` (1190 LOC; 41 tests: int cmp/splat/extract/replace/narrow/extend/extmul/extadd-pairwise/bitmask/AllTrue/alias-stash/shuffle/swizzle), `op_simd_float_test.zig` (941 LOC; 32 tests: FP arith/cmp/min-max NaN-correction/lane/convert/round/pmin/pmax + 2 emitI32x4TruncSatF32x4{S,U}). All 4 under 2000 LOC hard cap. Test totals 4+14+41+32 = 91 (matches original). op_simd.zig: 206 transitional `pub const emitXxx = <module>.emitXxx;` re-exports + 3 transient module-alias imports removed; the re-exports were a back-compat scaffold added in 9.9-h-15 for the then-unsplit test file. op_simd.zig 1201 → 983 LOC (now under 1000 soft cap). zwasm.zig: 3 new test-discovery imports. file_size_check hard-cap list 4 → 3 files (op_simd_test.zig leaves the list; remaining: arm64/inst_neon.zig 2323, arm64/op_simd.zig 2307, x86_64/inst_sse.zig 2464 — addressed by chunks 9.9-h-17/18/19). Lint clean. zig fmt clean. Zone check clean. 3-host gate: Mac aarch64 `zig build test` 1584/12/0; OrbStack x86_64 `zig build test` 1568/28/0; windowsmini deferred per ADR-0049. (`cb3ec874`) `[x]`
- **9.9-h-15** — Track B chunk 1/6: x86_64 `op_simd.zig` 4-way source split per `.dev/phase10_prep/track_b_source_split.md` §4.2 / §6. `op_simd.zig` 4694 LOC → 1202 LOC (orchestrator: emitV128* mem/bitwise/bitselect/anytrue/const + helpers); 3 new sibling files — `op_simd_int_arith.zig` (842 LOC: int ALU/sat/shift/min-max/neg/abs/avgr/popcnt/q15mulr/dot), `op_simd_int_cmp_lane.zig` (1694 LOC: int cmp/splat/extract/replace/narrow/extend/extmul/extadd-pairwise/bitmask/all_true/swizzle/shuffle), `op_simd_float.zig` (1276 LOC: emitF*/FP recipes/trunc-sat-FP/convert/promote/demote). All 4 files under 2000 LOC hard cap (file_size_check still warn-only per D-057; gate flip lands chunk 9.9-h-20). Cross-class primitives promoted to `pub` per prep doc §4.6 tiered visibility: `emitV128IntBinop`, `emitV128AllTrue`, `emitConstLoad`, `lookupOrAppendExtraConst`, `v128MemPrologue`, `v128LoadExtend`, `v128LoadLane`, `v128StoreLane` in op_simd.zig + `emitV128FpUnop` in op_simd_float.zig (consumed by int_arith Abs handlers). emit.zig: 3 new module imports + 206 call-site rewrites (`op_simd.emitXxx` → `op_simd_<class>.emitXxx`). zwasm.zig: 3 new test-discovery imports. op_simd.zig carries 206 transitional `pub const emitXxx = <module>.emitXxx;` re-exports so `op_simd_test.zig` keeps compiling until chunk 9.9-h-16 splits the test file. Zone 2 inter-arch isolation respected. 3-host gate: Mac aarch64 `zig build test` 1584/12/0 green; OrbStack x86_64 `zig build test` 1568/28/0 green; windowsmini deferred per ADR-0049. zig fmt + lint + zone_check + file_size_check all clean. D-057 source-split partial discharge (full close at 9.9-h-20 when file_size_check flips warn→gate). (`5ef919f8`) `[x]`
- **Phase 10 prep mode entered** (no chunk number; `.dev/phase10_prep.md` is the load-bearing artifact). After §9.9-h-14 closed all `now` debts and both hosts reached 11384/0 simd_assert, 4 design decisions remained that block clean Phase 9 close + Phase 10 entry (§9.10 scope vs D-074 bench infra; D-057/D-065 source-split partition; ADR-0029 skip semantics path A vs B; Phase 10 transition gate doc). Each requires human judgment between named alternatives. Modeled after Phase 8 hard-gate pattern: autonomous loop produces Markdown deliverables (no `src/` code changes) one track at a time, surfaces to user after each, suppresses `ScheduleWakeup` re-arm. Resumption is bucket-1 user intervention. Run order A → B → C → D enforced by dependency: Track A's outcome may rewrite §9.10 row wording; Track C's skip-counting rule changes whether Track A's §9.10 closes inside Phase 9 scope. Track D produces the gate doc that lets `/continue`'s hard-gate detector halt at Phase 10 entry. `[~]` (prep, no §9 row flip)
- **9.9-h-14** — 3-v128-arg dispatch + bitselect alias fix (D-070 + D-071 discharged). Two halves: (1) **runner dispatch** — new `entry.callV128_v128v128v128` helper for `(v128, v128, v128) → v128`, simd_assert_runner v128-result branch gains `n_args == 3` arm, manifest gen SUPPORTED set adds the shape, manifests regenerated → ~800 previously-skipped `v128-param-pending` assertions flip to runnable. (2) **Predicted alias bug surfaces on both arches** — D-070 said this would happen specifically when the dispatch landed. ARM64 `emitV128Bitselect`: regalloc LIFO slot-reuse can assign `result_vreg` the same physical V reg as v1 or v2; the `MOV mask, c` clobbers the aliased operand before `BSL` reads it. Fix: stash through V31 (popcnt scratch — outside any popcnt sequence in this handler). x86_64 `emitV128Bitselect`: same shape — `MOVAPS dst, a` clobbers c or b if dst aliases either. Fix: stash c through XMM14 (folds out when c already there), b through XMM15 (currently free after b's stage 1 load completes). Both alias-stash fixes land in the same chunk; both hosts reach **11384 / 0 simd_assert** (+114 PASS each). Mac unit tests 1586 / 1598 (12 skip, 0 fail). Lint clean. D-070 + D-071 moved Active → Recently discharged. D-066 mirror cluster fully swept. Lesson validates project discipline: D-070 sat as `blocked-by` for 6 cycles and was correctly predicted to surface specifically when the structural barrier dissolved. (`d3b6d109`) `[x]`
- **9.9-h-13** — `test-spec-simd` aggregated into `test-all` (preventive). Post-D-078 (c) both Mac aarch64 + OrbStack Linux x86_64 are at 11270 / 0 simd_assert FAIL; adding `test_all_step.dependOn(&run_simd_assert.step)` no longer breaks the gate. Why preventive: the autonomous `/continue` loop's `Parallel test gate` runs `zig build test-all` on both hosts per chunk; without this dependency a silent SIMD regression slips past per-chunk gating until the next `scripts/p9_simd_status.sh` run. Aggregating shifts detection from "next status check" to "this chunk's gate". Build-only change — no `src/` diff; no behaviour change beyond gate scope. Mac aarch64 `zig build test-all` green incl `simd_assert_runner: 11270 passed, 0 failed, 2471 skipped`. OrbStack Linux x86_64 same. windowsmini phase-boundary per ADR-0049 unchanged. (`b9b17628`) `[x]`
- **9.9-h-12** — ADR-0053 Part 3b/3c: `emitV128Bitselect` (3 v128 inputs + 1 v128 result; reclaims XMM7 as a 3rd staging slot for `a` and the spilled dst since the standard 2-stage pool can't hold all 4 operands simultaneously — c→stage 0 XMM14, b→stage 1 XMM15, a→XMM7, dst→home if in-reg else XMM7 with the MOVAPS-from-a folding to a no-op); `emitV128Load` / `emitV128Store` (dst/val migrated, stage 0); `emitV128Const` (dst migrated, stage 0, signature gains `spill_base_off`). Inline custom load/store for the Bitselect `a` and spilled dst via `encLoadXmmV128MemRBPDisp32` / `encStoreXmmV128MemRBPDisp32` directly (the standard `xmmLoadSpilledV128` hardcodes XMM14/XMM15). **OrbStack simd_assert: 11257 / 1 → 11270 / 0 FAIL** — simd_bitwise.17 cleared, **D-078 (c) FULLY DISCHARGED**. Mac aarch64 already at 11270 / 0 since §9.9-h-4 — both hosts now at zero failing fixtures. §9.9 exit-criterion `fail = skip = 0` is one host (windowsmini, deferred per ADR-0049 to phase boundary) + the SKIP backlog away from closure. D-078 row moved Active → Recently discharged with all-three-sub-clusters closed annotation. Mac unit tests 1586 / 1598 (12 skip, 0 fail). Lint clean. Lesson queued: scratch reclaim ("reclaim and prove" pattern) — XMM7's D-066 alias-stash role is per-call; a handler can safely reuse XMM7 if it can prove no alias check fires within the same handler. Bitselect's reclamation is the first such use; future ≥3-v128-operand handlers can apply the same pattern. (`5eb724e7`) `[x]`
- **9.9-h-11** — ADR-0053 Part 3a: v128 binop family + Not/Andnot/AnyTrue spill migration. 53 ops via the shared `emitV128IntBinop` helper migrated from `gpr.resolveXmm` (rejects `.spill`) to the §9.9-h-10 MOVUPS-based helpers (`xmmLoadSpilledV128` / `xmmDefSpilledV128` / `xmmStoreSpilledV128`); paralleled in `emitV128Not` / `emitV128Andnot` / `emitV128AnyTrue`. Wrapper signatures gain `spill_base_off: u32`; dispatch arms in `emit.zig` thread the existing local. Per-handler stage allocation: emitV128IntBinop uses rhs stage 0 (XMM14) + lhs/result stage 1 (XMM15) — `MOVAPS dst, lhs` writes the result XMM, then `encoder(dst, rhs)` reads stage 0; the existing D-066 alias-stash via XMM7 still fires for in-reg-pool collisions. emitV128Not flips stages so the PCMPEQ-ones scratch stays at XMM14. emitV128Andnot uses rhs stage 0 + result stage 0 — the MOVAPS dst,rhs preserves rhs in XMM14 across the PANDN. emitV128AnyTrue: src stage 0; dst is GPR. **emitV128Bitselect deferred to §9.9-h-12** (3 v128 inputs + 1 v128 result = 4 operands but only 2 stage XMMs; needs a 3rd-stage reservation, candidate XMM7 reclaim). One test rewrite: the prior `emitI32x4Add: spilled rhs surfaces UnsupportedOp` test asserted the legacy reject-on-spill behaviour; replaced with a positive check that the MOVUPS-load + PADDD sequence emits correctly. Mac aarch64 simd_assert: 11270/0 unchanged. Mac unit tests 1586/1598 (12 skip, 0 fail). Lint clean. OrbStack simd_assert: 11257/1 unchanged — simd_bitwise.17 still fails on the Bitselect path. Partial discharge of D-078 (c). (`1f5d4f99`) `[x]`
- **9.9-h-10** — ADR-0053 Part 2: v128 XMM spill helpers. Three new MOVUPS-based helpers in `src/engine/codegen/x86_64/gpr.zig`: `xmmLoadSpilledV128` (emits `MOVUPS xmm14, [RBP+disp]`), `xmmStoreSpilledV128` (emits `MOVUPS [RBP+disp], xmm14`), `xmmDefSpilledV128` (returns stage-or-home XMM). Distinct from the existing MOVSD-based 8-byte trio (which stays correct for f32/f64). Stage register pool unchanged (`abi.fp_spill_stage_xmms` = XMM14/XMM15). New `rbpLoadXmmV128Auto` / `rbpStoreXmmV128Auto` internal disp8/disp32 auto-pickers wrap the existing `encLoadXmmV128MemRBP*` / `encStoreXmmV128MemRBP*` encoders. 4 new unit tests: spilled-load MOVUPS emit, spilled-store MOVUPS emit, def-spilled stage selection (both stages), in-reg short-circuit. No callers migrated yet — pure addition; simd_bitwise.17 still fails on OrbStack pending Part 3 handler migration. Mac aarch64 simd_assert: 11270/0 unchanged. Mac unit tests 1586/1598 (+4 helper tests; 12 skip, 0 fail). Lint clean. (`89c91b08`) `[x]`
- **9.9-h-9** — ADR-0053 Part 1: shape-aware spill offsets. `Allocation` gains `spill_offsets: ?[]const u32` — a per-spill-slot byte offset table. Populated post-`compute()` by new `computeSpillOffsets()` helper when `shape_tags` non-null AND at least one slot is occupied by a v128 vreg. v128 slots pad up to the next 16-byte boundary + consume 16 bytes; scalar slots stay 8-byte. `Allocation.slot()` consults the table when present (legacy uniform-8 formula stays for the all-scalar fallback). `Allocation.spillBytes()` returns `align_up(last_offset + 16, 16)` when offsets exist. 5 new unit tests: scalar-only legacy path, mixed scalar+v128 layout, alignment-padding with leading scalar, no-v128-vreg null skip. No observable behaviour change in this chunk — only Parts 2 + 3 use the new table; existing helpers (qLoadSpilled / xmmLoadSpilled) ignore it. Foundation chunk for §9.9-h-10..-N. Mac aarch64 simd_assert: 11270 / 0 unchanged. Mac test-all green (1582/1594 unit, +5 new tests; realworld / wast / spec_assert / wasi / wast_runtime all 0 failed). OrbStack simd_assert: 11257 / 1 unchanged. Lint clean. (`d783b872`) `[x]`
- **9.9-h-8** — ADR-0053 design for x86_64 v128 XMM spill (D-078 (c) discharge design only). Three structural barriers identified for the last named OrbStack visible FAIL (simd_bitwise.17): (1) `Allocation.slot()` uniform 8-byte spill stride would have v128 vregs overlap neighbouring slots if MOVUPS'd at 16 bytes; (2) existing `xmmLoadSpilled` / `xmmStoreSpilled` use 8-byte MOVSD (truncates v128); (3) ~145 `resolveXmm` call sites reject `.spill` slots. ADR-0053 accepts the three-part design (shape-aware allocator spill sizing + MOVUPS helpers `xmmLoadSpilledV128` / `xmmStoreSpilledV128` / `xmmDefSpilledV128` + handler migration, co-delivering D-057 source-split of `op_simd.zig` at 4554 LOC). Alternatives A (uniform spill-aware `resolveXmm`), B (16-byte stride for all FP), C (reclaim XMM7 from D-066 stash), D (defer / SKIP) considered and rejected. Implementation chunk family = §9.9-h-9..-h-N (multi-chunk). No source change in this chunk — design lands; D-078 (c) row body retargeted at ADR-0053. Mac aarch64 unchanged 11270/0 FAIL; OrbStack unchanged 11257/1 FAIL. Lesson recorded in the ADR's "Alternative A rejected" framing: API-shape changes (new `xmmLoad/Store/DefSpilledV128` helpers) cannot be hidden behind a `resolve*` function alone — emit handlers need to know whether to flush back, so the API change is a handler migration regardless. (`558a2da6`) `[x]`
- **9.9-h-7** — x86_64 v128 call marshal + return + if-else merge — **D-080 discharged**. Three x86_64 emit gaps surfaced together once D-077 unmasked the FAIL list: (a) `marshalCallArgs.v128` rejected v128 args via the `v128 / funcref / externref => UnsupportedOp` catch-all (caller-side v128 ABI absent on x86_64 — Mac arm64 had it from §9.9-f-3); (b) `captureCallResult.v128` had the same catch-all rejection for v128 return values; (c) `emitEndIntra`'s if-else merge MOV uniformly used the GPR-32-bit path (`encMovRR(.d, ...)`), truncating v128 merges to 32 bits — verifiable as the `as-if-else-retval` value mismatch where got = then-branch's first 32 bits, expected = else-branch full 128 bits. Fixes: (a) SysV XMM0..XMM7 register-only path with MOVAPS (Win64 v128-by-hidden-pointer + ≥9 v128 args remain blocked by D-062); (b) MOVAPS from XMM0 (= `abi.return_xmm`) into resolved dst XMM; (c) shapeTag-dispatched MOVAPS branch in the merge MOV loop, mirroring `arm64/op_control.zig` lines ~423-440 (uses `alloc.shapeTag(merge_vreg) == .v128` to route v128 results to XMM/MOVAPS instead of GPR/MOV.d). All three fixes are register-only — spilled v128 vregs trip D-078 (c) cleanly via `resolveXmm`. **OrbStack simd_assert: 11232 PASS / 3 FAIL → 11257 PASS / 1 FAIL** (+25 PASS, -2 FAIL: simd_const.386 + .388 both cleared). The remaining OrbStack FAIL is simd_bitwise.17 (D-078 (c) — last named cluster). Mac aarch64 unchanged at 11270 / 0 FAIL. Mac unit tests 1577/1589 (12 skip, 0 fail). Lint clean. Per ADR-0049 windowsmini deferred to phase boundary. Lesson queued: grep `src/engine/codegen/x86_64/op_*.zig` for `rejectUnsupported.*v128` / `v128.*UnsupportedOp` patterns — preventive audit may surface more latent emit gaps no current fixture exercises. (`7925cf61`) `[x]`
- **9.9-h-6** — `simd_assert_runner` deinit panic on OrbStack — **D-077 discharged**. Root cause: `linker.link`'s empty-module early-exit at line 132 returns a `JitModule` whose `block.bytes = &[_:0]u8{}` (empty static sentinel slice, not mmap-backed). `JitModule.deinit → jit_mem.free` then called `std.posix.munmap` unconditionally; Linux's `std.posix.munmap` asserts INVAL → `unreachable` and the process panics. macOS hid the bug because `std.c.munmap` discards its return value silently (the Mac branch in `jit_mem.free` uses `_ = std.c.munmap(...)`). Fix: one-line `if (block.bytes.len == 0) return;` guard at the top of `jit_mem.free`, applied on every platform. OrbStack now prints the structured summary: **11232 passed / 3 failed / 2509 skipped** (vs Mac 11270/0/2471). The +3 failed surfaces a previously-masked x86_64 compile gap on simd_const.386 + simd_const.388 (both pass on Mac through the §9.9-h-2..-h-4 chain) — tracked as new D-080. Mac unchanged at 11270/0 FAIL. Mac unit tests 1577/1589 (12 skip, 0 fail). Lint clean. Per ADR-0049 windowsmini deferred to phase boundary. (`81ff62af`) `[x]`
- **9.9-h-5** — x86_64 FP replace_lane alias fix — **D-078 (a) discharged**. Root cause was NOT in `emitF64x2ExtractLane` (PSHUFD imm 0x44 was correct, as the debt narrative's static-analysis already noted) — it was in `emitF64x2ReplaceLane`. The fixture exported under the `f64x2_extract_lane` name combines extract+replace; the bug surfaced only on replace. Classic D-066 mirror: when regalloc's LIFO slot-reuse aliased `dst == value && dst != vec`, the unconditional `MOVAPS dst, vec` clobbered value before MOVSD/MOVLHPS read it (symptom: result returned vec = 0...0 instead of (value, vec_hi)). Fix: stash value through XMM7 in the alias case. `bug_fix_survey.md` grep surfaced `emitF32x4ReplaceLane` with the identical shape bug (INSERTPS substitutes for MOVSD/MOVLHPS but reads value through XMM); same stash pattern applied. Two new alias regression tests in `op_simd_test.zig` (`slot_ids = {0, 1, 1}` → result aliases value at XMM9). OrbStack simd_assert visible FAILs: simd_lane f64x2_extract_lane mismatch cleared; only simd_bitwise.17 + D-077 panic remain. Mac aarch64 unchanged at 11270/0 FAIL. Mac unit tests 1577/1589 (+2 new alias tests; 12 skip, 0 fail). Lint clean. Per ADR-0049 windowsmini deferred to phase boundary. Lesson queued: D-066 alias-stash pattern applies to every emit handler whose final-stage instruction reads `src` AFTER an unconditional `MOVAPS dst, vec` — `bug_fix_survey.md` grep for `encMovapsXmmXmm(dst,` is the routine check; ~20 sites in x86_64/op_simd.zig and several should be revisited. (`2a1c31aa`) `[x]`
- **9.9-h-4** — `call_indirect` v128 args Trap — **D-063 discharged**. Root cause: `simd_assert_runner.zig`'s `JitRuntime` construction left `funcptr_base = undefined`, `table_size = 0`, `typeidx_base = undefined`. The JIT-emitted `call_indirect` bounds check `CMP W17, W25 (=table_size=0)` set HS unconditionally for any non-negative idx → branched into the trap stub on every call. Direct `call` PASSes because it doesn't consult the table. The original D-063 spike fixated on `op_call.zig:emitCallIndirect` (emit looked rich) and asserted the runner-side setup mirrored `setupRuntime` — but `simd_assert_runner` uses the ADR-0045 scratch-buffer-direct path that skips `setupRuntime` entirely. Fix: new `runner.applyTableInit(allocator, wasm_bytes, &compiled, funcptrs_buf, typeidxs_buf)` helper walks active element segments and populates caller-owned scratch slots (parallel to 9.9-h-2's `applyDefinedGlobalsInit`); `simd_assert_runner` allocates `[32]u64 scratch_funcptrs` + `[32]u32 scratch_typeidxs` and calls the helper on each `module` directive after globals init. `JitRuntime.funcptr_base / table_size / typeidx_base` now point at the populated scratch. Mac aarch64 simd_assert: **11268 PASS / 2 FAIL → 11270 PASS / 0 FAIL**. OrbStack visible FAILs unchanged (D-078 a + c + D-077 panic; the call_indirect path was already correct on x86_64). Mac unit tests: 1575/1587 (12 skip, 0 fail). Lint clean. Lesson queued: hypothesis-space bias — a debt narrative naming a specific source file as the spike target frames the investigation, even when the root cause lives elsewhere. Future call_indirect spikes should walk both sides of the ABI contract before fixating on emit. (`dec22c93`) `[x]`
- **9.9-h-3** — v128 multi-arg setter invoke entry helpers — **D-079 (i) discharged**. Three new void-result entry helpers in `src/engine/codegen/shared/entry.zig`: `callVoid_v128` (1 v128 arg → V0/XMM0), `callVoid_v128v128` (2 args → V0+V1 / XMM0+XMM1), `callVoid_v128v128v128v128` (4 args → V0..V3 / XMM0..XMM3). Same callconv(.c) shape as the existing v128-result helpers. `simd_assert_runner`'s void-result branch grows three dispatch arms (n_args=1/2/4 with v128 args). `scripts/regen_spec_simd_assert.sh` SUPPORTED set adds `((v128,), ())`, `((v128, v128), ())`, `((v128, v128, v128, v128), ())`; manifests regenerated — simd_const.388's setter line flips from `skip v128-param-pending` to `assert_return as-global.set_value_$g0_$g1_$g2_$g3 v128:01... v128:02... v128:03... v128:04... -> ()`. Mac aarch64 simd_assert: **11263 PASS / 6 FAIL → 11268 PASS / 2 FAIL** (+5 PASS / -4 FAIL; the 5th passed line is the setter itself becoming a passing assertion vs prior skip). Residual 2 fails are D-063 (`as-call_indirect-param()` / `-param2()` v128-via-call_indirect Trap; orthogonal to the globals path). OrbStack visible FAILs unchanged (D-078 a + c + D-077 panic). Mac unit tests: 1575/1587 (12 skip, 0 fail). Mac lint clean. Per ADR-0049 windowsmini reconciliation deferred to phase boundary. D-079 retargeted: (i) discharged, (ii) v128 cross-module imports flipped to `blocked-by: Phase 10+ cross-module import-aware chunk schedule`. (`d8c722d9`) `[x]`
- **9.9-h-2** — v128 globals JIT path per **ADR-0052** (per-valtype byte-offset storage). `compileWasm` now computes per-defined-global `globals_offsets: []u32` + `globals_valtypes: []ValType` (scalars 8-byte / v128 16-byte with 16-byte alignment) and threads them through `compile_func.compileOne` → `emit.compile` → `EmitCtx` on both arches. arm64 `op_globals.zig` exposes `emitGlobalGet/Set` that dispatch on valtype; v128 path uses `LDR Q [X23, #byte_off]` / `STR Q` (encLdrQImm / encStrQImm — imm12 scaled-by-16, max byte_off=65520). x86_64 `op_globals.zig` parallel: new `encMovupsXmmMemBaseDisp32` in `inst_sse.zig` emits `MOVUPS XMM, [RAX + disp32]` after `MOV RAX, [R15 + globals_base_off]` reload. Runner-side `applyDefinedGlobalsInit` re-parses the wasm bytes and writes init values into a caller-owned byte buffer at the computed offsets (parallel to `applyActiveDataSegments`); `simd_assert_runner.zig` switches `scratch_globals` from `[16]Value` to `[256]u8 align(16)` and calls the new helper after `applyActiveDataSegments`. **Verifiable shape change** on simd_const.388 Mac fails: `got v128:2e000000...` (garbage from prior default-arm i32 emit) → `got v128:00000000010000000200000003000000` (= init expr i32x4(0,1,2,3)) — the JIT load + init pipeline now operate correctly. Mac aarch64 simd_assert: 11263 PASS / 6 FAIL unchanged in count (4 residual `global.get_gN` fails are now cascading from a SKIPPED `(invoke "as-global.set_value_$g0_$g1_$g2_$g3" v128×4)` setter whose `((v128,v128,v128,v128), ())` shape lacks an entry helper — separate gap, tracked as new **D-079**). Mac unit tests: 1575/1587 (12 skip, 0 fail). Mac `test-all` green. OrbStack simd_assert unchanged in pre-existing FAILs (D-077 panic still suppresses tail). Per ADR-0049 windowsmini reconciliation deferred to phase boundary. D-078 (b) marked PARTIALLY DISCHARGED with the storage gap closed; residual sub-cluster reassigned to D-079. ADR-0052 implementation scope items: `Runtime.globals_byte_storage` + v128 import resolution intentionally NOT in this chunk — covered by D-079 follow-up since the immediate fixture flows through `simd_assert_runner`'s `JitRuntime`-direct path (no `setupRuntime` interaction) and has no cross-module imports. (`8250871d`) `[x]`
- **9.9-g-20** — parser init_expr walker per Wasm spec §3.3.2.10. Replaced 8 sites of naive `while body[pos] != 0x0B: pos+=1` scans across `decodeGlobals` / `decodeData` / `decodeElement` with a `scanInitExpr` helper that walks each constant-expression opcode and skips its immediate(s) by spec-defined width. Handles `t.const c` (i32/i64 sleb128 via `skipLeb128` continuation walk; f32/f64 fixed 4/8 bytes), `ref.null t`, `ref.func x`, `global.get x`, `v128.const` (FD prefix + 0x0C sub-op + 16 raw immediate bytes), and 0x0B terminator. Extended-const (i32/i64 add/sub/mul) intentionally not yet listed — one-line addition when Phase 10 lands. Per `.claude/rules/bug_fix_survey.md`, fix landed at all 8 sites in one chunk (not just the simd_const.388-visible site) since the same shape was silently fragile for any opcode immediate containing 0x0B (e.g. `i32.const 11` = `0x41 0x0B 0x0B` was previously misparsed too — undetected because no fixture exercised it). Mac aarch64 simd_assert: **11263 PASS / 3 FAIL** unchanged at chunk close (simd_const.388 compile flips PASS, exposing 4 downstream runtime `global.get_gN` mismatches — `2e000000...` returned instead of v128.const-initialised values; this is the pre-existing v128-globals runtime gap from D-078 (b), which the upstream BadValType was masking). Mac unit tests: 1575/1587 (12 skip, 0 fail; +1 new regression test). OrbStack test-all green. D-078 (b) body updated: the gap is structural on both arches (Mac compile path tolerated via fall-through, returning garbage at runtime — `global.set` v128 likely no-op; investigation deferred to dedicated chunk). (`efec8e37`) `[x]`
- **9.9-g-19** — ARM64 `i*x*.bitmask` (4 handlers) + new `extra_consts` infrastructure per **ADR-0051** (option (a): mirror x86_64 ADR-0042 `extra_consts` on ARM64 instead of inlining MOVZ/MOVK mask chains; **D-067 discharged**). EmitCtx gains `extra_consts: *std.ArrayList([16]u8)` + `simd_consts_base: u32`; close-time const-pool flush concatenates `func.simd_consts` (lower-time) + `extra_consts` (emit-time) into one flat 16-byte-aligned pool addressed by global `const_idx`. 8 new clang-as-verified NEON encoders in `inst_neon.zig` (`encSshrV16B/8H/4S`, `encAddvB16B/H8H/S4S`, `encZip1V16B`, `encExtV16B`) + 1 new scalar encoder `encLsrImmX`. 4 bitmask emit handlers in `op_simd.zig`: i8x16 uses SSHR.16B #7 + per-shape mask via `extra_consts` LDR-Q-literal + AND + EXT #8 (halve swap) + ZIP1 + ADDV.8H + UMOV.H[0]; i16x8 / i32x4 skip the EXT+ZIP1 fold (direct ADDV.8H / .4S since lane mask bytes fit the H/S accumulator without overflow); i64x2 takes the scalar UMOV.D + LSR #63 + ADD + ORR path since NEON has no .2D ADDV. Validator routes 100/132/164/196 to `opSimdAllTrueOrAnyTrue` (same 1-pop-v128 / 1-push-i32 shape); lower wires 4 ZirOps. Mac aarch64 simd_assert: **11263 PASS / 4 FAIL → 11263 PASS / 3 FAIL** (-1 = simd_boolean.0 compile flipped from `StackUnderflow` to PASS; assertions remain SKIP via `v128-param-pending` until D-070's runner dispatch lands). OrbStack: **3 → 2** visible FAILs before pre-existing D-077 runner panic. Lint clean. Edge-case rationale recorded at `private/notes/p9-edge-case-rationale.md` (gitignored) per `.claude/rules/edge_case_testing.md` "When NOT to add a fixture" — encoder tests + spec-testsuite compile pass cover regression detection without an internal fixture; promote on future regression. (`e586e159`) `[x]`
- **9.9-g-18** — x86_64 explicit-`return` v128 marshal arm — closes the gap between the function-level `end` v128 path (already wired §9.9-b at emit.zig:1545) and the explicit `return` opcode handler at emit.zig:1445 which previously surfaced `UnsupportedOp[return-v128]`. Mirror of `arm64/emit.zig:1150`; same `resolveXmm` + MOVAPS to `abi.return_xmm` shape. Removed `rejectUnsupported` helper (no remaining callers; no_unused lint would have fired). simd_lane.140's func 1 has `(result v128) ... return` which previously failed compile on x86_64 only. **OrbStack simd_assert FAIL: 4 → 3** (-1 simd_lane.140 cleared). Mac aarch64 unchanged at 11263 PASS / 4 FAIL. Mac unit tests: 1564/1576 (12 skip, 0 fail). Lint: clean. OrbStack test-all: 28/28 succeeded; 1548/1576 unit tests (28 skip, 0 fail). Per ADR-0049 windowsmini deferred to phase boundary. Remaining 3 fails: simd_lane f64x2_extract_lane value mismatch (D-078 a), simd_boolean.0 StackUnderflow (D-067 bitmask validator-shape), simd_bitwise.17 compile UnsupportedOp (no debug print surfaces — D-078 c next). (`d623a55e`) `[x]`
- **9.9-g-17** — x86_64 `emitI8x16Shuffle` `dst==rhs` alias fix. Continuation of the D-066 mirror cluster — step 2's unconditional `MOVAPS dst, lhs` clobbered rhs when regalloc's LIFO slot-reuse aliased `dst == rhs`. Step 5's `MOVAPS t2, rhs` then read dst-after-PSHUFB (= zeros for shuffles selecting all from rhs lanes 16..31, e.g. v8x16_shuffle-2). The final POR(dst, t2) merged 0 with 0 → all-zero output. Fix: stash rhs through XMM7 when `dst == rhs` (regardless of dst==lhs); step 5 reads stashed copy. One alias unit test added. **OrbStack simd_assert FAIL: 9 → 4** (-5 of 6 simd_lane shuffle variants; remaining 1 simd_lane is f64x2_extract_lane — distinct value-mismatch bug, not an alias). Mac aarch64 unchanged at 11263 PASS / 4 FAIL. Mac unit tests: 1564/1576 (12 skip, 0 fail). OrbStack test-all: 28/28 succeeded; 1548/1576 unit tests (28 skip, 0 fail). Per ADR-0049 windowsmini deferred to phase boundary. Remaining x86_64 SIMD residuals (4 fails): simd_lane f64x2_extract_lane value mismatch + simd_lane.140 UnsupportedOp + simd_boolean.0 StackUnderflow + simd_bitwise.17 UnsupportedOp. (`83217ff5`) `[x]`
- **9.9-g-16** — x86_64 `emitI64x2Mul` `dst==rhs` alias fix — **D-071 part a discharged**. Same D-066 mirror class as 9.9-g-11..g-15. Step 9's `MOVAPS dst, lhs` (when `dst != lhs`) unconditionally overwrote dst regardless of whether dst aliased rhs. When regalloc placed `result_v` in the same physical XMM as `rhs_v` (with `dst != lhs`), step 10's `PMULUDQ dst, rhs` read dst-after-MOVAPS = lhs, degenerating to `a_lo*a_lo` instead of `a_lo*b_lo`. Step 11's `PADDQ dst, s1` then mixed in the (correct) cross terms, producing a value-shaped-but-wrong result. Symptom: `mul(1, 0xFFFFFFFFFFFFFFFF) → 0xFFFFFFFF_00000001` (= 1*1 + cross<<32) instead of `0xFFFFFFFFFFFFFFFF`. Fix: stash rhs through XMM7 when `dst != lhs && dst == rhs`; reads at steps 3, 4, and 10 use the stashed copy. One alias unit test added (`slot_ids = {0, 1, 1}` → result aliases rhs at XMM9). **OrbStack simd_assert FAIL: 35 → 9** (-26 = 27 simd_i64x2_arith fully cleared, modulo +1 simd_lane.140 re-appearing in breakdown — pre-existing UnsupportedOp masked in the prior chunk's measurement by D-077 deinit panic timing, not introduced here). Mac aarch64 unchanged at 11263 PASS / 4 FAIL. Mac unit tests: 1563/1575 (12 skip, 0 fail). OrbStack test-all: 28/28 succeeded; 1547/1575 unit tests (28 skip, 0 fail). Per ADR-0049 windowsmini deferred to phase boundary. **D-066 mirror cluster fully closed** (D-071 a/b/c-actual all discharged). Remaining x86_64 SIMD residuals: simd_lane misc ×6 + simd_lane.140 UnsupportedOp + D-067 (simd_boolean.0 + simd_bitwise.17) ×2 = **9 fails** (single root family probably; investigate next chunk). (`4dfe4b01`) `[x]`
- **9.9-g-15** — x86_64 `emitI8x16Popcnt` `dst==src` alias fix — **D-071 part b discharged**. Same D-066 mirror class. 1-pop op: src_v dies at popcnt's pop and the slot is the LIFO-top free slot, which regalloc hands back to result_v. Step 5's `MOVUPS dst_x, [RIP+LUT]` overwrites src before step 7's `MOVAPS t1, src_x` re-reads it; the low-nibble path then computes `LUT[LUT[low_nibble(src)]]` (LUT bytes are 0..4, all < 16) instead of `LUT[low_nibble(src)]`. Symptoms: popcnt(0x01) → 0 (LUT[LUT[0]]=0), popcnt(0xFF) → 5 (high path correct=4 + low LUT[LUT[4]]=LUT[1]=1), popcnt(0x80) → 2 (high path=1 + low LUT[LUT[1]]=1). Fix: stash src through XMM7 when `dst_x == src_x`. Two byte-level unit tests (alias + non-alias control). **OrbStack simd_assert FAIL: 61 → 35** (-26 = 25 simd_i8x16_arith2 + 1 simd_lane.140 dependent on popcnt). Mac aarch64 unchanged at 11263 PASS / 4 FAIL. Mac unit tests: 1562/1574 (12 skip, 0 fail). OrbStack test-all: 28/28 succeeded; 1546/1574 unit tests (28 skip, 0 fail). Per ADR-0049 windowsmini deferred to phase boundary. Remaining x86_64 SIMD residuals: D-071 (a) i64x2.mul ×27, simd_lane misc ×6, D-067 (simd_boolean.0 + simd_bitwise.17) ×2. (`2934783d`) `[x]`
- **9.9-g-14** — x86_64 `emitV128IntNe` `dst==rhs` alias fix — **D-071 part c-actual discharged**. The handler had been authored with the same MOVAPS-elide pattern as the cmp helpers but missed the D-066 stash for `dst != lhs && dst == rhs`. When regalloc's LIFO slot-reuse aliased the result with the rhs vreg, `MOVAPS dst, lhs` clobbered rhs before `PCMPEQ dst, rhs` read it; the comparison degenerated to `lhs ?== lhs` = all-ones, which the trailing PXOR-with-all-ones flipped to all-zeros — the exact `got 0x0000... expected 0xffff...` symptom seen in OrbStack `simd_i16x8_cmp` / `simd_i32x4_cmp` `ne` fixtures. Fix mirrors lines 1097-1108 of `emitV128IntCmpUnsigned`: stash rhs through XMM7 when the alias is detected. One byte-level alias unit test (`emitI16x8Ne` with `slot_ids = {0, 1, 1}`) added — fails on the bug, passes with the stash. **OrbStack simd_assert FAIL: 79 → 61** (-18 = 8 simd_i16x8_cmp + 8 simd_i32x4_cmp + 2 parsing-artefact wrapped lines that depended on those fails). Mac aarch64 unchanged at 11263 PASS / 4 FAIL (separate emit path). Mac unit tests: 1560/1572 (12 skip, 0 fail). Lint: clean. OrbStack `test-all`: 28/28 succeeded; 1544/1572 unit tests (28 skip, 0 fail). Per ADR-0049 windowsmini deferred to phase boundary. Remaining x86_64 SIMD residuals: D-071 (a) i64x2.mul ×27, D-071 (b) i8x16.popcnt ×25, simd_lane ×6, simd_bitwise.17 + simd_boolean.0 + simd_lane.140 (D-067) ×3. (`a1d799ce`) `[x]`
- **9.9-g-13** — x86_64 `emitV128IntCmpUnsigned` `dst==lhs` alias fix — D-071 part c, **preventive**. The `.ge / .le` tail computes `PCMPEQ dst, lhs_x` after `PMINU/PMAXU dst, rhs` overwrote dst; if regalloc's LIFO slot-reuse aliases `dst == lhs`, the tail reads dst-after-min/max (= self-equal = always all-ones) instead of original lhs. Pre-existing comment claiming "the tail still works because dst was the same as lhs before MOVAPS-elision" was wrong. Stash lhs through XMM7 (mutually exclusive with the existing `dst==rhs` stash since dst can't equal both unless lhs==rhs). Two byte-level alias unit tests (`emitI8x16GeU` / `emitI16x8LeU` with `slot_ids = {0, 1, 0}`) added — fail on the bug, pass with the stash. **OrbStack simd_assert_runner: 79 FAIL unchanged at chunk close** — no current fixture exercises the `ge_u/le_u + dst==lhs` alias. The 16 residual `simd_i*x*_cmp` fails the prior handover predicted this would clear were actually `i16x8.ne / i32x4.ne` family (separate recipe: PCMPEQ + PXOR-ones), not the alias case. D-071 part (c) discharged preventively (regression-detecting tests in place); the actual ne-family residual cluster retargeted under D-071 (c-actual) per the live evidence. Mac aarch64 unchanged at 11263 PASS / 4 FAIL. **Companion landing**: the drift between handover prediction and live evidence motivated the same-commit-batch landing of `.claude/rules/no_handover_predictions.md` + `scripts/p9_simd_status.sh` + `/continue` Resume Step 0.5b live-status check + lesson `2026-05-11-handover-prediction-vs-evidence.md` — handover.md no longer carries numeric predictions, live counts come from the status script. (`8e7501d4`) `[x]`
- **9.9-g-12** — x86_64 `emitV128IntCmpSigned` aliasing fix — D-071 part b. Mirror of 9.9-g-11's three-helper alias fix applied to the remaining x86_64 SIMD comparison helper that follows the same `MOVAPS dst, base; PCMPGT(dst, cmp)` shape. Stash cmp through XMM7 when `dst_x == cmp_x && dst_x != base_x`. Linux x86_64 simd_assert_runner (OrbStack): **114 → 79 FAIL** (-35) — cleared most simd_i*x*_cmp residuals (52→16). Mac aarch64 unchanged at 11263/4. Remaining 79 OrbStack residuals split off to D-071: i64x2.mul x86_64 PMULUDQ recipe ×27, i8x16.popcnt PSHUFB recipe ×25, IntCmp `dst==lhs` alias ×16, simd_lane×6, simd_bitwise.17 + simd_boolean.0 (D-067). (`1c4c3252`) `[x]`
- **9.9-g-11** — x86_64 SIMD binop/cmp aliasing fix — D-066 mirror. Three helpers `emitV128IntBinop` / `emitV128FpCmp` / `emitV128IntCmpUnsigned` had pre-existing "Aliasing dst == rhs is not handled" comments based on a regalloc assumption invalidated by ADR-0037 (LIFO free-pool reuses recently-freed slots; result_v can coincide with rhs_v). Same root cause as D-066 in ARM64. Fix: stash aliased operand through XMM7 (project-canonical SIMD scratch — `abi.zig:200` reserves it mirroring arm64's V31). Linux x86_64 simd_assert_runner (OrbStack): **~4468 → 114 FAIL** (-4354, -97%). Mac aarch64 unchanged at 11263/4 (Mac doesn't exercise x86_64 codegen). **Critical surfaced gap** (recorded in handover): `test-spec-simd` was NOT aggregated into `test-all` — the x86_64 simd_assert had been silently broken since Phase 9 opened. Aggregation pending residual 114 fails (D-071 — IntCmpSigned alias mirror + i64x2.mul + simd_lane + simd_bitwise.17). (`a96ee85b`) `[x]`
- **9.9-g-10** — corpus expansion + ARM64 int min/max/avgr_u (14 ops). (1) Adds `simd_boolean` + 4 `simd_i*x*_arith2` to NAMES (5 fixtures, 26 manifests total). (2) ARM64 wiring for `i*x*.{min_s,min_u,max_s,max_u}` for B/H/S shapes + `i*x*.avgr_u` for B/H — 14 ops total. NEON has no .2D form for these; Wasm spec correspondingly omits i64x2 min/max/avgr; URHADD (avgr_u) only exists for B/H per spec. 14 new clang-verified encoders in `inst_neon.zig` (encSmin/Umin/Smax/Umax/Urhadd × shape); 14 emit handlers reusing `emitV128Binop`; 14 dispatch arms in `arm64/emit.zig`; 14 sub-op→ZirOp arms in `lower.zig`. validator already routed via opSimdBinop (94..211 binop fallthrough); ZirOps existed since IR catalogue; x86_64 dispatch pre-existed since §9.7-au. Mac aarch64 simd_assert_runner: **10788 → 11263 PASS** (+475) / 3 → **4 FAIL** (+1 transient — simd_boolean.0 StackUnderflow, bitmask validator-shape gap; D-067 follow-up — needs `opSimdBitmask` 1-pop arm) / 2127 → 2476 SKIP (over 26 manifests). OrbStack green. `inst_neon.zig` LOC 2029 → 2076 (D-065's known §A2 violation grows by 14 encoder defs; ADR for source-split still pending). Residual 4 fails: simd_boolean.0 StackUnderflow + 2× simd_const call_indirect Trap (D-063) + simd_const.388 BadValType. (`93c16b70`) `[x]`
- **9.9-g-9** — D-066 discharge — `emitV128ReplaceLaneFp` aliasing fix. The naive `MOV result_v ← src_v; INS result_v.D[lane], V<new_lane>.D[0]` miscompiled when regalloc's LIFO slot-reuse assigned `result_v == new_lane_v`: in simd_lane.137's `extract_lane → replace_lane` chain on `(v128, v128) → v128`, the extracted-lane vreg dies at replace_lane and its V-reg is the LIFO-top free slot, then handed back to the new replace_lane result. The copy MOV erased new_lane_v's content; INS read zero. Fix: detect `src_v != result_v && new_lane_v == result_v` and stash new_lane_v through V31 (popcnt scratch — outside any popcnt sequence) before the copy. Mac aarch64 simd_assert: **10787 → 10788 PASS** (+1) / **3 FAIL** (-1; only D-063 ×2 + simd_const.388 BadValType remain). OrbStack green. Lesson `2026-05-11-regalloc-lifo-vreg-alias-inplace-modify.md` records the shape; same risk filed as **D-070** for `emitV128Bitselect` + `emitV128Select` (not currently exercised — bitselect's value-comparing assertions are SKIP'd as v128-param-pending). (`ec4eaeb0`) `[x]`
- **9.9-g-8** — ARM64 shr_s/shr_u (8 ops) via NEG-then-(U|S)SHL synthesis. 4 new SSHL encoders. Critical mask fix: Wasm spec takes shift amount mod element_width; NEON USHL/SSHL zeroes when |shift| >= element_width — the two diverge for amounts at or beyond lane size. Added explicit `MOVZ X16, #(lane-1); AND W17, W<amt>, W16` mask before NEG/DUP. Also retroactively fixed 9.9-g-7's shl handler (same gap, no test caught it because no fixture exercised shl with amount ≥ element_width). Both shl and shr now share `emitV128IntShift(lane_mask, is_64bit, is_shr, dup_enc, shift_enc)` helper. Added simd_bit_shift to corpus. Mac aarch64 simd_assert: **10727 → 10787 PASS** (+60) / 4 FAIL unchanged (D-063 ×2, simd_const.388, D-066). OrbStack green. Discharges D-069. (`810e0779`) `[x]`
- **9.9-g-7** — SIMD shift family — validator `opSimdShift` (pop i32, pop v128, push v128) + lower-side wiring for 12 sub-ops + ARM64 emit for shl (4 shapes via DUP+USHL recipe; shr_s/shr_u deferred — D-069 residual). 4 new encoders `encUshl{16B,8H,4S,2D}` (Advanced SIMD vector USHL — positive amount = shift left). **Critical off-by-one fix to 9.9-g-6 extend wiring**: per Wasm SIMD spec (BinarySIMD.md authoritative numbering), i16x8 extends are 0x87..0x8A (135..138), NOT 134..137 as the lower.zig comment misled. Same shift for i32x4 extends (167..170 not 166..169). The +24 PASS at 9.9-g-6 came from coincidence on inputs where the wrong wiring happened to match. Mac aarch64 simd_assert: **10499 → 10727 PASS** (+228) / 5 → **4 FAIL** (-1 — simd_int_to_int_extend.0 fully PASSes after the fix). OrbStack green. (`8c3450d2`) `[x]`
- **9.9-g-6** — wire 12 SIMD int extend sub-opcodes in lower.zig — 134..137 (i16x8.extend_*), 166..169 (i32x4.extend_*), 199..202 (i64x2.extend_*). ZirOps + per-arch emit dispatch pre-existed; only the lower-side mapping was missing. Added simd_int_to_int_extend to corpus. Mac aarch64 simd_assert: **10475 → 10499 PASS** (+24) / 4 → **5 FAIL** (+1 transient — simd_int_to_int_extend.0 module compile fails on shift sub-ops 138..140/171..173/203..205, validator-shape mismatch — D-069). OrbStack green. (`59c90d98`) `[x]`
- **9.9-g-5** — add `simd_load_extend` to the SIMD spec corpus — sub-ops 1..6 (`v128.load*x*_{s,u}`) fully wired since §9.9 / 9.9-d-3. Mac aarch64 simd_assert: **10391 → 10475 PASS** (+84) / 4 FAIL unchanged (D-063 ×2, simd_const.388, D-066). OrbStack green. `simd_int_to_int_extend` + `simd_boolean` deferred — they need extend (sub-ops 134..137/166..169/199..202) + shift (138..) + bitmask (100/132/164/196) emit handlers, all of which fall through `lower.emitPrefixFD`'s `else => NotImplemented` arm. The lower-side comment claiming 134..137 etc. are wired is misleading: no `<sub-op> => emit(.@"...")` entries exist in `emitPrefixFD`. The follow-up chunk that wires them (extend + shift family) will pick them up cheaply once landed. (`c96e2f88`) `[x]`
- **9.9-g-4** — wire 5 v128 splat emit handlers — `i{8x16,16x8,64x2}.splat` (DUP V.<T>, W/X — GPR-broadcast) + `f{32x4,64x2}.splat` (DUP V.<T>, V.<T>[0] — element form). 4 new encoders (`encDup{16B,8H}` GPR-broadcast + `encDup{4SFromS0,2DFromD0}` element form; encDup4S + encDupGen2D pre-existed). Shared `emitV128SplatFromGpr` + `emitV128SplatFromV` helpers in op_simd.zig (refactored `emitI32x4Splat` to use the GPR helper). 5 new dispatch arms in arm64/emit.zig. Mac aarch64 simd_assert: **10385 → 10391 PASS** (+6) / **5 → 4 FAIL** (simd_lane.138 flipped end-to-end with 6 splat-shape exports going through the new handlers + 9.9-g-3's reduction handlers). OrbStack green. Discharges D-068. Residual 4 fails: 2× simd_const call_indirect Trap (D-063), simd_const.388 BadValType, simd_lane f64x2_extract_lane mismatch (D-066). (`0a433672`) `[x]`
- **9.9-g-3** — wire 5 v128 reduction emit handlers — `v128.any_true` (UMAXV.16B + CMP/CSET) + `i{8x16,16x8,32x4}.all_true` (UMINV.{16B,8H,4S} + CMP/CSET, shared `emitV128ReduceWithEncoder` helper) + `i64x2.all_true` (NEON has no UMINV.2D, dedicated 6-instr GPR detour: 2× UMOV X,V.D[k] + 2× CMP/CSET + final AND). 4 new encoders (`encUmaxv16B`/`encUminv{16B,8H,4S}`); validator routes 99/131/163/195 to `opSimdAllTrueOrAnyTrue` (correct pop-v128/push-i32 shape; previously mistreated as binop). Lower wires 83/99/131/163/195 → ZirOps. Mac simd_assert unchanged at 10385/5/1925 because simd_lane.138 still blocks on the **splat-handlers gap** (only `i32x4.splat` dispatched in arm64/emit.zig — D-068 records the missing i8x16/i16x8/i64x2/f32x4/f64x2 splat handlers as the next chunk's scope). The 9.9-g-3 commit's value is **substrate**: when 9.9-g-4 lands the splat handlers, simd_lane.138 will need this commit's reduction handlers to flip end-to-end. OrbStack green. (`37abf772`) `[x]`
- **9.9-g-2** — scale SIMD spec corpus + wire 50 cmp sub-opcodes in lower.zig. (1) Added cmp + lane to NAMES (`simd_{i8x16,i16x8,i32x4,i64x2,f32x4,f64x2}_cmp` + `simd_lane` = 7 manifests). (2) lower.zig wired sub-ops 35..76 + 214..219 → ZirOps (50 dispatch arms; ZirOps + per-arch emit dispatch pre-existed). (3) validator added 214..219 (i64x2 cmp signed-only) to binop list. (4) simd_assert_runner manifest read limit 1<<18 → 1<<22 (simd_f32x4_cmp / simd_f64x2_cmp manifests exceed prior cap). Mac aarch64 simd_assert_runner: **3549 → 10385 PASS** (+6836; ×2.93) / 3 → 5 FAIL (+2; transient simd_lane gaps) / 1520 → 1925 SKIP. OrbStack test-all green. Residual 5 fails: 2× simd_const call_indirect Trap (D-063), simd_const.388 BadValType, simd_lane f64x2_extract_lane mismatch (D-066), simd_lane.138 UnsupportedOp (D-067 — v128.any_true + i*x*.all_true reduction handlers missing). 9.9-g-1 was D-063 spike: static analysis ruled out marshal stage-register conflict; remaining hypotheses (bounds/sig fixup target arithmetic, BLR target X17 upper-bit issue, frame-layout v128 alignment) recorded in D-063 row body. (`07d0f874`) `[x]`
- **9.9-f-8** — validator binop arm for `i64x2.mul` (sub-op 213) — the ARM64 emit handler `emitI64x2Mul` (8-instr GPR-detour: UMOV → MUL X → INS, scratch X16/X17, alias-safe per-lane order) + dispatch arm pre-existed from §9.5-c-vii-mul; the structural gap was the validator's 94..211 binop list not including 213. One-line fix in `validator.zig:dispatchPrefixFD`. Mac aarch64 simd_assert_runner: **3366 → 3549 PASS** (+183) / 5 → **3 FAIL** (-2) / 1703 → 1520 SKIP — `simd_i64x2_arith.0/.12` flip from compile:NotImplemented to PASS, unblocking ~183 cascaded assertions. OrbStack test-all green. Residual 3 fails: 2× simd_const call_indirect Trap (D-063), simd_const.388 BadValType (parse-side). Discharges D-064. The cranelift NEON-only synthesis (REV64 + MUL.4S + XTN + ADDP + SHLL + UMLAL — 7 instr, `OSS/wasmtime/cranelift/codegen/src/isa/aarch64/lower.isle:848-913`) recorded in commit body for Phase 8 revisit. (`44d051c8`) `[x]`
- **9.9-f-7** — wire ARM64 emit dispatch for the missing int unops surfaced by 9.9-f-6. New `emitI{8x16,16x8,32x4,64x2}{Abs,Neg}` + `emitI8x16Popcnt` handlers in `arm64/op_simd.zig` reusing the pre-existing `emitV128Unop` helper (originally introduced for f32x4 / f64x2 unops in §9.6 / 9.6-b — discovered the duplicate during refactor of `emitV128Not` and reused). 9 new NEON encoders in `inst_neon.zig` (`encAbs{16B,8H,4S,2D}` / `encNeg{16B,8H,4S,2D}` / `encCnt16B` — Advanced SIMD two-reg-misc, Q=1, opcode=01011 (ABS/NEG) or 00101 (CNT, byte-only); U=0 → ABS, U=1 → NEG; size[23:22] selects shape) verified via `clang -arch arm64`. 9 new dispatch arms in `arm64/emit.zig`. Pre-existing `emitV128Bitselect` SPILL-EXEMPT comment placement fixed to satisfy `spill_aware_check`'s per-line discipline. Mac aarch64 simd_assert_runner: **2893 → 3366 PASS** (+473) / 11 → 5 FAIL (-6) / 2176 → 1703 SKIP. Residual 5 fails: 2× `simd_i64x2_arith.0/.12 NotImplemented` (i64x2.mul sub-op 213 unwired in validator + needs multi-instr synthesis since NEON has no `MUL.2D` — 9.9-f-8 scope, recorded as D-064); 2× simd_const call_indirect Trap (D-063); simd_const.388 BadValType (parse-side gap). Tests: 1554/1566 Mac. `inst_neon.zig` LOC reached 2029 / cap 2000 — recorded as D-065 (mirror of D-057 for x86_64 `op_simd.zig`; same source-split-via-ADR discharge pattern). (`d37cdbb0`) `[x]`
- **9.9-f-6** — scaled corpus to 5 new arith fixtures (`simd_f64x2_arith`, `simd_i32x4_arith`, `simd_i16x8_arith`, `simd_i8x16_arith`, `simd_i64x2_arith`; ~7400 assertions); split validator's 94..211 prefix-FD range into per-op unop / binop arms (unops: 96/97/98 i8x16.{abs,neg,popcnt}; 124..127 extadd_pairwise; 128/129 i16x8.{abs,neg}; 134..137 i16x8.extend_*; 160/161 i32x4.{abs,neg}; 166..169 i32x4.extend_*; 192/193 i64x2.{abs,neg}; 199..202 i64x2.extend_*); wired 19 int-arith sub-opcodes in `lower.zig` (i8x16.{abs,neg,popcnt,add,sub} / i16x8.{abs,neg,add,sub,mul} / i32x4.{abs,neg,sub,mul} / i64x2.{abs,neg,add,sub,mul}). Mac aarch64 simd_assert_runner: **1628 → 2893 PASS** (+1265) / 4 → 11 FAIL (+7) / 908 → 2176 SKIP. New fails are 8× ARM64 emit-dispatch UnsupportedOp (existing helpers `emitI8x16Add/Sub`, `emitI16x8Add/Sub` not yet dispatched in arm64/emit.zig + `i*.{neg,abs,popcnt}` handlers not yet implemented) — 9.9-f-7 scope. Tests: 1552/1564 Mac, 1536/1564 OrbStack. (`d8d762b1`) `[x]`
- **9.9-f-5** — two structural fixes that close the validator + lower dispatch gap for f32x4 / f64x2 arith — emit handlers already landed in §9.6 / §9.7 cycles. (1) **Validator** (`validator.zig:dispatchPrefixFD`): split 224..255 prefix-FD sub-opcodes into per-op unop/binop arms. The 9.4 MVP routed all 224..255 through `opSimdBinop` (pop 2 / push 1), miscounting `f32x4.{abs,neg,sqrt}` (224, 225, 227) and `f64x2.{abs,neg,sqrt}` (236, 237, 239) as binops. New: 224 / 225 / 227 / 236 / 237 / 239 → `opSimdUnop`; 226 / 228..235 / 238 / 240..255 → `opSimdBinop`. Surfaced `simd_f32x4_arith.1.wasm` StackUnderflow. (2) **Lower** (`lower.zig`): wire 22 sub-opcodes — 224..235 (f32x4 abs/neg/sqrt + add/sub/mul/div + min/max/pmin/pmax) and 236..247 (f64x2 mirror). Mac aarch64 simd_assert_runner: **443 → 1628 PASS** (+1185) / 7 → 4 FAIL (-3) / 2093 → 908 SKIP. The simd_f32x4_arith fixture's 1819 assertions now mostly run through the JIT. Residual 4 fails: 2× simd_const call_indirect v128 Trap (D-063), simd_const.388 BadValType, simd_const.389 NotImplemented (separate validator/lower gaps). 3-host gate green at HEAD `47cf7d0f`. `[x]`
- **9.9-f-4** — two parts. (a) Scaling: new `entry.callV128_v128` (`(v128) → v128` unop shape — FP / int unop fixtures) + runner dispatch arm + `regen_spec_simd_assert.sh` SUPPORTED dict expansion. NAMES gains `simd_f32x4_arith` (1819 upstream assertions). (b) Defer call_indirect v128 Trap to debt: filed **D-063** for the `simd_const.386` `as-call_indirect-param()` / `-param2()` runtime Trap. Direct `call $f` PASSES same module same callees, isolating to call_indirect-specific work; marshal looks identical, setupRuntime element-segment is v128-blind, table_size + func_typeidx correct. Investigation deferred — broader scaling has higher leverage. Mac aarch64 simd_assert_runner: **412 → 443 PASS** (+31) / 4 → 7 FAIL (+3). New fails: simd_f32x4_arith.0 NotImplemented (lower-side missing FP arith opcodes 228..231 / 240..243), simd_f32x4_arith.1 StackUnderflow (validator approximates 224..255 as binop; FP unop arms need split — 9.9-f-5 scope), simd_f32x4_arith.18 NotImplemented (same family). (`cf5f78f9`) `[x]`
- **9.9-f-3** — three structural ARM64 fixes to unblock simd_const.386 end-to-end. (1) `arm64/op_control.zig:emitEndIntra` v128 merge MOV — the existing scalar D-038 spill-aware path emitted `ORR W d, WZR, W s` (32-bit) for every result slot regardless of type, silently truncating v128 merges. Now per-slot dispatch on `alloc.shapeTag(merge_vreg)`: v128 → q* helpers + `encMovV16B`, scalar → existing GPR helpers. (2) `arm64/op_call.zig:marshalCallArgs` v128 caller-side marshal per AAPCS64 §6.4 SIMD calling convention — V0..V7 + 16-byte-aligned stack overflow per §6.4.2 stage C.4 (consume 2 of 8-byte slots per overflow v128). Mirror of 9.9-e-1 callee-side. (3) `captureCallResult` v128 — callee returns v128 in V0; mirror of f64 capture using q* helpers + `encMovV16B` (in-reg) / `encStrQImm` (spill, 16-byte aligned). Mac aarch64 simd_assert_runner: **394 → 412 PASS** (+18) / 3 → 4 FAIL (+1). New fail is `as-call_indirect-param()` / `-param2()` Trap (call_indirect with v128 arg compiles past marshal but traps at runtime — likely sig-typeidx comparison or table-entry-typeidx setup gap; 9.9-f-4 scope). simd_const.386 now compiles + executes end-to-end. windowsmini gate green at HEAD `80b2f1c5`. `[x]`
- **9.9-f-2** — two-line fix to `validator.readBlockType` + `lower.readBlockArity`: accept -5 (0x7B) as v128 single-valtype block result per Wasm spec §5.3.5. The -1..-4 switch covered i32/i64/f32/f64; v128 was missing, surfacing as `Error.BadBlockType` for any `(block (result v128) ...)` construct. Mac aarch64 simd_assert_runner: **381 → 394 PASS** (+13) / 4 → 3 FAIL (-1). simd_bitwise.17 unblocked end-to-end (`v128.{not, and, or, xor, andnot, bitselect}-in-block` exports). simd_const.386 moved past validator into emit-side UnsupportedOp — `arm64/op_control.zig:emitEndIntra` merge MOV uses GPR helpers (works for scalar block results; v128 needs `qLoadSpilled` / `encMovV16B`). 9.9-f-3 scope. (`ded3e237`) `[x]`
- **9.9-f-1** — first scaling chunk of §9.9-f. Adds simd_bitwise to NAMES; wires (v128,v128)→v128 entry helper `entry.callV128_v128v128` + runner dispatch arm; splits validator's prefix-FD 35..82 binop range — 77 (`v128.not`) is unop, 78..81 binop, 82 (`v128.bitselect`) new `opSimdBitselect` 3-pop helper. Lower-side wires 78..82 (was else=>NotImplemented). ARM64 NEON emit handlers for `emitV128{And,Or,Xor,Andnot,Not,Bitselect}` in op_simd.zig + dispatch in arm64/emit.zig. 4 new encoders in inst_neon.zig (`encAnd16B` 0x4E201C00, `encBic16B` 0x4E601C00, `encEor16B` 0x6E201C00, `encMvn16B` 0x6E205800) clang-as verified via 4 unit tests; `encOrrV16B` + `encBsl16B` reused. Mac aarch64 simd_assert_runner: **257 → 381 PASS** (+124) / 3 → 4 FAIL / 295 → 338 SKIP. Tests 1548 → 1552. Residual 4 fails: simd_bitwise.17 + simd_const.386 BadBlockType (block-result v128 type-decoder gap), simd_const.388 BadValType, simd_const.389 NotImplemented — separate validator/lower gaps. (`3abfb890`) `[x]`
- **9.9-d-7** — two runner-side fixes investigated under "residual 21 value-mismatch + 3 simd_align ExportNotFound" charter. (1) `runner.applyActiveDataSegments` (new pub helper) mirrors `setupRuntime`'s data-init half without paying the per-module allocation; called from `simd_assert_runner.zig` after the `@memset(scratch_memory, 0)` baseline so v128.load fixtures see the fixture-declared data-segment bytes. Unblocks 21 simd_address value-mismatches. (2) `regen_spec_simd_assert.sh` detects space-containing export-name fields (e.g. simd_align's `v128.load align=16`) and emits `skip export-name-has-spaces '<field>'` instead of `assert_return`; re-baked `simd_align/manifest.txt` flips 3 assert_returns from FAIL to SKIP. The runner-format extension to handle quoted names is tracked separately. Mac aarch64 simd_assert_runner: **227 → 257 PASS** / **36 → 3 FAIL** / 292 → 295 SKIP. Remaining 3 fails are simd_const.386 BadBlockType + .388 BadValType + .389 NotImplemented (validator/lower gaps, not v128-codegen). (`d8fb4939`) `[x]`
- **9.9-e-2** — x86_64 mirror of 9.9-e-1 per `p9-9.9-e-survey.md` strategy C: new `LocalLayout` helper in `x86_64/emit.zig` (RBP-negative disps, scalars 8-byte stride low region, v128 16-byte stride high region; v128 disps point to the most-negative byte of each 16-byte slot since `MOVUPS [RBP+disp]` writes upward). v128 PARAM marshal SystemV-only — XMM0..XMM7 → `MOVUPS [RBP+disp_v128], XMM<n>` via new `rbpStoreXmmV128` auto-helper; Win64 v128 stays UnsupportedOp (Microsoft x64 ABI passes v128 by hidden pointer); SysV stack-arg overflow (fp_arg_idx ≥ 8) UnsupportedOp pending follow-up. v128 local.get / local.set / local.tee via xmmDefSpilled / xmmLoadSpilled + new MOVUPS RBP-disp encoders. Zero-init for declared v128 locals via two `MOV [RBP+disp], RAX` (RAX zeroed via XOR EAX, EAX). 4 new encoders in `inst_sse.zig` (`encStoreXmmV128MemRBP[Disp32]` / `encLoadXmmV128MemRBP[Disp32]`, opcode 0x0F 0x10/0x11 no prefix, REX.R for xmm ≥ 8) verified against clang-as via 5 unit tests; re-exported in `inst.zig`. The pre-existing v128-param `expectError(UnsupportedOp)` test at `emit_test_int.zig:860` flips to `try compile()` for SysV; Win64 retains the rejection. Mac aarch64 1548/1560 (+5 encoder tests vs 9.9-e-1's 1543/1555); OrbStack 1532/1560. simd_assert_runner unchanged at 227/36/292 (Mac aarch64 doesn't directly exercise x86_64 codegen). (`6de58406`) `[x]`
- **9.9-e-1** — ARM64 v128 frame layout + param marshal + local.get/set/tee handlers per ADR-0046 + p9-9.9-e-survey.md strategy C. New `LocalLayout` helper in `arm64/emit.zig`: scalars at 8-byte stride low region, v128 at 16-byte stride high region (rounded up to 16-byte alignment); `local_base_off` rounded to 16 when v128 locals exist so absolute SP-relative v128 slots satisfy `encStrQImm` / `encLdrQImm` imm12 alignment. v128 param marshal per AAPCS64 §6.4 + §6.4.2 stage C.4 overflow (16-byte align next stack-arg slot, consume 2 of 8-byte slots per overflow v128). v128 local.get / local.set / local.tee via `qDefSpilled` / `qLoadSpilled` + Q-form encoders. Zero-init for declared v128 locals via two `STR XZR` per slot. Mac aarch64 simd_assert_runner: 226 → **227 PASS** / 36 → 36 FAIL / 296 → 292 SKIP. The 3 compile UnsupportedOps from 9.9-d-5/-6 (simd_select.0 + simd_const.387 + simd_align.90) all moved to PASS or runner-stage gaps (simd_align ExportNotFound is runner-side mapping). x86_64 mirror is 9.9-e-2's scope. Edge-case rationale at `private/notes/p9-edge-case-rationale.md` (spec testsuite covers the boundary post-flip; edge-case runner doesn't yet handle v128 results). (`a111f1c0`) `[x]`
- **9.9-l-1b-d093-d85** — Wasm §3.3.5 polymorphic-stack type-checking tightening: **+14 PASS**. Two structural fixes to the polymorphic-stack (post-unreachable / br / return) validation, draining all 14 SKIP-VALIDATOR-GAP entries in the unreached-invalid corpus. (a) `expectFrameEndTypes`: pre-d-85 form bailed out of type-checks entirely when `frame.unreachable_flag` was set. Spec §3.3.5 says missing values synthesise as `.bot`, but PRESENT values must still type-check against the expected slot; excess values are an unconsumed-result error even in unreachable code. New impl: `have` = operand_len - frame.height; reject `have > expected_len`; for single/multi result types, walk present values against `expected[(expected_len - have) + i]` (top-of-tuple-mapping per polymorphic spec). Drains unreached-invalid.{5,18,20,22,28,30,32,40,42,44,82,115}. (b) `opBrTable`: complementary changes — (i) enforce label-type ARITY equality across targets regardless of polymorphic flag (different arities cannot unify via `.bot` — drains unreached-invalid.86 with `block (result f32)` + outer `block (void)` targets); (ii) always call `popLabelTypes(first)` — pre-d-85 skipped the pop in polymorphic mode, leaving post-unreachable concrete pushes unchecked (drains unreached-invalid.85 = `block (result i32); unreachable; f32.const 0; i32.const 1; br_table 0` where the f32 must reject against the inner block's i32 result type); (iii) numeric-type-equality across targets still gated on `!polymorphic` so the `meet-bottom` fixture in unreached-valid.wast (block f32 vs block f64 unifying via `.bot`) remains accepted. **Outcome**: spec_assert non-simd 23987/0/2083 → **24001/0/2069** **Mac + OrbStack bit-identical** (D-134 silent run #2 consecutive — heisenbug discharge candidate after 3+ more silent runs) (+14 PASS, 0 FAIL; skip-impl 1587→1573). Per-corpus full drain: unreached-invalid 14→0. **Total SKIP-VALIDATOR-GAP 14→0 — VALIDATOR-GAP corpus FULLY DRAINED**. Both VALIDATOR-GAP and PARSER-GAP corpora now zero; remaining skip-impl 1573 is PURELY structural (Phase 10+ cross-module imports + Phase 11+ multi-result family). Cumulative d-74→d-85 (13 chunks): **+217 PASS** (23784 → 24001). (`48632565`) `[x]`
- **9.9-l-1b-d093-d84** — PARSER-GAP full drain (19→0): canonical-encoding + section-consistency tightening: **+19 PASS**. Five bundled spec-rule tightenings: (a) `compileWasm` eagerly decodes the type section when present (catches canonical-LEB issues in modules without imports/funcs; drains binary-leb128.{31,32,56,57,90} + binary.74); (b) function-vs-code consistency mirror in the empty-fn early-return path rejects "code section non-empty with no function section" (binary.57); (c) new `Error.DataCountMismatch` enforces data_count section's LEB value against data section's entry count (binary.{62,63,64}); (d) new `Validator.data_count_section_present: bool` field threaded through `WithMemory`: opMemoryInit / opDataDrop reject (UnknownMemory) when the section is absent but the opcodes appear in any body (binary.{66,67}); (e) decodeTables rejects flag bytes outside {0, 1} (binary.87 = flag 0x08); (f) parser validates each custom section's name-prefix LEB at id==0 handling — over-long / overflow / missing-name-LEB all rejected via `leb128.Error` propagation (custom.{4,5} + binary-leb128.{30,55}). **Outcome**: spec_assert non-simd 23968/0/2102 → **23987/0/2083** **Mac + OrbStack bit-identical** (+19 PASS, 0 FAIL; skip-impl 1606→1587). Full per-corpus drain: binary 8→0, binary-leb128 7→0, custom 4→0. Total SKIP-PARSER-GAP 19→0. Updated `parse: custom sections allowed anywhere` test fixture to use valid name-LEB-prefixed bodies. **First D-134-silent OrbStack run since d-65** (heisenbug instrumentation from d-72 still in place; not yet conclusive). Cumulative d-74→d-84 (12 chunks): **+203 PASS** (23784 → 23987). (`978b3e01`) `[x]`
- **9.9-l-1b-d093-d83** — Wasm §3.4.6 elem-funcidx-range + §3.3.5.20 table.init reftype-match: **+2 PASS**. (a) Runner walks every elem segment's `funcidxs` slice; non-maxInt entries must satisfy `fidx < total_funcs` (computed early as `num_func_imports + defined_funcs_count` so the check fires before the function-section-presence branch — call_indirect.36.wasm has 1 table + 1 active elem with 2 funcidx=0 entries but zero functions). (b) Validator gains `elem_types: []const ValType` field (parallel to elem_count); opTableInit compares `elem_types[elemidx]` against `tables[tableidx].elem_type` and returns `StackTypeMismatch` on disagreement (drains elem.63.wasm = `(table $t 1 externref) (elem $e funcref) (func (table.init $t $e ...)`). **Outcome**: spec_assert non-simd 23966/0/2104 → **23968/0/2102** (+2 PASS, 0 FAIL; skip-impl 1608→1606). Per-corpus full drains: call_indirect 1→0, elem 1→0. Total VALIDATOR-GAP 16→14 — all remaining are the deferred unreached-invalid (14) family requiring a polymorphic-stack pop-with-expected-type refactor (~200 LOC; complexity-to-drainage poor; deferred to substrate audit). Cumulative d-74→d-83 (11 chunks): **+184 PASS** (23784 → 23968). OrbStack D-134 flake (orthogonal). (`cedb2244`) `[x]`
- **9.9-l-1b-d093-d82** — Wasm §3.4.7.3 / §3.4.10 ref.func declared-set validation: **+3 PASS**. (a) New `Validator.declared_funcs: []const bool` field; `opRefFunc` rejects funcidxs absent from the declared set (= globals' ref.func init-exprs + elements' funcidxs + exports' func entries; explicitly EXCLUDING start function and function bodies, per spec interpreter `valid/valid.ml` line 711 `refs = Free.module_ ({m.it with funcs = []; start = None})`). (b) `validateGlobalInitExpr` grows a `total_funcs` param + range-checks `ref.func N` (drains `ref_func.2.wasm` = `(global funcref (ref.func 7))` with only 2 imports). (c) New helper `initExprRefFunc` extracts the funcidx from a `ref.func N; end` init-expr for declared-set seeding. New Error variant `UndeclaredFuncRef`. **Outcome**: spec_assert 23963/0/2107 → **23966/0/2104** (+3 PASS, 0 FAIL; skip-impl 1611→1608). Per-corpus full drain: ref_func 3→0. Total VALIDATOR-GAP 19→16. Cumulative d-74→d-82 (10 chunks): **+182 PASS** (23784 → 23966). OrbStack D-134 flake (orthogonal). (`d5e7eee5`) `[x]`
- **9.9-l-1b-d093-d81** — Wasm §3.3.2.2 select-reftype + §3.3.5 if-missing-else validation: **+5 PASS**. (a) untyped `select` (0x1B) requires numeric operands (reftypes need `select_typed` 0x1C); (b) `if` block with implicit empty else requires `start_type == end_type` (impossible for `if (result T)` without explicit else). **Outcome**: spec_assert 23958/0/2112 → **23963/0/2107** (+5 PASS, 0 FAIL; skip-impl 1616→1611). Per-corpus full drains: select 1→0, if 4→0. Total VALIDATOR-GAP 24→19. Cumulative d-74→d-81 (9 chunks): **+179 PASS** (23784 → 23963). OrbStack D-134 flake (orthogonal). (`ca2f5621`) `[x]`
- **9.9-l-1b-d093-d80** — Wasm §3.3.5.6 + §3.4.6 reftype-matching validation: **+2 PASS**. (a) call_indirect's referenced table must have reftype `funcref` (not `externref`); (b) active elem segment's `elem_type` must match referenced table's `elem_type`. New Error variant `ElemSegmentTypeMismatch`. **Outcome**: spec_assert 23956/0/2114 → **23958/0/2112** (+2 PASS, 0 FAIL; skip-impl 1618→1616 = −2). Per-corpus drop: call_indirect 2→1, elem 2→1. Total VALIDATOR-GAP 26→24. **Mac + OrbStack bit-identical**. Cumulative d-74→d-80 (7 chunks): **+174 PASS** (23784 → 23958). (`a6b91720`) `[x]`
- **9.9-l-1b-d093-d79** — Wasm §3.4.4 / §3.3.5 memory-op validation in function bodies: **+8 PASS**. New `Validator.memory_count` field (default=1 for legacy callers) + `Error.UnknownMemory`. opLoad / opStore / opMemorySize / opMemoryGrow / opMemoryFill / opMemoryCopy / opMemoryInit all guard `memory_count == 0`. New wrapper `validateFunctionAndCollectSelectTypesWithMemory` for production callers; legacy callers (tests, wast_runner) keep default. `compileWasm` computes total_memories (imports + defined) and threads it. **Outcome**: spec_assert 23948/0/2122 → **23956/0/2114** (+8 PASS, 0 FAIL; skip-impl 1626→1618 = −8). Per-corpus full drains: memory 6→0, memory_fill 1→0, memory_copy + memory_init residuals→0. Total VALIDATOR-GAP 32→26. Cumulative d-74→d-79 (6 chunks): **+172 PASS** (23784 → 23956). OrbStack D-134 flake hit (orthogonal). (`14bf705a`) `[x]`
- **9.9-l-1b-d093-d78** — Wasm §3.4.6 / §3.4.7 active offset-expression validation: **+25 PASS**. Reuse `validateGlobalInitExpr` const-expr helper (now generic) with `want_valtype = .i32` to validate offset_expr of active elem + data segments. Two new validation blocks per compileWasm path (main + empty-fn early-return), since most active-segment invalid modules have sections-only. **Outcome**: spec_assert non-simd 23923/0/2147 → **23948/0/2122** (+25 PASS, 0 FAIL; skip-impl 1651→1626 = −25). Per-corpus drop: **data 12→0 full drain**, elem 14→2 (residual = per-element init-expr reftype-matching). Mac + OrbStack **bit-identical** (OrbStack `test-all` exit 0). Cumulative d-74→d-78 (5 chunks): **+164 PASS** (23784 → 23948). (`71373801`) `[x]`
- **9.9-l-1b-d093-d77** — Wasm §3.4.3 / §3.3.2 global init-expression validation: **+34 PASS** (`global` corpus 17→0 full drain). New `validateGlobalInitExpr` helper checks: (a) first opcode is const (t.const / ref.null / ref.func / v128.const / global.get-of-immutable-import); (b) producer type matches declared valtype; (c) single trailing `end`. Called in both empty-fn early-return path (where most `global.wast` invalid modules land) and main path. **Mid-cycle correction**: first attempt missed `0xFD` SIMD prefix → `simd_const.388` / `simd_lane.140` regressed; added v128.const branch. **Outcome**: spec_assert non-simd 23889/0/2181 → **23923/0/2147** (+34 PASS, 0 FAIL; skip-impl 1685→1651 = −34); simd 13301/0/440 unchanged (post-fix). Cumulative d-74→d-77: **+139 PASS** (23784 → 23923). OrbStack D-134 flake hit (orthogonal). (`a50ad8c9`) `[x]`
- **9.9-l-1b-d093-d76** — Wasm §3.4.5 table + §3.4.6 elem + §3.4.8 start + §3.4.9 import-typeidx validation: **+26 PASS**. Four spec-rule validators bundled in `compileWasm`: (a) §3.4.5 table limits `max ≥ min`; (b) §3.4.6 active elem segments' tableidx ∈ [0, total_tables); (c) §3.4.9 function import typeidx < types.len; (d) §3.4.8 start funcidx in range AND signature `[] → []`. New Error variants: `InvalidTableLimit`, `ElemSegmentRequiresTable`, `ImportTypeIdxOutOfRange`, `InvalidStartFunction`. **Outcome**: spec_assert non-simd 23863/0/2207 → **23889/0/2181** (+26 PASS, 0 FAIL; skip-impl 1711→1685 = −26). Per-corpus SKIP-VALIDATOR-GAP: table 4→0, start 3→0, imports 3→0, func_ptrs 5→0 (full drain × 4 corpora); elem 24→14, memory 6→5. Total VALIDATOR-GAP 89→68. Mac + OrbStack **bit-identical** (OrbStack test-all exit 0 — D-134 didn't trigger this run; cumulative since d-74 +105 PASS). (`d9e858a8`) `[x]`
- **9.9-l-1b-d093-d75** — Wasm spec §3.4.4 memory + §3.4.7 data validation: **+42 PASS**. Three spec-rule checks in `compileWasm` (single block early in the function, covering both empty-fn and main paths): (a) §3.4.4 at most 1 memory in Wasm 2.0 (imports + defined ≤ 1, else `MultipleMemories`); (b) §3.4.4 per-memory limits — `min ≤ 65536`, `max ≤ 65536` if specified, `max ≥ min` if both (else `InvalidMemoryLimit`); (c) §3.4.7 active data segments require a memory to exist with memidx in range (else `DataSegmentRequiresMemory`). **Outcome**: spec_assert non-simd 23821/0/2249 → **23863/0/2207** (+42 PASS, 0 FAIL; skip-impl 1753 → 1711 = −42). Per-corpus SKIP-VALIDATOR-GAP drop: memory 18→6, data 22→12; total VALIDATOR-GAP 123→89. Mac aarch64 `zig build test` rc=0 ✓; `test-spec-wasm-2.0-assert` green. OrbStack `test-all` SEGV in `zwasm-spec-wasm-2-0-assert` — known D-134 flake (validator addition orthogonal to signal handling). (`9701400a`) `[x]`
- **9.9-l-1b-d093-d74** — Wasm spec §3.4.10 export validation: **+37 PASS**. Drain `exports.wast` SKIP-VALIDATOR-GAP family by hardening `compileWasm`. Two rules per §3.4.10: (a) each export's idx must reference a defined entity (funcidx<total_funcs, tableidx<total_tables, memidx<total_memories, globalidx<total_globals); (b) export names pairwise distinct (no duplicates). Pre-d-74 `compileWasm` decoded the export section only on demand via `findExportFunc`; assert_invalid modules with structurally invalid exports were accepted and surfaced as SKIP-VALIDATOR-GAP. **Implementation**: `Error.ExportIdxOutOfRange` + `Error.DuplicateExport` added to `Error` set; validation block in `compileWasm`'s main path (after sections decoded) counts imports per kind + defined section entries, decodes export section, per-export idx range check + per-name duplicate check via `std.StringHashMap`; mirror block in the empty-fn early-return path since most exports.wast assert_invalid modules lack a function section. **Outcome**: spec_assert non-simd 23784/0/2286 → **23821/0/2249** (+37 PASS, 0 FAIL; skip-impl 1790 → 1753 = −37). Per-corpus SKIP-VALIDATOR-GAP drop: exports 31→0, global 18→17, unreached-invalid 14→13; total VALIDATOR-GAP 154→123. Mac aarch64 `zig build test` + `test-spec-wasm-2.0-assert` green. OrbStack `test-all` SEGV in `zwasm-spec-wasm-2-0-assert` — known **D-134 flake** (orthogonal to validator addition; no signal-handling impact). (`c2c636a5`) `[x]`
- **9.9-l-1b-d093-d73** — debt cleanup: 24 stale `discharged-by:` rows pruned from active section of `.dev/debt.md` per Step 0.5 "when discharged, delete the row". A bulk audit at d-73 resume surfaced D-101 / D-107 / D-108 / D-109 / D-110 / D-111 / D-112 / D-113 / D-114 / D-115 / D-116 / D-118 / D-119 / D-120 / D-121 / D-122 / D-123 / D-124 / D-125 / D-127 / D-128 / D-129 / D-130 (+ 1 straggler) all with `Status: discharged-by: <chunk>` that were never deleted at discharge time. The git log already records each via the named chunk commit; the row was redundant. Active D-rows 52 → 28. Recently-discharged section (58 rows) untouched per the "one-cycle backreference" discipline. `zig build` rc=0 (doc-only). (`5e52bed6`) `[x]`
- **9.9-l-1b-d093-d72** — D-134 probe: surface sigaltstack errors + sigaction readback (OrbStack PASSED this run, no diagnostic prints). Replace `installSigsegvHandler`'s `std.posix.sigaltstack(...) catch {}` silent swallow with `catch |err| std.debug.print("...", .{err})`; add `sigaction(.SEGV, null, &oact)` readback that prints when `oact.handler.handler != sigsegvHandler`. Both prints fire ONLY on failure (happy-path runs stay quiet). **OrbStack result**: `zig build test-all` exit 0; no `installSigsegvHandler:` errors; spec_assert_runner_non_simd 23784/0/2286 bit-identical Mac aarch64. **Hypothesis space update**: (iii-a) sigaltstack silent failure and (iii-c) handler replaced at install time both **mostly ruled out** for the happy-install path on this run. Caveat: this run passes (no SEGV), so the diagnostic stayed silent — D-134 is now an "instrumented flake" awaiting evidence on its next failing run. **Heisenbug context**: d-71 reproduced SEGV; d-72 passes; the d-72 source change is compile-time only (errdefer expansion + conditional print) but adds bytes to the binary, plausibly perturbing layout in a way that masks the trigger this run. The autonomous loop should pivot to other work — proactively probing more D-134 hypotheses without a failing run to anchor on is wheel-spinning. Mac aarch64 `zig build test` + `test-spec-wasm-2.0-assert` green. (`0e340f0e`) `[x]`
- **9.9-l-1b-d093-d71** — D-134 disambiguation probe: distinct unarmed-SEGV exit code (142, not 139). Change `sigsegvHandler`'s unarmed branch from `_exit(139)` (= 128 + SIGSEGV; ambiguous with kernel-delivered raw SIGSEGV — `zig build` reports both as "signal SEGV") to `_exit(142)` (uncommon, unambiguous). **OrbStack result**: `zig build test-all` reproduced D-134 SEGV and reported `signal SEGV` — NOT `exit code 142`. **Conclusion**: our handler does NOT fire for the real SEGV. With d-68's Zig-startup-handler-disable in place and zero other stdlib `sigaction(.SEGV)` callers (verified via repo grep), hypothesis (ii) "competing Zig handler" is fully refuted; **hypothesis (iii)** "handler not active at signal-delivery time" is now the leading candidate, with sub-cases (iii-a) `sigaltstack` silent failure (the `catch {}` at `installSigsegvHandler:1143` swallows errors), (iii-b) signal mask blocks SIGSEGV at delivery, (iii-c) handler replaced by unenumerated path. **No behavior change** on happy path; on D-134 failing runs, this provides the disambiguation evidence. d-72 will probe (iii-a) by surfacing sigaltstack errors instead of swallowing. Mac aarch64 `zig build test` + `test-spec-wasm-2.0-assert` green (23784/0/2286). (`e9f36b5d`) `[x]`
- **9.9-l-1b-d093-d70** — debt cleanup: D-093 + D-097 + D-099 deleted per Step 0.5 "when discharged, delete the row". D-093 (`wasm-2.0 spec corpus failures surfaced by k-1-expand-2 bisection`): all 4 named clusters (a-d) discharged through d-1..d-69. D-097 + D-099 had `discharged-by:` status (stale). D-095 narrative tweaked. Handover trimmed 272→85 lines per Step 7 cap. Doc-only commit (`ccf732ef` + `66e629c7`). `[x]`
- **9.9-l-1b-d093-d69** — **D-135 DISCHARGED**: missing errdefers in `compileWasm` empty-fn-section path. **Bug**: the d-52 / D-127 empty-fn early-return path (`src/engine/runner.zig:146-194`) allocated 6 separate allocations (`empty_results`, `empty_module` via `linker.link`, `sigs`, `typeidxs`, `empty_global_offsets`, `empty_global_valtypes`) but only `arena` was errdefer-covered. Two downstream `Error.MissingTypeSection` returns leaked these: (1) line 165 `module.find(.type) orelse return` (import-only module with no type section) leaked all 6; (2) line 172-176 `tidx >= types.items.len` check (out-of-range typeidx) manually freed `sigs`+`typeidxs` but leaked the other 4. **Symptom**: D-135 documented 4 leak sites per OrbStack process exit, all tracing through `spec_assert_runner_base.zig:1441` (`.assert_invalid` arm calling compileWasm). The leak was INSIDE compileWasm's error path, not the caller's `var c = compiled_ok; c.deinit(gpa)` deinit. **Fix**: pair each alloc with `errdefer free` immediately after success. Success-path `return .{...}` doesn't fire errdefers; error returns now unwind cleanly. Removed redundant manual `allocator.free(sigs); allocator.free(typeidxs);` inside the tidx check. **Path-discriminator tests** added at `src/engine/runner.zig`: `compileWasm: empty function section (count=0) — binary.60.wasm shape (D-135 path)` + `compileWasm: empty code section (count=0) — binary.61.wasm shape (D-135 path)`; existing `compileWasm: empty module (header only)` test ungated (was Mac-only). All three pass on Mac aarch64 + OrbStack Linux x86_64 with no DebugAllocator leak reports — confirming the leak was the error-return paths, not the deinit. **D-134 retrospective**: d-68's `enable_segfault_handler = false` claim of "DISCHARGED" was overoptimistic — the heisenbug re-triggered on d-69's `zig build test-all` despite the disable still being in place. d-68 likely reduced the rate via binary-layout perturbation; d-69's errdefer additions (also layout-perturbing, compile-time only and structurally orthogonal to signal handling) re-triggered the flake. **D-134 re-instated as `now` debt** with refined narrative; the d-68 phase-log entry's "DISCHARGED" claim is retroactively scoped to "rate-reduced via competing-handler removal". Mac aarch64 `zig build test` + `test-spec-wasm-2.0-assert` green (23784/0/2286 unchanged). OrbStack `zig build test` rc=0 (no leaks), `test-all` SEGV reproduces (D-134 flake). (`00b93cbd`) `[x]`
- **9.9-l-1b-d093-d68** — D-134 rate-reduced (was claimed DISCHARGED at d-68 close; revised at d-69 — see d-69 row): disable Zig default segfault handler in spec_assert runner (0 PASS Δ at the runner-tally level, but **unblocks OrbStack per-chunk gate**). d-65 → d-67 → d-68 hypothesis chain converged on d-67-named hypothesis (ii): Zig's `std.debug.handleSegfaultPosix` chain was firing on the real OrbStack SEGV despite our explicit `installSigsegvHandler` sigaction. **Mechanism**: in Debug builds, `default_enable_segfault_handler = runtime_safety and have_segfault_handling_support` is `true`, so Zig startup calls `maybeEnableSegfaultHandler()` → `attachSegfaultHandler()` → `updateSegfaultHandler(act)` → `posix.sigaction(.SEGV, .ILL, .BUS, .FPE, ...)` installing the recursive `writeCurrentStackTrace` → `array_list.Aligned` → `mem.Allocator.alignedAlloc` → `mem.Alignment.toByteUnits` safety-panic chain (= ziglang/zig#14658) with `SA.RESETHAND`. Our `installSigsegvHandler` runs in main and overwrites SEGV/BUS, but Zig's reinstall paths (DebugAllocator diagnostic / panic-print) can race and re-route the next SEGV through Zig's chain on OrbStack Linux x86_64 under the d-62 SA.ONSTACK shape. The d-65 narrative claimed `std_options.enable_segfault_handler = false` had been set, but a d-68 resume-time repo-wide grep showed **no `std_options` declaration existed anywhere** — the disable was aspirational, never landed (lesson at `.dev/lessons/2026-05-16-narrative-claim-vs-landed-state.md`). **Fix**: `pub const std_options: std.Options = .{ .enable_segfault_handler = false };` at module scope in `test/spec/spec_assert_runner_non_simd.zig`. Zig's startup no longer installs `handleSegfaultPosix`; our handler is the sole SEGV/BUS sigaction; real SEGVs dispatch through `sigsegvHandler` → `siglongjmp` → recovery, run continues. **Result**: OrbStack `zig build test-all` **exits 0** (was non-deterministic SEGV across d-64 through d-67). Per-runner tallies bit-identical Mac aarch64 + OrbStack: spec_assert_runner_non_simd 23784/0/2286 (= 1790 skip-impl + 496 skip-adr, 84 manifests); simd_assert_runner 13301/0/440 (= 50 skip-impl + 390 skip-adr, 26 manifests); realworld_run_runner 45/55 (10 WASI-skipped); diff_runner 40/55 matched / 5 skipped-empty / 10 skipped-v2; edge-case runner 40+46; wasi_runner 2/0; wast_runner 72/0; wast_runtime_runner 5/0; spec_runner 3+9/0. **D-134 row removed from `.dev/debt.md`**. **§9.9 closing path now unblocked** — Windows reconciliation + audit_scaffolding + 9.9 `[x]` flip → substrate audit hard gate at 9.12 surfaces next. References: ziglang/zig#14658, `~/Documents/OSS/zig/lib/std/debug.zig:default_enable_segfault_handler`, `~/Documents/OSS/zig/lib/std/Options.zig:enable_segfault_handler`. (`e2231c89`) `[x]`
- **9.9-l-1b-d093-d67** — D-134 probe: single_threaded spec_assert runner rules out cross-thread `siglongjmp` (negative result; 0 PASS Δ; diagnostic narrowing). Probe of d-65's leading hypothesis — that the OrbStack `zwasm-spec-wasm-2-0-assert` SEGV is a cross-thread `siglongjmp` (POSIX-undefined when `sigsetjmp`'s thread ≠ SEGV-receiving thread, because `std.Io.Threaded.init` spawns workers per `~/Documents/OSS/zig/lib/std/Io/Threaded.zig:127`). **Probe mechanism**: set `.single_threaded = true` on `non_simd_assert_runner_mod` in `build.zig`; that switches the compile to `-fsingle-threaded` so `Threaded.init` returns `.init_single_threaded` (`builtin.single_threaded` short-circuit). The runner walks corpora + invokes JIT bodies purely sequentially — no `async` / `concurrent` use — so single-threaded is the correct execution shape regardless. **Result**: Mac aarch64 `test-spec-wasm-2.0-assert` 23784/0/2286 **unchanged** (single_threaded is a no-op on the runner's sequential pipeline). OrbStack `test-all` SEGV **still reproduces** at the same `zwasm-spec-wasm-2-0-assert` step → **cross-thread `siglongjmp` is refuted**. **Hypothesis space narrowed to** (post-d-67): (i) libc-context SEGV — d-65 valgrind captured RIP in the libc.so.6 region (addr=0x1, NULL+1 deref pattern); (ii) Zig's `std.debug.handleSegfaultPosix` chain still firing despite our sigaction (D-134 candidate path (d) — toolchain PR #25227 / ziglang/zig#14658). **Build flag retained**: the runner is semantically sequential, so single-threaded is correct regardless and keeping it permanently removes cross-thread from future D-134 investigations. Mac `zig build test` rc=0 ✓. simd 13301/0/440 unchanged. References: d-65 retrospective at `.dev/lessons/2026-05-16-zig-sigsegv-recovery-flake.md`; D-134 row in `.dev/debt.md`. `[x]`
- **9.9-l-1b-d093-d66** — D-133 partial: arm64 `emitTableSize` X10 → X14 (0 PASS Δ; latent / preventive). Continuation of d-64's X14/X15 scratch-rename sweep. `src/engine/codegen/arm64/op_table.zig::emitTableSize` (lines 161 + 168) used X10 for the `tables_ptr` pre-load while X10 is in `abi.allocatable_caller_saved_scratch_gprs` — same latent vreg-clobber class as D-132. emitTableSize is the simplest sweep target (single scratch register) so the rename is mechanical (`encLdrImm(10, ...)` → `encLdrImm(14, ...)`, `encLdrImmW(wd, 10, len_off)` → `encLdrImmW(wd, 14, len_off)`). When `wd` is itself X14 (= `spill_stage_gprs[0]` returned by `gprDefSpilled` for spilled results), the second LDR `LDR W14, [X14, +len_off]` reads tables_ptr from X14 and writes len-zext into X14 in one instruction — correctness preserved (the address is consumed before the write). Per-function docstring + module header comment updated to cite §9.9-l-1b-d093-d64/d66 + D-132/D-133 + the substrate-audit Q5 unification trigger. **No new edge fixture**: `edge_case_testing.md` "Implementation details that aren't observable from Wasm" exemption applies — the rename is internal regalloc-pool hygiene with no current corpus trigger; the existing `test/edge_cases/p9/table_ops/size_initial.{wat,wasm,expect}` smoke fixture + the table.size spec corpus exercise the post-fix path. **Remaining D-133 sites** (need ≥3 scratch slots simultaneously): emitTableFill / emitTableGrow / emitTableCopy / emitTableInit (op_table.zig) + emitMemoryInit + inline `data.drop` / `elem.drop` (op_memory.zig + emit.zig). Out of scope for the 2-slot X14/X15 pattern — queued for the Phase 9 完備 substrate audit's comptime-disjointness mechanism (`.dev/archive/phase9/phase9_completion_substrate_audit.md` §Q5). spec_assert non-simd Mac aarch64 23784/0/2286 unchanged (latent — no current corpus trigger); simd 13301/0/440 unchanged. Mac aarch64 `zig build test` + `test-edge-cases` + `test-spec-wasm-2.0-assert` green. OrbStack `test-all` SEGV reproduces in `zwasm-spec-wasm-2-0-assert` — **pre-existing D-134 flake**, not a d-66 regression; 1603/1631 other OrbStack tests pass per Build Summary (mirror of d-64's documented OrbStack disposition). `[x]`
- **9.9-l-1b-d093-d65** — D-134 deep investigation (no fix, doc-only). Three concrete findings: (1) our `sigsegvHandler` installs OK and fires on intentional null deref (probe) but NOT on the real SEGV → SEGV reaches a context where our handler cannot run (worker thread? signal masked?); (2) valgrind captured a recursive Zig-default-handler chain faulting in `mem.Alignment.toByteUnits` safety panic inside `writeCurrentStackTrace` (ziglang/zig#14658); (3) RIP at SEGV is in libc.so.6 region, addr is NULL+1 or stack-guard-adjacent. Two distinct triggers observed: `assert_return as-binary-right` (call_indirect manifest) AND `assert_exhaustion runaway`. Web survey points at cross-thread `siglongjmp` (POSIX-undefined when `sigsetjmp`'s thread ≠ SEGV-receiving thread) as primary hypothesis. **D-135 filed** for a concurrent (distinct) DebugAllocator leak at `compileWasm` empty-function-section early-return — not the SEGV root cause (direct-binary repro produces leak AND exits 0 in some runs and SEGVs in others). **Phase 9 完備 substrate audit §Q6 added** (libc dependency boundary, pre-Phase-10 hygiene) — current code has deepening libc fanout (`sigsetjmp`/`siglongjmp` core + many `std.c.*` callsites) while Zig 0.16 stdlib aims at buildable-without-libc. Full retrospective at `.dev/lessons/2026-05-16-zig-sigsegv-recovery-flake.md`. (`9329c799` investigation + `c4a1d75c` §Q6) `[x]`
- **9.9-l-1b-d093-d64** — D-132 partial discharge: arm64 op_table X10/X11/X12 → X14/X15 (+1 PASS Mac aarch64). Root cause of D-132 (`table_get:is_null-funcref(2)` JIT roundtrip bug filed at d-63): `src/engine/codegen/arm64/op_table.zig::emitTableGet` / `emitTableSet` hardcoded X10, X11, X12 as intra-op scratch (loading `tables_ptr` → X10, refs ptr → X11, len → W12) while those three slots were members of `abi.allocatable_caller_saved_scratch_gprs` (the regalloc pool). When a vreg's live range crossed a table.get / table.set and regalloc landed the vreg on X10/X11/X12, the emit's `LDR X10, [X19, #tables_ptr_off]` silently clobbered the vreg's value — surfaced canonically by `(table.set $t3 2 (table.get $t3 1))` where the outer set's idx vreg (V_const2) landed on X10 and the inner get's X10-LDR overwrote it. **Bisect path**: minimal `funcref_roundtrip.wat` edge fixture progressively narrowed the trigger to "two table.set ops in one function, second nesting a table.get" — JIT-byte dump via `private/dbg/d132/repro.zig` made the X10 clobber visible. **Fix**: re-target scratch in emitTableGet / emitTableSet to X14/X15 (the `spill_stage_gprs` pair — non-allocatable, free after the operand-load step). Sequencing fits in 2 scratch regs: `LDR X14, [X19, +tables_ptr_off]; LDR W15, [X14, +(tbl_off+8)]; LDR X14, [X14, +tbl_off]; CMP W17, W15; B.HS trap; LDR/STR X*, [X14, X17, LSL #3]`. **Regression fixture**: `test/edge_cases/p9/table_ops/funcref_roundtrip.{wat,wasm,expect}` — elem-init populates $t[1], $setup copies $t[1] → $t[2], test asserts `ref.is_null(table.get $t 2) → 0`. Mac aarch64 PASS. **Regen script**: d-63 targeted-skip post-process removed; the manifest now carries the real `assert_return is_null-funcref i32:2 -> i32:0` line. Mac aarch64 spec_assert non-simd 23783/0/2287 → **23784/0/2286** (+1 PASS). **D-132 discharged**. **D-133 filed** for the same-shape hardcoded-scratch sites left untouched at emitTableSize / emitTableFill / emitTableGrow / emitTableCopy / emitTableInit / emitElemDrop + op_memory.zig's emitMemoryInit / emitDataDrop (current corpus doesn't trigger; same vreg-clobber failure mode). **D-134 filed** for an OrbStack `test-spec-wasm-2.0-assert` SEGV that reemerged d-62-class flake — verified the d-63 source `b4e11a86` also reproduces today (NOT a d-64 regression). **Retrospective**: `.dev/lessons/2026-05-16-regalloc-pool-scratch-overlap.md` (4-angle: no-workaround / debug ergonomics / future-proofing / design issues) — names W54 pattern recurrence as the dominant lesson; queued 5 enforcement actions into Phase 9 完備 substrate audit gate (`.dev/archive/phase9/phase9_completion_substrate_audit.md` §Q5): comptime disjointness extension, `audit_scaffolding` §G magic-numeral lint, `bug_fix_survey` enforcement in `/continue` Step 4, "comment-as-invariant" rule, test-design stress-axis requirement. (`1bec1d9a`) `[x]`
- **9.9-l-1b-d093-d63** — Drain non-scalar-arg/result family via reftype-alias-to-i64 (+176 PASS). 33 entries across `table_get` / `table_set` / `table_grow` / `table_fill` / `ref_null` / `ref_is_null` reclassified out of the `skip-impl non-scalar-{arg,result}` / `trap-non-scalar-arg` / `action-non-scalar-arg` buckets. Per ADR-0061 (d-33 codegen plumbing), reftypes (`externref` / `funcref`) alias onto the i64 8-byte GPR-class scalar path at the C-ABI boundary; d-63 leverages this at the manifest+runner level so previously-skipped reftype entries execute end-to-end. **Distiller fix** (`scripts/regen_spec_2_0_assert.sh`): new `kind_alias(t)` helper maps `externref` / `funcref` → `i64` for `arg_kinds` / `result_kind` tuple lookups; `fmt(v)` encodes reftype values as `i64:<host_extern_encoding>` (ref.null → `i64:0`, ref.extern N / ref.func N → `i64:(0x8000_0000_0000_0000 | (N+1))` to avoid the `externref 0` collision with `ref.null` and to put host refs in an address-space-disjoint band from FuncEntity pointers); `allowed_scalar` lambda extended to accept reftypes across all 4 directive arms (assert_return / assert_trap / assert_exhaustion / action); `supported` / `trap_supported` / `action_supported` sets extended with 3 new tuples (`(i32, i64) → i32` for table_grow grow, `(i32, i32) → i64` for check-table-null funcref-as-i64 return, `(i32, i64, i32) → void` for table_fill). **entry.zig additions**: 3 new helpers (`callI32_i32i64`, `callI64_i32i32`, `callVoid_i32i64i32`) mirror the AAPCS64/SysV pattern. **Runner dispatch**: 3 new arms in `dispatchScalarResult` / `dispatchVoidResult`, plus 1 trap arm in `nonSimdRunAssertTrap` (reuses the void helper since trap-detection ignores result type), plus 1 invoke-action arm in `invokeActionShape`. **D-132 filed** (post-process targeted skip): table_get's `assert_return is_null-funcref i32:2 -> i32:0` exposes a JIT-side roundtrip bug where init's `(table.set $t3 2 (table.get $t3 1))` writes through `tables_ptr[k].refs[2]` but the subsequent `is_null-funcref(2)` returns null. Both ops use the same storage path at the arm64 emit level (no funcptr_base vs refs divergence — this is NOT D-126's class of bug). Root cause investigation deferred to d-64; the regen-script post-process sed-replaces the specific failing line with `skip-impl funcref-table-roundtrip-bug ... — D-132`, keeping the gate green while preserving the 9 other passing table_get assertions. **Side effect** (skip-adr drop): the `skip_host_state_diverged` ADR mechanism (d-43's `module_state_diverged` flag set by `skip-impl action-non-scalar-arg`) no longer fires because reftype actions now succeed; 181 previously-skipped follow-up assertions now execute and PASS. Outcome: spec_assert non-simd 23607/0/2463 → **23783/0/2287** (+176 PASS, 0 FAIL; skip-impl 1786 → 1791 = +5 net (33 reftype reclass + 13 shape-gap reclass + 0 originally-passing - 1 D-132 funcref-bug skip - 50 reclassified-but-shifted), skip-adr 677 → 496 = -181 host_state_diverged retirements); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`b4e11a86`) `[x]`
- **9.9-l-1b-d093-d62** — Drain directive-assert_exhaustion skip-impl backlog (+15 PASS). 15 entries across `call_indirect` ×2, `fac` ×1, `call` ×2, `skip-stack-guard-page` ×10. Wast's `(assert_exhaustion (invoke "fn" ...) "call stack exhausted")` asserts the named function traps due to runaway recursion. In our JIT, recursion accumulates native AAPCS64/SysV frames until the kernel's stack-guard page is hit; the SIGSEGV is converted to `Error.Trap` by the d-29 sigsetjmp/siglongjmp handler. From the runner's perspective the PASS criterion (= "invocation trapped") is identical to assert_trap; the directive name is preserved for manifest auditability. **Distiller fix** (`scripts/regen_spec_2_0_assert.sh`): new `elif t == 'assert_exhaustion':` arm mirrors the assert_trap arm shape — skip-adr-skip_cross_module_action for cross-module actions, skip-impl exhaustion-* for non-invoke / non-scalar / shape-gap, otherwise emit `assert_exhaustion {field} {args}`. Reuses the assert_trap arg-shape filter (`exhaustion_supported` set, kept in lockstep with `trap_supported`). **Runner-base fix** (`test/spec/spec_assert_runner_base.zig`): new `DirectiveKind.assert_exhaustion` + classifyDirective arm (`assert_exhaustion ` = 18-char prefix) + runCorpus dispatch arm that delegates to `callbacks.handle_assert_trap` — same dispatch as assert_trap since trap-reason classification is out of scope per D-022. **Altstack landing**: `installSigsegvHandler` now installs an explicit alternate signal stack (256 KB static `signal_stack` array, page-aligned via `align(std.heap.page_size_max)`) via `std.posix.sigaltstack` and adds `std.posix.SA.ONSTACK` to the sigaction flags. Without altstack delivery the handler itself would crash for lack of SP when the JIT body exhausts the native stack. Zig's start-up code installs a threadlocal altstack via `Thread.maybeAttachSignalStack` (default 256 KB), but that wasn't sufficient on Linux x86_64 across the test-all aggregate's binary variants; owning the altstack at the runner level removes the host-specific behavioural divergence. siglongjmp's saved-SP restore correctly returns from the altstack to the dispatch frame on the main stack. **Atomic flag**: `sigsegv_armed` upgraded from plain `bool` to `std.atomic.Value(bool)`. Under SA.ONSTACK the handler runs on a different stack from the dispatcher, and Zig 0.16 can legitimately fold the BSS load when the handler is invisible to its dataflow analysis. The diagnostic verified this empirically: standalone `zig build test-spec-wasm-2.0-assert` succeeded but `zig build test-all` produced a different binary hash that elided the load and SEGV'd at deep recursion. `*volatile bool` pointer casts didn't help either — `std.atomic.Value`'s release-store / acquire-load pair forces a real memory access via the canonical atomic intrinsics in every codegen path. Updated the 2 dispatch-site stores (arm + disarm in `nonSimdRunAssertTrap`) and the 2 in-test arm/disarm pairs in the runner-base test blocks. **Verification**: clean-cache (`rm -rf .zig-cache`) full test-all on both hosts passes. All 15 entries exercise recursive functions; each invocation SIGSEGVs at the stack guard, handler longjmps via altstack, dispatch returns `Error.Trap`, runner PASSes the assertion. Outcome: spec_assert non-simd 23592/0/2478 → **23607/0/2463** (+15 PASS, 0 FAIL; skip-impl 1801 → 1786 = -15, skip-adr 677 unchanged); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`da18faae`) `[x]`
- **9.9-l-1b-d093-d61** — Drain runner-shape-gap skip-impl residual (+16 PASS). Continuation of d-55's drain. Live tally post-d-60 showed 16 residual `runner-shape-gap (...)` entries across `float_exprs` (×13) + `func` (×3) — 7 unique dispatch shapes that d-55's batch did not cover (FP-result 2-arg-i32, i32-result 3-arg-FP, mixed 3-arg, 8-arg f64, mixed 6-arg). **entry.zig additions**: 7 new helpers (`callF32_i32i32`, `callF64_i32i32`, `callI32_f32f32f32`, `callI32_f64f64f64`, `callI32_i32f64i32`, `callF64_f64f64f64f64f64f64f64f64`, `callF64_f32i32i64i32f64i32`) mirror the established AAPCS64/SysV pattern (rt + a0..aN args, callconv(.c) Fn alias, trap_flag check). **Dispatch additions**: 7 matching arms in `spec_assert_runner_non_simd.zig::dispatchScalarResult` use `@bitCast` to bridge `ArgValue.f32/f64` integer storage to the helper's f32/f64 signature; `@bitCast` reverse to `u32/u64` on return. **Capacity bump**: `var args: [5]ArgValue` → `[8]ArgValue` at all 3 call sites — pre-d-61 cap was sized for d-55's 5-arg max `(i64 f32 f64 i32 i32, *)`; the 8-arg + 6-arg shapes parsed-out as `error.TooManyArgs` with a `> 5 args unsupported` FAIL on first run, requiring the bump. **Distiller**: 7 new tuples in the `supported` set in `scripts/regen_spec_2_0_assert.sh`, driving the regen to emit `assert_return ... -> ...` lines instead of `skip-impl runner-shape-gap`. **Corpora touched**: `float_exprs` ×13 + `func` ×3 = 16 reclassified. **Verification**: all 16 entries flipped from `skip-impl` to `PASS` (none surfaced as new FAILs after the cap bump). Outcome: spec_assert non-simd 23576/0/2494 → **23592/0/2478** (+16 PASS, 0 FAIL, +0 manifests; skip-impl 1817 → 1801 = -16, skip-adr 677 unchanged); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`31e076a6`) `[x]`
- **9.9-l-1b-d093-d60** — D-131 discharge: prefix-vocab gate green (mechanical vocab rename + 2 ADR docs; 0 PASS Δ, 0 FAIL Δ). Pre-d-60 baseline `bash scripts/check_skip_adrs.sh --gate` reported `Prefix-vocab violations: 291` — every `skip-adr-cross-module-action ...` (110 lines, 5 corpora) and `skip-adr-host-state-diverged ...` (181 lines, 8 corpora) manifest entry violated ADR-0029 Path B's vocab rule (the script extracts `adr_id = prefix#skip-adr-` and looks up `.dev/decisions/${adr_id}.md`; cross-module-action.md / host-state-diverged.md never existed, while the gate-passing pattern is `skip-adr-skip_<id>` matching `.dev/decisions/skip_<id>.md`). Per `.claude/rules/no_workaround.md` and `lessons_vs_adr.md`, the principled fix is rename + ADR rather than patching the script to accept the bare-name form. **Distiller fix**: 5 string-replacements in `scripts/regen_spec_2_0_assert.sh` — three `skip-adr-cross-module-action` arms (`assert_return` / `assert_trap` / `action`) and two `skip-adr-host-state-diverged` emits (`assert_return` flag-gated + comment) updated to the `skip-adr-skip_<id>` form. **ADRs**: `.dev/decisions/skip_cross_module_action.md` (cross-module composition is Phase 10+ scope per D-079; details the path forward — instance-aware spec harness + per-`.wast` instance registry + reroute distiller's three arms to emit real cross-module dispatch) and `.dev/decisions/skip_host_state_diverged.md` (reftype-arg dispatch gap at runner's `[5]ArgValue` matrix; details the path forward — add reftype `ArgValue` variant + plumb through dispatch ladders + the `module_state_diverged` flag stops getting set). Both ADRs cite the originating chunks (d-37 / d-43), the paired `skip_cross_module_register.md`, and the runner-level prefix-routing mechanism. **Runner**: no change — `classifySkipLine`'s prefix match on `skip-adr-` continues to route every entry to `skipped_adr++`. Outcome: `check_skip_adrs --gate` now exits 0 (was 1); `Prefix-vocab violations: 0` (was 291); Total skip-ADRs: 6 (was 3 — added cross_module_register + cross_module_action + host_state_diverged). spec_assert non-simd 23576/0/2494 → **23576/0/2494** (vocab rename is bit-identical to runner-level prefix-routing — counts identical at the tally level). simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. **D-131 discharged** (deleted from `.dev/debt.md` per Step 7 close discipline). (`310476bd`) `[x]`
- **9.9-l-1b-d093-d59** — Drain directive-register skip-impl backlog (skip-adr reclassification only; 0 PASS Δ, 0 FAIL Δ). Pre-d-59 manifest had 21 entries tagged `skip-impl directive-register` (catch-all from the distiller's `else: lines.append(f'skip-impl directive-{t}')`). The wast `(register "alias" $M)` directive binds the named (or current) module under a host-import key so subsequent modules in the same `.wast` can `(invoke $alias "fn" ...)`. Our spec_assert scaffold already filters every `action.module`-bearing assertion via `skip-adr-cross-module-action` (distiller `assert_return` / `assert_trap` / `action` arms) and every module with non-spectest imports via `hasUnbindableImports` (d-37); nothing downstream consumes the register's effect. **Distiller fix**: new `elif t == 'register':` arm emits `skip-adr-skip_cross_module_register as={c["as"]}` — the gate-conforming `skip-adr-skip_<adr_id>` vocab matching ADR file `skip_<adr_id>.md` per ADR-0029 Path B's `check_skip_adrs.sh` prefix-vocab rule (vs the older `skip-adr-cross-module-action` / `skip-adr-host-state-diverged` vocabs introduced d-37 / d-43 without paired ADRs — those 291 violations are pre-existing and tracked as D-131). **ADR**: `.dev/decisions/skip_cross_module_register.md` covering the vocab + removal plan (Phase 10+ instance-aware refactor + cross-module imports row) + machine-checkable removal condition (every line is replaced by a real `register {as} {wasm}` directive against an instance-aware runner). **Runner**: no change — the existing `classifySkipLine` prefix match on `skip-adr-` automatically routes the 21 reclassified entries to `skipped_adr++` instead of `skipped_impl++`. Corpora touched: `elem` ×3, `imports` ×2, `linking` ×9, `memory_grow` ×2, `ref_func` ×1, `table_copy` ×1, `table_grow` ×2, `table_init` ×1 = 21. **D-131 filed** for the pre-existing 291 prefix-vocab gate violations (cross-module-action × 5 manifests + host-state-diverged × 8 manifests; mechanical sed + 2 new ADR docs; d-60 candidate). spec_assert non-simd 23576/0/2494 → **23576/0/2494** (counts unchanged at the tally level — purely a redistribution within `skipped`: skip-impl 1838 → 1817 = -21, skip-adr 656 → 677 = +21); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`0815e94e`) `[x]`
- **9.9-l-1b-d093-d58** — Drain directive-assert_unlinkable skip-impl backlog. Pre-d-58 manifest had 83 entries tagged `skip-impl directive-assert_unlinkable` (`imports` ×71 + `linking` ×12). This directive asserts that a module fails to link due to `unknown import` (no provider for the named import) or `incompatible import type` (provider exists but wrong type). Mirrors d-57's pattern but the trigger is import resolution rather than instantiation work, so the dispatch arm is fully inline in `runCorpus` (no callback needed). **Distiller fix**: `t == 'assert_unlinkable'` arm emits `assert_unlinkable {filename}` for binary modules; copy-loop expanded. **Runner-base fix**: new `DirectiveKind.assert_unlinkable` + `classifyDirective` arm + `runCorpus` dispatch with three paths — (1) `hasUnbindableImports` trips → PASS (structurally unlinkable in our spec scaffold: any non-spectest module name OR any non-function spectest import); (2) `compileWasm` rejects → PASS (compile-stage caught the import-type mismatch); (3) module compiles cleanly with bindable imports → SKIP-NO-LINK-TYPECHECK + skip-adr++ (we lack link-time import-type validation against the host binding; ADR-grade gap). **Outcome distribution** of the 83 reclassified entries: 79 PASS (path 1 or 2 — mostly `unknown import` cases caught by hasUnbindableImports), 4 SKIP-NO-LINK-TYPECHECK (incompatible-type cases that compile cleanly under our weak link checking). spec_assert non-simd 23497/0/2573 → **23576/0/2494** (+79 PASS, 0 FAIL; skip-impl 1921 → 1838 = -83 = all reclassified out; skip-adr 652 → 656 = +4 = the SKIP-NO-LINK-TYPECHECK path); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`454b214f`) `[x]`
- **9.9-l-1b-d093-d57** — Drain directive-assert_uninstantiable skip-impl backlog. Pre-d-57 manifest had 34 entries tagged `skip-impl directive-assert_uninstantiable` (catch-all from the distiller's `else: lines.append(f'skip-impl directive-{t}')`). This directive asserts that a module is valid (compiles cleanly) but fails to instantiate — typically OOB active data/elem segments or trapping start function. **Distiller fix**: `t == 'assert_uninstantiable'` arm emits `assert_uninstantiable {filename}` for binary modules (text modules → `skip-impl directive-assert_uninstantiable-non-binary`); copy-loop expanded to include the new directive's wasm file. **Runner-base fix**: new `DirectiveKind.assert_uninstantiable` + `classifyDirective` arm. New optional `RunnerCallbacks.handle_assert_uninstantiable` callback (null-safe — runners that lack it surface SKIP-NO-INSTANTIATE-CB instead of FAIL). New `runCorpus` arm: applies d-37 cross-module-imports pre-filter (SKIP), treats compile-stage failure as PASS (compile-rejected ⇒ never instantiable), then dispatches to the callback. **Non-simd specialisation fix**: new `nonSimdHandleAssertUninstantiable(gpa, wasm, compiled, stdout, name) anyerror!bool` mirrors `nonSimdOnModuleLoaded`'s init pipeline (memory-limits reset → applyActiveDataSegments → applyDefinedGlobalsInit → applyTableInit → setupMultiTableScratch → start-fn invocation) but inverts outcomes — any step's error returns `true` (PASS); only a clean instantiation prints `FAIL ... but module instantiated cleanly` and returns `false`. **Outcome distribution** of the 34 reclassified entries: 19 PASS (`data` ×14 OOB-write traps, `elem` ×4 OOB-table-init traps, `start.8` `unreachable` start fn), 11 SKIP-CROSS-MODULE-IMPORTS (linking corpus + some elem cases whose modules import unbindable host state — surfaces via existing d-37 hasUnbindableImports filter), 4 remain skip-impl as `directive-assert_uninstantiable-non-binary` (text-format wasts). spec_assert non-simd 23478/0/2592 → **23497/0/2573** (+19 PASS, 0 FAIL; skip-impl 1940 → 1921 = -19); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`a1d98530`) `[x]`
- **9.9-l-1b-d093-d56** — Drain trap-shape-gap skip-impl backlog. Mirror of d-55 runner-shape-gap fix on the assert_trap path. Pre-d-56 manifest had 30 entries tagged `skip-impl trap-shape-gap (i32 i32 i32) <field>` — `memory_copy` ×14 (bounds-trap variants on (dest, src, len) triples), `table_copy` ×11 (same shape on tables), `memory_fill` ×3, `call_indirect` ×2 (`as-call-1/-2/-3` 3-arg trap variants). **Fix**: distiller's `trap_supported` set in `scripts/regen_spec_2_0_assert.sh` extended with `('i32', 'i32', 'i32')`; matching dispatch arm added to `nonSimdRunAssertTrap` in `test/spec/spec_assert_runner_non_simd.zig` (reuses d-55's `entry.callI32_i32i32i32` helper — trap result-type immaterial since `Error.Trap` is the only PASS outcome for assert_trap). All 30 entries reclassify from skip-impl to `assert_trap` and execute. spec_assert non-simd 23448/0/2622 → **23478/0/2592** (+30 PASS, 0 FAIL; skip-impl 1970 → 1940); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`1181d927`) `[x]`
- **9.9-l-1b-d093-d55** — Drain runner-shape-gap skip-impl backlog. Live measurement (post-d-54): manifest skip-impl had 748 entries, 483 of them tagged `runner-shape-gap (...)` — multi-arg dispatch shapes the spec_assert runner's `dispatchScalarResult` ladder doesn't yet handle. Top families: `(i32 i32 i32, i32)` × 275 (covers `nop` as-call-* + memory_copy/fill ranges + many other 3-i32 tests), `(f64 f64 f64, f64)` × 46 (3-f64 FP arith), `(f32 f32 f32, f32)` × 41, mixed-FP `(f32 f64, f32)` × 21 + `(f64 f32, f32)` × 20, 4-arg FP `(f64 f64 f64 f64, f64)` × 18 + `(f32 f32 f32 f32, f32)` × 17, `(f64 f64 i32, f64)` × 10 + `(f32 f32 i32, f32)` × 10, `(i32 i64, i64)` × 5, `(i64 i64 i32, i64)` × 4. **d-55 fix**: 11 new entry helpers added to `src/engine/codegen/shared/entry.zig` (`callI32_i32i32i32`, `callI64_i32i64`, `callI64_i64i64i32`, `callF32_f32f32f32`, `callF32_f32f32f32f32`, `callF32_f32f32i32`, `callF32_f32f64`, `callF32_f64f32`, `callF64_f64f64f64`, `callF64_f64f64f64f64`, `callF64_f64f64i32`) — each ~16 LOC mirroring the established AAPCS64/SysV pattern (rt + a0..aN args, callconv(.c) Fn alias, trap_flag check). Wired into `spec_assert_runner_non_simd.zig`'s `dispatchScalarResult` (uses the d-54 `base.printCallTrap` helper for trap routing). Distiller's `supported` set in `scripts/regen_spec_2_0_assert.sh` extended with the 11 matching `(arg_kinds, result_kind)` tuples — this drives the regen to emit `assert_return ...` instead of `skip-impl runner-shape-gap (...)` for these shapes. spec_assert non-simd 22981/0/3089 → **23448/0/2622** (+467 PASS, 0 FAIL; skip-impl 2437 → 1970 = -467 = the unblocked assertions executed end-to-end and passed); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`65e5bacb`) `[x]`
- **9.9-l-1b-d093-d54** — D-129 close. Original framing was distiller-side reachability analysis (parse the wasm to detect spectest-import calls in each export's body); d-54 took the runtime-side alternative which is much smaller. **Sentinel design**: `hostImportTrapStub` writes `HOST_IMPORT_TRAP_SENTINEL = 0xBADC0DE` into `rt.trap_flag` (was the JIT-body-standard `1`). The JIT body's own trap stub still writes `1`; any other non-zero value observed by the runner post-call is the host-import path. Added `pending_host_import_skip` bool side-channel + new `printCallTrap(rt, name, fn_name, args_s, err, stdout)` helper in spec_assert_runner_base. Helper detects sentinel, prints `SKIP-HOST-IMPORT  …` instead of `FAIL …`, and sets the flag. `runCorpus` resets the flag before each callback dispatch and routes `ok=false-with-flag` to `tally.skipped_adr++` instead of `tally.failed++` (assert_return + invoke-action paths; assert_trap doesn't need it because host-import trap satisfies the spec's "trap expected" contract naturally). 40 FAIL-print sites in spec_assert_runner_non_simd.zig consolidated to `base.printCallTrap()`. **`names` + `imports` re-enabled in NAMES** — the +483 PASS that was waiting on D-129 lands. spec_assert non-simd 22498/0/2916 → **22981/0/3089** (+483 PASS, 0 FAIL, +2 manifests = `names` + `imports`); the 2 spectest-import-wrapper traps now show as SKIP-HOST-IMPORT lines + skipped_adr++. simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`1d59c587`) `[x]`
- **9.9-l-1b-d093-d53** — D-128 partial close (the manifest-parse-split half). The 9 names FAILs surfaced at d-51 trial-enable came from the python distiller emitting `f'assert_return {a["field"]} {args_s} -> {results_s}'` with the export name passed through raw — when the name contained whitespace / control chars / quotes / colon (names.wast exports `\t\n`, `​`, `:`, `\xEF\xBB\xBF` BOM, ZWJ, etc.), the manifest line split mid-token and the runner saw `unknown directive '() -> i32:N'` / `'assert_return'` parse FAILs. **Distiller fix**: new `quote_field(name)` helper in `scripts/regen_spec_2_0_assert.sh` checks `all(0x21 <= ord(c) <= 0x7E and c not in ("'", ':') for c in name)`; when false, emits `:hex:<utf8-hex>` (e.g. `:hex:0a09` for `\n\t`, `:hex:c385` for `Å`). Wired into the 3 directive emit lines (assert_return, assert_trap, invoke-action). **Runner fix**: new `decodeFnName(fn_name, buf)` helper in `test/spec/spec_assert_runner_base.zig` — when fn_name starts with `:hex:`, decodes the trailing hex into the caller's stack buffer (512 bytes) and returns the slice; otherwise passes through. Wired into all 3 fn_name-extraction sites in `spec_assert_runner_non_simd.zig`. 6 new unit tests cover empty-hex, UTF-8 multi-byte, odd-hex reject, buffer-overflow reject. SCRATCH_MAX_FUNCS bumped 256 → 1024 (names.2.wasm declares 479 funcs). **`names` + `imports` re-deferred to D-129**: trial-enable showed 22981 PASS / 2 FAIL — the 2 remaining FAILs (`names: call print32(i32:42 i32:123): Trap` + `imports: call print_i32(i32:13): Trap`) are spectest-import-wrapper calls that the d-35 `hostImportTrapStub` correctly traps but spec asserts expect succeed (side-effect-only invokes); needs distiller-side reachability analysis to mark these assert_returns as `skip-adr-spectest-import-call`. The +577 PASS lands when D-129 closes. spec_assert non-simd 22498/0/2916 unchanged at chunk close (the d-53 fix is infrastructure with no enabled-corpus impact yet); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`b347cb80`) `[x]`
- **9.9-l-1b-d093-d52** — D-127 + D-130 paired close. **D-127** (`binary.60.wasm` MissingTypeSection on compile): Wasm 2.0 spec allows an EMPTY Function section (count=0) without a corresponding Type section. binary.60.wasm encodes `03 01 00` (Function section id=3 size=1 count=0). Pre-d-52 `compileWasm` early-out only fired when the function section was ABSENT; the empty-function-section case fell through to the type-section requirement. Fix in `src/engine/runner.zig::compileWasm`: detect body == single LEB128 0x00 byte and treat as absent. **D-130** (`unreached-valid.wast` `meet-bottom`: `(block (result f64) (block (result f32) unreachable; i32.const 1; br_table 0 1 1) drop f64.const 0)`): per Wasm 2.0 §3.3.5 + §3.3.5.8, in unreachable code the joined br_table label type collapses to bottom and targets with non-unifiable types are accepted. Pre-d-52 `validator.zig::opBrTable` enforced `labelTypesEq` strictly for ALL targets. Fix: skip the strict check + skip popLabelTypes when `topFrame.unreachable_flag` is set. **Companion emit fix**: lower's `closeBlock` for the inner (reachable-then-dead-via-`unreachable`) `block f32` doesn't push merge result vregs (the only `br_table` was emitted as a no-op since `unreachable_at_depth` was set); the outer block's reachable post-end `drop` then has no vreg to pop. The d-5 `.loop` fall-through placeholder pad (vreg 0 sentinel recognised by marshalFunctionReturn / `.drop` etc.) is extended at d-52 to all block kinds in both `arm64/op_control.zig::emitEndIntra` and `x86_64/op_control.zig::emitEndIntra` (was `.loop`-only). Edge fixture `test/edge_cases/p9/br_table/meet_bottom_unreachable.{wat,wasm,expect}` mirrors the wast `meet-bottom` shape + expects trap. binary + unreached-valid corpora land in NAMES. spec_assert non-simd 22404/0/2889 → **22498/0/2916** (+94 PASS, 0 FAIL, +2 manifests); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`9a601838`) `[x]`
- **9.9-l-1b-d093-d51** — Batch enable +11 NAMES via per-corpus trial. Validator-only corpora landing cleanly to skip-impl assert_invalid/malformed: `binary-leb128`, `comments`, `custom`, `inline-module`, `obsolete-keywords`, `token`, `unreached-invalid`. Module-system corpora (mostly cross-module-imports SKIP via d-37 hasUnbindableImports filter): `exports`, `linking`, `table-sub`, `skip-stack-guard-page`. **4 corpora deferred** with new debt rows for the residual FAILs each surfaced: D-127 `binary` (1 FAIL: binary.60.wasm has empty Function section count=0 without Type section; Wasm 2.0 spec allows this but our validator/parser rejects with MissingTypeSection — likely 1-line guard fix), D-128 `names` (9 FAILs: distiller's `fmt(x)` doesn't escape special chars in export names — backslash/quotes/spaces split manifest lines mid-token, surfacing as `unknown directive` parse FAILs; needs distiller-side quoting + runner-side parse for quoted field names), D-129 `imports` (1 FAIL: spectest-import-call traps via d-35 hostImportTrapStub but spec asserts succeed — distiller needs reachability analysis to mark assert_returns whose action transitively calls a spectest import as `skip-adr-spectest-import-call`), D-130 `unreached-valid` (1 compile FAIL: ArityMismatch on .1.wasm — validator's unreachable-path arity tracking gap). spec_assert non-simd 22259/0/2638 → **22404/0/2889** (+145 PASS, 0 FAIL, +11 manifests); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. No source code change beyond regen-script NAMES expansion + commentary citing the new debt rows. (`f7b2aabe`) `[x]`
- **9.9-d-6** — closes D-061: `populateShapeTags` (`src/engine/codegen/shared/regalloc.zig`) (a) extends `any_simd` trigger to v128 in `func.sig.params`/`results`/`func.locals` so a function whose body has no inline SIMD op but whose locals declare v128 still produces shape_tags; (b) handles `local.get` / `local.tee` with type-aware tagging from `func.localValType(payload)` so v128-typed locals propagate into shape_tags; (c) replaces the prior walk's `else => false` arm with `liveness.stackEffect(op).pushes` fall-through so vreg numbering stays aligned with `liveness.compute` even for ops outside the explicit SIMD/scalar producer lists. `liveness.stackEffect` + `StackEffect` are now `pub` to keep the catalogue single-sourced. simd_assert_runner totals unchanged at 226/36/296 because the simd_select.0 fixture remains blocked on v128 local.get/set/tee handlers (arm64+x86_64 reject v128 explicitly + frame layout uses 8-byte slots) — that work is §9.9-e's scope. New tests: "D-061 — v128 params trigger populate via local.get tagging" + "scalar binop between SIMD ops keeps vreg numbering aligned". (`b93e1336`) `[x]`
- **9.9-l-1b-d093-d50** — D-119 + D-120 paired close (mirror of d-49's elem-segment fix for data segments). Spec_assert harness pre-d-50 left `JitRuntime.data_segments_ptr` + `data_dropped_ptr` `undefined` — JIT `memory.init` reads `[X10 + idx*16]` for `seg.ptr`/`seg.len` and `[data_dropped_ptr + idx]` for the dropped byte; both deref garbage and SEGV'd outside any armed sigsetjmp. d-50 adds three globals to spec_assert_runner_base: `scratch_data_segments: [128]SegmentSlice`, `scratch_data_arena: [64KB]u8`, `scratch_data_dropped: [128]u8`. New `populateDataSegments` (called from `setupMultiTableScratch`) walks the data section, copies segment bytes into the flat arena, and flips active segments to dropped per Wasm 2.0 §4.5.5 (active data segments are consumed at instantiation; subsequent `memory.init` against them must trap on n>0). `makeJitRuntime` wires `data_segments_ptr/count` + `data_dropped_ptr/count`. **`src/engine/runner.zig::setupRuntime` patched in lockstep**: the standalone runner had the same active-data-consumed gap — surfaced via new edge fixture `test/edge_cases/p9/memory_ops/init_active_consumed.{wat,wasm,expect}`. **Latent bug also fixed**: `setupMultiTableScratch`'s `if (num_tables == 0) return;` early-return path skipped both `populateElemSegments` and `populateDataSegments` for memory-only modules (e.g. bulk.4.wasm: memory + passive data segment, no tables). Restructured to invoke both populate calls before the early return. SCRATCH_MAX_DATA_SEGMENTS bumped 64 → 128 (memory_init.90.wasm + bulk.6.wasm declare 65 segments). `memory_init` lands in NAMES; **bulk DEFERRED to D-126**: corpus surfaces 4 residual FAILs in bulk.12.wasm — `(table.copy 0 0 dst src n)` writes to `tables_ptr[k].refs` but the legacy table-0 fast-path `JitRuntime.funcptr_base` / `typeidx_base` (= `scratch_funcptrs` / `scratch_typeidxs`) stay stale, so post-mutation `call_indirect` reads stale entries (architectural divergence between the two parallel arrays; likely Phase 10+ instance-aware refactor). bulk corpus dir cleaned (`rm -rf test/spec/wasm-2.0-assert/bulk`). spec_assert non-simd 22049/0/2632 → **22259/0/2638** (+210 PASS, 0 FAIL, +1 manifest = `memory_init`); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`c781e6e9`) `[x]`
- **9.9-l-1b-d093-d49** — D-123 + D-124 paired close. D-123 (`table_init.wast` SEGV mid-corpus): root cause was `JitRuntime.elem_segments_ptr` and `elem_dropped_ptr` being `undefined` in the spec_assert harness — JIT `table.init` reads `[X10 + elemidx*16]` and `[elem_dropped_ptr + elemidx]` and SEGV'd outside any armed sigsetjmp. Pre-d-49 the elem corpus's table.init asserts were SEGV'd and accidentally satisfied by d-29/d-30 SIGSEGV→Trap recovery; pre-d-49 table_init corpus wasn't even in NAMES. d-49 adds three globals to spec_assert_runner_base: `scratch_elem_segments: [128]ElemSlice`, `scratch_elem_refs_arena: [4096]u64`, `scratch_elem_dropped: [128]u8`. New `populateElemSegments` (called from `setupMultiTableScratch`) walks the element section, packs `Value.ref`-encoded funcref pointers into the flat arena, and marks active + declarative segments as dropped per Wasm 2.0 §4.5.4 (active elem segments are consumed at instantiation; their effective size becomes 0 for any subsequent `table.init`). `makeJitRuntime` wires `elem_segments_ptr/count` + `elem_dropped_ptr/count` from the new scratch. **`src/engine/runner.zig::setupRuntime` patched in lockstep**: the standalone runner had the same gap (zeroed `elem_dropped` unconditionally, violating §4.5.4) — surfaced via new edge fixture `init_active_consumed.{wat,wasm,expect}`. D-124 (`table_copy.wast` "8" FAIL — actually 1 FAIL, d-46 narrative was wrong): root cause was `applyTableInit` returning `UnsupportedEntrySignature` when `base + seg.funcidxs.len > funcptrs_buf.len`. table_copy.50.wasm declares `(table 128 128 funcref)` with elem segment writing range 112..128; harness's `scratch_table_capacity = 32` (table-0 fast-path arena in both spec_assert_runner_non_simd + simd_assert_runner) was too small. Bumped 32 → 1024 in both runners (mirror of d-21's GROWABLE_MEMORY_CAPACITY bump). Iso bisect (table_init module 11 SEGV → table_copy.50.wasm UnsupportedEntrySignature) was load-bearing for narrowing the narrative. table_copy + table_init land in NAMES. spec_assert non-simd 20925/0/1311 → **22049/0/2632** (+1124 PASS, 0 FAIL, +2 manifests); simd 13301/0/440 unchanged. Mac aarch64 + OrbStack `test-all` both green. (`d7483097`) `[x]`
- **9.9-l-1b-d093-d48** — D-122 + D-125 paired close. New `JitRuntime.table_grow_fn: *const fn(*JitRuntime, u32, u64, u32) callconv(.c) i32` slot extends head_size 216 → 224; default `defaultTableGrowReject` keeps the SEGV-safe always-fail behaviour for non-spec consumers. ABI follows ADR-0059's `memory_grow_fn` shape: `(rt, tableidx, init_reftype_bits, delta)` → previous size or -1. arm64 `op_table.emitTableGrow` mirrors `emit.zig::.@"memory.grow"` — stage delta into W3, init into X2, MOVZ tableidx into W1, MOV X0 from X19, LDR X16 from `[X19, #table_grow_fn_off]`, BLR; capture W0 → result vreg via the same `.reg`/`.spill` ladder. x86_64 `op_table.emitTableGrow` follows SysV: stage delta into ECX, init into RDX (full 8-byte), MOV ESI #imm = tableidx, MOV RDI = R15, MOV RAX = `[R15+table_grow_fn_off]`, emitShadowAlloc + CALL RAX + emitShadowFree, capture EAX. `usage.usesRuntimePtr` whitelist gap discovered + fixed (table.grow needed R15 for the fn-ptr load; without the prologue's `MOV R15, RDI` the CALL would jump to a garbage address → SIGSEGV; the docstring's "same-class grep target" warning was the structural defence we triggered correctly). Spec runner harness: new `runner.declaredTableMax` (mirror of `declaredTableMin`); `setupMultiTableScratch` reads the declared max into `scratch_tables_descriptor[k].max` so growableTableGrowFn enforces the cap; `growableTableGrowFn` extends `desc.len` in place against `scratch_table_refs[k]`. `SCRATCH_EXTRA_TABLE_CAPACITY` 64 → 1024 (mirror of d-21's GROWABLE_MEMORY_CAPACITY bump; satisfies `table_grow.wast`'s `grow($t, 800)` sequence). Distiller's `non-scalar-arg` path now sets `module_state_diverged` so post-skip observation asserts skip cleanly (same `(ref.null extern)`-arg shape that already had `module_state_diverged` for cross-module actions). Edge fixtures `test/edge_cases/p9/table_ops/grow_happy.{wat,wasm,expect}` (default-reject: grow returns -1, size unchanged) + `grow_max_cap.{wat,wasm,expect}` (no spec-harness wiring; both observe the standalone `defaultTableGrowReject` behaviour). spec_assert non-simd 20927/0/1219 → **20925/0/1311** (-2 PASS, +2 manifests `table_size`/`table_grow`; the -2 PASS = state-diverged conservative skip on `select`/`global`/`br_table`/`ref_is_null`/`table_fill`/`table_set` post-non-scalar-arg observation asserts that previously read state which was technically divergent). simd 13301/0/440 unchanged. Mac + OrbStack `test-all` both green. (`323b0046`) `[x]`
- **9.9-l-1b-d093-d47** — D-121 close. Pre-d-47 `makeJitRuntime` reset `scratch_tables_descriptor[0].len` to `funcptrs.len` (= harness scratch_table_capacity 32) on every per-assert call, clobbering the module-derived `tbl_min` that `setupMultiTableScratch` populated at on_module_loaded. Surfaced as `table_get.wast: get-externref(2)` not trapping — the JIT bounds check saw len=32 instead of the declared 2 for the externref table. d-47 (a) drops the makeJitRuntime descriptor[0] clobber (setupMulti's bind is authoritative for the module lifetime); (b) uses `runner_mod.declaredTableMin(gpa, wasm_bytes, 0)` for k=0 in setupMultiTableScratch instead of `table0_funcptrs.len`. The legacy `JitRuntime.table_size` scalar (W25 / x86_64 [R15+offset]) stays at scratch capacity for call_indirect's fast path; sig-check via maxInt sentinel still traps OOB indices (spec assert_trap accepts the sig-mismatch trap reason). `table_get` lands in NAMES. spec_assert non-simd 20918/0/1213 → **20927/0/1219** (+9 PASS, 0 FAIL, +1 manifest = `table_get`); simd 13301/0/440 unchanged. D-124 trial-enable surfaced `populateTableRefs` UnsupportedEntrySignature on the no-import 2-table `table_copy.19.wasm` (separate root cause); D-123 still SEGVs. Both stay open. (`664b3fa4`) `[x]`
- **9.9-l-1b-d093-d46** — Batch-enable +3 table_* NAMES (`table`, `table_set`, `table_fill`) via per-corpus isolated bisect. 5 of 8 candidates deferred to new debt rows: D-121 (`table_get` externref-OOB-not-trapping, 1 FAIL), D-122 (`table_size` UnsupportedOp at compile, 1 FAIL), D-123 (`table_init` SEGV mid-corpus), D-124 (`table_copy` 8 trap-asserts not firing), D-125 (`table_grow` UnsupportedOp at compile, 6 FAIL). Bisect ran the prebuilt `zwasm-spec-wasm-2-0-assert` binary against an isolated `/tmp/iso2/<name>` corpus subdir per candidate. spec_assert non-simd 20898/0/1153 → **20918/0/1213** (+20 PASS, 0 FAIL, +3 manifests); simd 13301/0/440 unchanged. (`469c50cf`) `[x]`
- **9.9-l-1b-d093-d45** — D-118 close. `br_table.wast`'s `large` declares 16149 br_table targets — past arm64's `count >= (1 << 12)` (4096) cap (mis-labelled `Error.SlotOverflow`) and x86_64's `count > 127` cap (CMP imm8 / Jcc rel8). d-45 introduces per-case CMP dispatch on i magnitude: arm64 `i < 4096` keeps `CMP Wn, #imm12`, `i >= 4096` uses `MOVZ W16, #lo16` + `MOVK W16, #hi16, LSL #16` + `CMP Wn, W16`; x86_64 `i ≤ 127` keeps `CMP r32, imm8` + `Jcc rel8`, `i > 127` uses `CMP r32, imm32` + `Jcc rel32`. W16/X16 is the intra-procedure scratch outside the regalloc pool (ADR-0017). Secondary BadBlockType from reftype-class block result types (`(block (result <ref>) ...)` in `meet-funcref` / `meet-externref` exports) closed via `readBlockType` / `readBlockArity` accepting -16 (0x70 funcref) / -17 (0x6F externref) per Wasm 2.0 §5.3.5; D-104 d-32/d-33 already plumbs the reftype-class codegen path so no further runtime work needed. `br_table` lands in NAMES; one unit test (`compile: br_table count > 127 → UnsupportedOp (i8 cap)`) renamed to assert wide-case compiles. spec_assert non-simd 20728/0/1150 → **20898/0/1153** (+170 PASS, 0 FAIL, +1 manifest = `br_table`); simd 13301/0/440 unchanged. (`50237a0e`) `[x]`
- **9.9-l-1b-d093-d44** — Batch-enable 5 NAMES via per-corpus isolated bisect under the d-42 / d-42b / d-43 multi-table + refs scratch infrastructure. Green: `data`, `global`, `memory_copy`, `memory_fill`, `memory_grow` (+4352 PASS / +5 manifests). Deferred to debt: `br_table` SlotOverflow at compile (D-118), `bulk` SEGV mid-corpus (D-119), `memory_init` 1 value-mismatch + 1 missing-trap (D-120); `table_*` family deferred separately (pre-d-42 D-090 + multi-table refs population for externref writes still incomplete). Bisect ran the prebuilt `zwasm-spec-wasm-2-0-assert` binary against an isolated `/tmp/iso/<name>` corpus subdir per candidate. spec_assert non-simd 16376/0/740 → **20728/0/1150** (+4352 PASS, 0 FAIL, +5 manifests); simd 13301/0/440 unchanged. (`24d875ce`) `[x]`
- **9.9-l-1b-d093-d43** — D-113 close. Pre-d-43 `scratch_tables_descriptor[k].refs` was `undefined` → JIT `table.get` on any table SEGV'd outside any armed sigsetjmp. d-43 mirrors `engine/runner.zig::setupRuntime`'s table_refs arena into the static-scratch spec_assert harness: new `scratch_table_refs[SCRATCH_MAX_TABLES][SCRATCH_EXTRA_TABLE_CAPACITY]u64` + `scratch_func_entities[SCRATCH_MAX_FUNCS]FuncEntity` + `active_func_count`. `makeJitRuntime` now wires `JitRuntime.func_entities_ptr/count` + `tables_ptr[0].refs = &scratch_table_refs[0]` (was undefined). `setupMultiTableScratch` re-initialises `scratch_func_entities[0..num_funcs]` per module load + walks the elem section via new `populateTableRefs(tableidx, refs_out)` helper writing `Value.ref`-encoded FuncEntity pointers for funcref segments and `Value.null_ref` for externref / null slots. The single residual FAIL after the SEGV fix (`externref-elem(i32:1) -> 0` requires `init`'s skipped ref.extern write) is closed via distiller-side `module_state_diverged` flag: set on `skip-impl action-non-scalar-arg`, cleared by next `invoke-action`; while set, subsequent `assert_return` lines emit `skip-adr-host-state-diverged` cleanly. spec_assert non-simd 16369/0/732 → **16376/0/740** (+7 PASS, 0 FAIL, +1 manifest = `ref_is_null`); simd 13301/0/440 unchanged. (`077ca871`) `[x]`
- **9.9-l-1b-d093-d42b** — D-112 close. Spec_assert static-scratch harness wires multi-table call_indirect via new `runner_mod.applyTableInitForTable(tableidx, ...)` (parameterised splay of `applyTableInit`) + `countDeclaredTables` / `declaredTableMin` helpers. Harness adds `SCRATCH_MAX_TABLES = 4` + `SCRATCH_EXTRA_TABLE_CAPACITY = 64` + `scratch_extra_funcptrs` / `scratch_extra_typeidxs` (per non-zero table) + `scratch_table_jit_ci` (TableJitCallInfo array for `JitRuntime.tables_jit_ci_ptr`) + `scratch_tables_descriptor` (TableSlice array for `JitRuntime.tables_ptr` — `.refs = undefined` because the JIT call_indirect path only reads `.len` from this; table.get/set use their own scratch). `setupMultiTableScratch(gpa, wasm_bytes, compiled, table0_funcptrs, table0_typeidxs)` called from each `on_module_loaded` walks per-non-zero-table elem segments via `applyTableInitForTable` and binds `scratch_table_jit_ci[k]` / `scratch_tables_descriptor[k]` / `active_table_count = num_tables`. `makeJitRuntime` always rebinds entry 0 + table-0 TableSlice from its args + wires both new JitRuntime arrays. Both spec_assert harnesses (non_simd + simd) call it. `scripts/regen_spec_2_0_assert.sh` adds `select` to NAMES with a rationale comment citing the d-42 / d-42b discharge chain; the regen produces 30 .wasm fixtures + a 148-line manifest. The 4 historical `select.wast: as-call_indirect-{first,mid}` trap-FAILs flip to PASS end-to-end. Mac aarch64 + OrbStack `test-all` green; spec_assert non-simd 16276/0/679 → **16369/0/732** (+93 PASS, 0 FAIL, +1 manifest); simd 13301/0/440 unchanged. (`e0bc2ef8`) `[x]`
- **9.9-l-1b-d093-d42** — D-112 JIT side: multi-table `call_indirect` dispatch (Wasm 2.0 / spec §3.4.6 + §4.4.10.1). New `TableJitCallInfo` extern struct (`funcptr_base` + `typeidx_base`, 16-byte stride) + `tables_jit_ci_ptr` array in JitRuntime (`head_size` 200 → 216; new offsets at 200 + 208). Entry 0 reuses table-0's flat `funcptrs/typeidxs` so the legacy scalar fast path (X24/X25/X26 reload on arm64, `[R15+scalar_off]` on x86_64) stays unchanged; entries `k > 0` back a per-call slow path that loads bases via `tables_ptr[k].len` (size) + `tables_jit_ci_ptr[k].{funcptr,typeidx}_base` (data). `emitCallIndirect` on both backends now branches on `ins.extra` (= `table_idx` from `lower.zig:927`). `src/engine/runner.zig::setupRuntime` allocates per-table `extra_funcptrs` / `extra_typeidxs` arenas keyed by per-table offsets; the elem-section loop drops the `sync_table0` gate and writes funcref-typeidx pairs into the right table's slice. Edge fixture `test/edge_cases/p9/call_indirect/multi_table_dispatch.{wat,wasm,expect}` (2-table module, `call_indirect $t1 (type $sig) (i32.const 6) (i32.const 0)`) flips FAIL→PASS=42. Mac aarch64 + OrbStack `test-all` green; spec_assert non-simd 16276/0/679 unchanged; simd 13301/0/440 unchanged. **spec_assert harness wire-up + `select` NAMES enable deferred to d-42b**. (`ebe2a992`) `[x]`
- 9.9-d-5 (ARM64 v128 select + 8 lane mem handlers per chunk-granularity rule. 8 ops `v128.{load,store}{8,16,32,64}_lane`: bounds-check via shared `v128MemPrologue` (D-060 helper) → scalar LDR{B,H,W,X}/STR{B,H,W,X} W17 + INS/UMOV V<vec>.<sz>[lane] (per Wasm spec §4.4.7.4 / §4.4.7.5; the 8 INS/UMOV/scalar-load-store encoders pre-existed). 1 op `select`/`select_typed` v128 dispatch path: existing arm64/emit.zig handler now branches on `alloc.shapeTag(val1_v) == .v128` to `emitV128Select` recipe `CMP cond,#0; CSETM X17,NE; DUP V<mask>.2D,X17; BSL V<mask>.16B,V<v1>.16B,V<v2>.16B`. New encoders: `inst.encCsetmX` (alias of CSINV; Arm IHI 0055 §C6.2.59) + `inst_neon.encDupGen2D` (DUP V.2D from X; §C7.2.106) — verified against clang-as via 6 unit tests. Per-arch divergence vs x86_64 (which PEXTRs before its prologue to avoid RCX clobber) noted in handler comments: ARM64 prologue → UMOV → STR runs cleanly because X17 is a transient post-prologue. Mac aarch64 simd_assert totals **unchanged at 226/36/296** — new emit handlers wired but unreachable for `simd_select.0` because `populateShapeTags` returns null when no SIMD ops appear in the function body (`local.get v128 / select` shape). Filed as **D-061** (9.9-d-6 discharge target). `[x]`).

- **9.13-0** — §9.13-0 Cat IV windowsmini reconcile sweep CLOSED. W4 retry 11 at `be107343` is the first full Windows `test-all` green: `spec_assert_runner_non_simd: 23427 passed, 0 failed, 2499 skipped` across 84 manifests + Build Summary 39/39 + `[run_remote_windows] OK`. The retry chain (11 cycles) surfaced 5 Win64-specific failure classes; 3 closed by autonomous fixes (W3.b-1/2/2b SEH bridge via VEH + threadlocal RecoveryInfo + callJitOrTrap + STACK_OVERFLOW filter); 4 documented as debt-trackable SKIP tokens per ADR-0078 taxonomy: D-162 (SKIP-WIN64-EXHAUSTION), D-163 (SKIP-WIN64-CALL-INDIRECT-TRAP), D-164 (SKIP-WIN64-MULTI-RESULT covering type-all-* + as-binary-all-operands / as-mixed-operands / fac-ssa via narrow name-match — root cause Win64 ABI hidden-pointer mismatch + JIT epilogue cap=1 silent-truncate). Spike `private/spikes/win64-recovery-pc-sp/` Status: merged-into-prod (production SHA `c97cb72f`). `scripts/should_gate_windows.sh --record` set HEAD `be107343` as last windowsmini-tested commit. Permanent value across cycles: trap handler module (`c97cb72f`), callJitOrTrap helper + per-callsite wiring (`72d8a0e8`, `af4eff55`), VEH filter + per-directive beacons (`09ee5bb9`, `ee7403ff`, `aeb01a23`), narrow SKIP discipline (`b6038963`, `be107343`). Phase 10 entry gate (§9.13) opens next — collaborative review per `.dev/phase10_transition_gate.md`; bucket-1 user touchpoint per `/continue` hard-gate detection. (originally flipped at `d771356f`; **REVERTED 2026-05-22 per `.dev/phase9_close_master.md` §3** — Tier-0 user direction reframed Win64 SKIPs as workarounds, not legitimate out-of-scope; row re-opened until Tier 1 land.) `[ ]`

- **9.12-F** — Phase-9-eligible debt cohort substantially addressed per ADR-0102 (`a6e3eb4f`; per-row predicate (a)/(b)/(c)/(d)). 4 of 6 cohort-named rows closed (D-090/D-141/D-081/D-055 cleared at `0497c3b0` one-cycle retention); 2 remain genuinely blocked-by named structural barriers (D-094 x86_64 multi-result indirect-buffer ABI; D-062 arm64 v128 9th+ stack overflow). Non-cohort rows all carry explicit Phase 10/11+/v0.1.0 RC/upstream-fix-pending barriers. Original numeric "< 15" exit reframed by ADR-0102 to "substantially addressed" predicate. Active debt 22 → 21 after D-136 close (`0497c3b0`). (originally flipped at `d771356f`; **REVERTED 2026-05-22 per `.dev/phase9_close_master.md` §3** — Tier-0 user direction reframed Win64 SKIPs as workarounds, not legitimate out-of-scope; row re-opened until Tier 1 land.) `[ ]`
- **9.12-I** — ADR + lesson + private/ closure: `check_adr_history.sh --gate` exit 0 (3 SHA backfills landed at `15e790e9`); `check_lesson_citing.sh` 0 (already clean); ADR `Accepted` count 35 → 29 (6 §9.12-era closures at `006f0d6d`: ADR-0064 / 0098 / 0101 / 0100 → Closed (§9.12-A DONE); ADR-0062 / 0071 → Closed (§9.12 DONE)). D-149 SHA backfill ongoing for the 6 new Revision history rows filed this cycle (next D-149 sweep). Skip-ADR Status wording cleanup + Lesson promotion candidates scan deferred to incremental future cycles (no blockers; quality-improvement work). (originally flipped at `d771356f`; **REVERTED 2026-05-22 per `.dev/phase9_close_master.md` §3** — Tier-0 user direction reframed Win64 SKIPs as workarounds, not legitimate out-of-scope; row re-opened until Tier 1 land.) `[ ]`

- **9.13-0** (re-close 2026-05-24, post-ADR-0104 reframe) — §9.13-0 CLOSED per `.dev/phase9_close_master.md` §5.3a + §6. **Phase A**: A.1 (D-157 `SKIP-NO-LINK-TYPECHECK`=0 across 3 hosts) `[x]`; A.2 (D-139 c_api Instance lifecycle audit + 4 coverage tests in `src/api/instance.zig`) `[x]`; A.3 (D-079 (ii) / D-170 v128 cross-module global imports — in-source `src/api/instance.zig` test `wasm 2.0 cross-module v128 global via wasm_instance_new: D-170 close` at `00cb63de` — structurally disproven, regression-detector landed; D-079 retires both (i) spec-runner + (ii) c_api sub-gaps; lesson `2026-05-24-c_api-v128-spec-boundary.md` Citing-backfilled) `[x]`. **Phase B**: B.1 windowsmini reconcile at HEAD `14f35e66` — run 1 D-028-class flake (zwasm-spec-simd.exe exit code 3 mid-corpus on simd_bit_shift; signature distinct from original D-028 IPC-timeout — exit 3 vs exit 1, no `test runner failed to respond` line); run 2 GREEN with `[run_remote_windows] OK` + simd_assert_runner 13351 PASS / spec_assert_runner_non_simd 25457 PASS / wast_runtime_runner 266+5 PASS / spec_assert_runner 212 PASS / realworld_runner 55 PASS — ALL `0 failed`; ZERO `SKIP-WIN64-EXHAUSTION` / `SKIP-WIN64-CALL-INDIRECT-TRAP` / `SKIP-WIN64-MULTI-RESULT` / `SKIP-NO-LINK-TYPECHECK` emission per `check_phase9_close_invariants.sh --gate` 18/18 PASS. Single re-iterate per master plan §5.3a B.1 — within scope. B.2 row `[x]` flip in this commit. SHA backfill for §9.x bare-Status rows deferred to the Phase-9-close batch commit after §9.13 hard gate clears (per master plan §5.4). Next ROADMAP row: §9.13 (🔒 Phase 10 entry gate; `.dev/phase10_transition_gate.md`) — bucket-1 user touchpoint per `/continue` hard-gate detection. `[x]`
